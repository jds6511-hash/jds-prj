[pytest]
; 경로 없이 pytest 실행 시 work/·data/ 등 대용량 산출물 디렉터리까지 수집 대상으로
; 스캔해 수십 분간 멈추는 문제 방지 (2026-07-09 실측: 무경로 실행 16분+ 무응답)
testpaths = tests
; src 는 공개 패키지(jds_video), _internal 은 그 안의 검증된 구현.
; 둘 다 있어야 `pytest` 만으로 전체 계약이 돈다.
pythonpath = src src/jds_video/_internal
