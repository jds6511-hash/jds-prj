# 프로젝트 규율 (한국어 영상 모먼트 검색 파이프라인)

어떤 모델·세션이든 이 프로젝트에서 작업할 때 반드시 지킬 규칙. 위반은 연구 타당성을 깨뜨린다.

**현재 배포:** `Qwen2.5-VL-3B / P0 / 4bit`, KURE-v1, z-score 융합, α=0.5(CLI 주입).
`Qwen3-VL-4B`는 **후보이며 채택 상태가 아니다.** 진행 상황·수치는 이 파일이 아니라
`docs/작업현황_*.md`와 `docs/preregistration/`이 source of truth다.

## 절대 규칙 (방법론)

1. **test 재평가 금지.** test 39건은 확정 config로 공식 평가가 끝났다(접촉 이력: 튜닝 0회,
   확정 절차 **공식 평가 7회** = 검색(M6) 5회 + 리포트(M9) 2회 — DESIGN_SPEC 8-6에 ①~⑦ 사유·경계 사례 1건 명시).
   test 39는 **비가역 자원**이다. 모든 튜닝·ablation·실험은 dev 전용이고, test를
   caption 후보 선택·튜닝에 쓰지 않는다.
   **확정 config 변경은 test 재평가의 필요조건일 뿐 충분조건이 아니다.** dev 절차 완료 +
   별도 test-opening 사유 문서화 + **사용자 명시 승인** 후에만 가능하다.
   추가 test-opening은 그 자체가 별도 승인 사건이다. **39→72 확장(신규 33건)은 준비돼
   있으나 별도 test-opening 이벤트로 HOLD**하며 본 질의 파일에 병합하지 않는다.
2. **캡션 수동 편집 금지.** 캡션 수정은 `common.is_corrupted_caption` 자동 판정분만
   `m3 --recaption-corrupted`로 재생성(leakage 방지). 내용을 보고 고르거나 고치면 안 된다.
3. **라벨 작성 시 검색 결과 참조 금지.** GT는 프레임 실물 검증으로만(캡션 불신).
   test 라벨은 병합 전 유형별 목표 사전 등록. gt_seg_idx는 gt_start/end 파생 규칙 준수
   (유일한 예외 wl_q03은 8-6에 문서화됨 — 새 예외를 만들지 마라).
   **라벨용 도구는 `m5_search`·`m6_evaluate`를 import조차 하지 마라** — 제시 항목을
   순위로 고르면 선정 자체가 오염이다. `docs/probes/frame_human_kit.py`는 `rank==1`로
   항목을 고르므로 **라벨 작성에 쓰지 마라**(프레임 내용 진단 전용). 허용 도구는
   `scripts/label_contact_sheet.py`(프레임+시각만, 캡션·자막 없음)와
   `scripts/label_intake.py`(gt_seg_idx 자동 파생, 자동 병합 없음)다.
   두 도구는 `segments.json`을 직접 읽지 않고 **`scripts/label_guard.py`의
   allowlist**(`idx`·`start`·`end`·`rep_frame`)를 거친다 — 같은 파일에 `caption`·
   `subtitle`이 들어 있고 **기확보 영상에는 그것이 이미 존재**하므로 관행이 아니라
   도구가 막는다. `derive_gt_seg_idx`는 중립 모듈 `common`에 있다(2026-08-20에
   `m6_evaluate`에서 옮겼다 — 라벨 도구가 그 함수 때문에 평가 모듈을 import하는
   특례가 있었다). **이 조항에 예외는 없다.**
4. **변형 실험 격리.** config 사본에 paths.work/results 동시 분리, 항상 config.yaml에서
   재생성(scratchpad의 gen_ablation_configs.py 패턴). 본 config·본 인덱스를 실험으로
   오염시키지 않는다.
5. **α는 config에 없다** — CLI 주입(`--alpha`), 확정값은 results/alpha_search_dev.json의
   alpha_star. static_threshold=0(치환 off)·abstention max(sub,cap) τ=0.55·**융합 정규화
   z-score**(minmax는 유의 열세 실측, 2026-07-13 개정)는 실측 확정, 근거 없이 되돌리지 마라.

## 판단 기준

- **속도보다 정확성이 최우선.** 모델·구성 채택 판단에서 추론 속도를 **기각 사유로 쓰지
  마라**. 지연은 배치·양자화·flash-attn·서버 GPU로 줄일 여지가 있지만 정확도 손실은
  되돌릴 수 없다. 정확도 우열이 통계적으로 애매할 때만 속도를 타이브레이커로 쓴다.
  (예: STT arm C가 현행 대비 92배 느린 것은 기각 사유가 아니다 — 작업현황 2026-08-04 §1)

- **후보 모델이 진 것인지, 파이프라인이 이득을 죽인 것인지 반드시 가른다.**
  "비유의"를 "차이 없음"으로 쓰지 마라. 후보 검증 프로브는 아래 5개를 **전부** 하고,
  못 한 항목은 못 했다고 결과에 적는다. 각 항목은 실제로 났던 사고에서 나왔다
  (상세·사고 이력: DESIGN_SPEC 8-7 "후보 모델 검증 규약").
  1. **채널 격리** — 후보가 바꾸는 채널 **단독**으로도 잰다(캡션 모델이면 α=0.0,
     STT면 α=1.0). 융합에서는 개선분이 절반으로 희석돼 사라진다.
  2. **검출 한계 병기** — 이미 1위인 질의 수·남은 여지·CI 폭을 같이 적는다.
     dev 96·영상 3편에서 CI 폭이 ±0.08이라 +0.03짜리 실제 개선은 검출 자체가 안 된다.
  3. **현행 전용 설정 재탐색** — 프롬프트·토큰 상한·α는 전부 현행 모델로 튜닝된 값이라
     후보에게 불리하게 작동한다. α는 arm별로 재탐색해 기록하고, 프롬프트는 못 하면 기록.
     **단, arm별 α curve는 diagnostic이다.** 이미 frozen된 PRIMARY·adoption 규칙을
     결과에 맞춰 소급 변경하는 근거로 쓰지 마라. 새 α를 채택하려면 별도 사전등록이다.
  4. **동시점 대조군** — 후보와 현행을 **같은 하드웨어·라이브러리·코드 경로에서 함께
     생성**해 모델 효과를 비교한다. 시간에 따른 라이브러리·코드·생성 경로 차이를
     통제하기 위한 절차다. **저장된 산출물과 새 산출물의 차이를 별도의 "환경 효과"
     추정치로 해석하지 마라**(그 가설은 아래 실무 규칙대로 기각됐다).
  5. **생성물 전량 저장** — 표본 몇 개만 남기면 다른 각도로 볼 때 GPU를 다시 써야 한다.

## 실무 규칙

- **재캡셔닝 후 m4 실행 필수.** text_hash가 자동 감지·차단하지만(M5 ValueError), m4를
  돌려야 갱신된다. `--force` 불필요(해시가 변경 감지).
- **확정 인덱스의 캡션은 사전등록·승인된 절차 외에는 재생성하지 마라.** 허용되는 것은
  셋뿐이다 — ① 자동 오염 판정에 따른 정규 `--recaption-corrupted`, ② 사전등록된
  모델·config 변경 실험, ③ 사용자 승인 후 공식 재색인.
  이유는 **어느 변화가 지표를 움직였는지 사후에 분리하기 어렵기 때문**이다. 새 영상
  추가 시 **그 영상만** 돌린다.
  **"생성이 흔들려서"를 근거로 쓰지 마라 — 2026-08-18에 정정됐다.** 같은 서버·같은
  commit·같은 경로에서 AI Hub **2,328구간을 재생성했더니 상이 0건, 완전일치 1.0**이고
  MRR도 소수 4자리까지 같았다(`docs/재분석_2x2_2026-08-18.md` §8). 즉 greedy
  파이프라인은 통제된 조건에서 **결정적**이다. 앞서 2026-08-14↔08-17 dev 655구간에서
  8건(1.2%)이 달라 MRR이 0.5590→0.5466으로 움직인 것은 두 실행 사이에 **통제되지 않은
  차이가 있었다**는 뜻이고, 그 차이는 **여전히 미확정**이다.
- **2026-08-07에 관측한 큰 생성환경 페널티(Δ−0.0879)를 운영 규칙의 근거로 쓰지 마라.**
  dev에서는 노트북↔서버 캡션 완전일치율이 **25.6%**였으나, 2026-08-12 dev와 독립적인
  **AI Hub A-half 97영상·1,164구간에서도 완전일치율은 23.2%(76.8% 상이)**였다.
  그럼에도 **562질의 검색 성능 차이는 Δ−0.0046, CI[−0.0267, +0.0174]**로
  큰 방향성 환경 페널티가 **재현되지 않았다**(MDE ±0.036).
  즉 **기계를 건너면 캡션 문자열은 대부분 달라질 수 있지만, 그것을 곧 검색 성능
  저하로 해석해서는 안 된다.** AI Hub 562질의는 모델 확증에 사용한 **1,086질의의
  부분집합**이므로 두 결과를 서로 독립된 확증으로 세지 않는다.
  근거: `docs/probes/_scratch/aihub_env_check.json`, 작업현황 2026-08-12 §2.
- **정식 GPU 배치는 git에 등록된 스크립트만 쓴다.** 계획 → 소규모 canary → 검증 →
  본 실행 순서를 따르고, 장시간 배치는 **본 실행 전 사용자 승인**을 받는다.
  `pgrep` 문자열 매칭이나 즉석 `/tmp` 스크립트를 정식 배치의 제어 흐름으로 쓰지 마라
  (2026-08-17 사고 3건이 전부 여기서 났다 — 자기 런처 셸 매칭으로 GPU 8.5시간 유휴,
  배타 플래그 조합, `/tmp` 경로 해석 차이로 **편집본 ≠ 실행본**). 진행 판정은
  "프로세스가 사라졌는가"가 아니라 **이전 단계의 완료 마커 + validator PASS**로 한다.
  실행 commit SHA·config·provenance를 결과에 기록한다.
  콘솔 수치는 cp949로 깨지므로 **최종 수치는 항상 UTF-8 JSON 파일에서 확인**한다.
  완료 후 검증 절차는 `.claude/skills/pipeline-verify` 참조.
- **M8/M9는 로컬 실행 불가**(7B가 6GB 초과 실측, 3B는 예시 복사 오염으로 기각).
  격리된 오염 산출물을 되살리지 마라. **랩실 GPU 확보 완료(2026-08-06, <LAB_MACHINE> RTX 4090
  24GB)** — 7B를 `llm_4bit: false`로 올린다(VRAM 20.1GB 실측). 서버 실행은 `config.yaml`
  사본(`config_server.yaml`)으로 하고 본 config를 편집하지 마라. 저장은 `/ssd`,
  `HF_HOME=/ssd/$USER/cache` 필수(비대화형 SSH는 `.bashrc`를 안 읽으므로 명령마다 명시).
- **M9는 `split=="test"`가 하드코딩**돼 있어 **돌리는 것 자체가 test 접촉**이다.
  **M9는 test-opening 승인 없이는 실행 금지다.** M8이 dev에서 확정되는 것은 전제 조건일
  뿐이고, **그것만으로 M9 실행 권한이 생기지 않는다.** M8 확정 전에 돌리면 그 수치를 보고
  프롬프트를 고치게 되어 오염이고, M8 확정 후에도 별도 승인이 필요하다.
- **자막 크레딧 환각 필터**(`common.is_subtitle_credit`, 2026-08-06): STT가 무발화 구간에
  생성하는 `한글자막 by …`를 발화 단위로 제거한다. 캡션 쪽 `is_corrupted_caption`과 같은
  원칙 — **자동 판정만, 내용 보고 고르지 않는다**. 전체 일치로만 판정한다(문장 안에 섞이면
  실제 발화). 상세 DESIGN_SPEC 8-5(5).
- **완료 주장 전 검증**: 테스트 전체 실행(`python3 -m pytest tests/ -q`) + 해당되면
  브라우저 E2E(`scripts/m7_browser_e2e.py`). 증거 없는 "될 것이다" 금지.
- 코드 변경은 TDD(실패 테스트 먼저), 커밋 전 전체 테스트 통과 확인.

## 재시작 시

`docs/작업현황_*.md` 중 **가장 최신 파일**부터 읽는다(날짜를 이 파일에 박지 않는다 —
stale해진다). 확정 수치·다음 할 일·주의사항이 거기 있다.
어느 문서가 무엇인지는 `docs/README.md`(문서 지도)에 있다 — 특히
`docs/preregistration/`은 **내용을 고치지 않는 문서**다.
발표 예상질문 대응은 `docs/presentation/예상질문_방어.md`.
