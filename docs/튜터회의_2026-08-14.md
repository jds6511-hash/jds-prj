# 튜터 회의 자료 — 2026-08-14

**대상 기간:** 8월 11일(화) 오후 ~ 8월 14일(금)
**한 줄 요약:** 모델 교체를 실행에 옮기려다 **배치가 조용히 실패**했고, 그 과정에서
**M8 리포트가 요약이 아니라는 것**을 수치로 확인했다. 평가 규모 확장(39 → 72건)과
재평가 절차 사전 등록은 완료했다.

*직전 회의 자료: `docs/튜터회의_2026-08-11.md`*

---

## 1. 지난 회의 이후 한 일

| 항목 | 결과 | 커밋 |
|---|---|---|
| 최종 평가 확장 준비 | 신규 3편 인덱싱 + **라벨 33건** 작성 완료 (39 → 72건) | `8cdf16b` `1d2a921` |
| 8회차 재평가 절차 | **결과 보기 전** 판정 규칙 확정·커밋 | `d914e79` |
| 후보 모델 채택 블로커 | VLM 클래스를 모델 id로 고르도록 수정 (Qwen3-VL 적재 불가 해소) | `c053120` |
| 중간성과발표회 자료 | 13슬라이드 + 발표자 노트 (8/21용) | `9484edf` 외 4건 |
| 시연 검증 | 테스트 **216건 전부 통과** · 브라우저 E2E 19/19 · 인덱스 불변 | — |
| **채택 1단계 배치** | **실패** — 아래 §2 | — |
| **M8 리포트 품질 실측** | **결함 확인** — 아래 §3 | `fecb624` |

라벨링은 **연구자 본인이 직접** 했다. 프레임 실물만 보고 작성했고, 검색 결과나
시스템이 만든 캡션·자막은 보지 않았다. 유형별 목표는 작성 전에 등록했다.

---

## 2. 사고 보고 — 배치가 "성공"으로 끝났는데 캡션이 전부 비었다

### 무슨 일이 났나

캡션 모델을 Qwen3-VL-4B로 교체하기 위해 서버에서 10편을 재생성하는 배치를 걸었다.
배치는 `=== 1단계 완료 ===`를 찍고 정상 종료했다. 실제로는 **10편 중 3편만 성공**했다.

| 영상 | 결과 |
|---|---|
| dev 3편 | 재생성 성공, 빈 캡션 0건 |
| **test 7편** | **전 구간 실패 — 캡션이 빈 문자열로 덮였고, 그 상태로 색인까지 됐다** |

원인은 단순하다. **재캡셔닝에 필요한 프레임 이미지가 서버에 없었다.** 이전 실험에서
dev 3편만 올려뒀고 test 7편은 올린 적이 없다. 실패 로그가 1,525줄 쌓였다.

```
seg 0 캡션 실패: FileNotFoundError: work/panibottle_vietnam1/frames/seg_0000.jpg
```

### 왜 못 막았나 — 두 군데가 뚫려 있었다

1. **사전 검사가 절반만 있었다.** 코드는 `segments.json`에 `rep_frame` **필드가
   있는지**는 확인하지만 **그 이미지 파일이 실제로 있는지**는 확인하지 않는다.
   필드는 복사돼 있었고 이미지만 없었다.

2. **실패해도 결과를 저장한다.** 전 구간이 실패했는데도 빈 캡션을 디스크에 쓰고
   나서 종료 코드를 1로 냈다. 저장이 먼저였다.

```python
failed = caption_all(...)          # 1,525건 전부 실패
common.save_segments(...)          # ← 그래도 저장한다
if failed:
    sys.exit(1)                    # 죽는 건 저장 다음
```

### 피해와 복구

- **배포 인덱스(노트북)는 무사하다.** 11편 전부 캡션 결손 0, 프레임 전량 존재,
  무결성 해시 일치를 확인했다. 손상된 것은 서버 작업 사본뿐이다.
- 배치 직전 백업을 떠뒀기 때문에 **7편 전부 복구 가능**하다.
- 재개하려면 **test 7편의 프레임 약 1,518장을 서버에 올려야** 한다.

### 재발 방지

이번 주말에 두 가지를 코드에 넣는다.

- 재캡셔닝 시작 전 **프레임 파일 실물 존재를 전수 확인**하고, 없으면 시작조차 하지 않는다
- **실패율이 임계 이상이면 저장하지 않는다** — 부분 진행 보존과 전량 파괴를 구분한다

*이 사고는 성능과 무관한 운영 결함이지만, 자동화된 배치가 "성공"을 보고할 때
그 보고를 믿을 수 있어야 한다는 점에서 방법론 문제이기도 하다.*

---

## 3. M8 리포트가 "요약"이 아니라는 것을 수치로 확인했다

발표 자료에 M8 산출물을 싣다가 실물을 열어봤다. 겉보기에는 문장마다 근거 구간
번호가 붙어 그럴듯한데, 재보니 **요약이라고 부르기 어렵다.**

### 측정 결과 (panibottle_vietnam1, 21.8분)

| 항목 | 값 | 요약이라면 |
|---|---|---|
| 압축률 | **0.66 문장/구간** (88구간 → 58문장) | 훨씬 낮아야 한다 |
| 문장당 인용 구간 폭 | **2.1개** | 넓게 묶여야 한다 |
| 인용 순서 | **100% 시간순** | 되돌아오거나 묶여야 한다 |
| 어미 반복 | **34%가 "…이 보입니다"** | 다양해야 한다 |
| 문장 첫 단어 | **41%가 "남성이"** | 다양해야 한다 |

즉 지금 출력은 **구간 설명을 시간순으로 이어 붙인 것**이지 영상을 압축한 것이 아니다.
존댓말과 평어도 섞여 있다("보입니다" 20회 / "보인다" 6회).

### 더 심각한 것 — 뒤 2/3가 없다

**영상 262구간 중 87번까지만 다룬다. 커버 34%.** 마지막 문장이 중간에 끊기고 그
자리에 중국어 문자열이 들어가 있다.

```
비행기 내부에서 파란색 좌석에 헤드靠垫的图片未加载成功，请稍后再试或检查网络连接。
```

이건 문체 문제가 아니라 **결함**이다. 생성 상한을 16,384로 올리는 수정을 8/6에
했는데 이 산출물도 8/6자라, 그 수정이 반영된 뒤인지부터 확인해야 한다. 원인 규명은
GPU 없이 가능해서 주말에 한다(저장된 중간 출력 5개가 남아 있다).

### 발표 자료에는 어떻게 실었나

**개선 예정 단계로 명시했다.** 슬라이드 제목을 "M8 현재 출력 — 근거는 붙지만 아직
'요약'이 아니다"로 바꾸고, 위 측정치를 그대로 실었다. 잘 되는 것(근거 구간 인용)과
안 되는 것을 나눠 적었다.

**M9 채점 수치는 싣지 않았다.** 그 수치를 낸 판정자(Qwen2.5-7B)가 사전 게이트에서
탈락한 계측기이기 때문이다(직전 회의 §6). 판정 방식과 게이트 결과만 남겼다.

---

## 4. 앞으로 진행할 내용

### 4-1. 이번 주말 (서버 GPU 없이 가능)

| 순서 | 작업 | 소요 |
|---|---|---|
| 1 | **재캡셔닝 사전 게이트** — 프레임 실물 존재 확인 후 시작 | 30분 |
| 2 | **전량 실패 시 저장 금지** — 빈 캡션이 인덱스를 덮는 경로 차단 | 1시간 |
| 3 | **M8 절단 원인 규명** — 저장된 중간 출력으로 map/reduce 중 어디서 잘렸는지 판정 | 1시간 |
| 4 | **M8 요약 품질 척도 사전 등록** — §3의 지표로 "무엇이 개선인가"를 미리 고정 | 2시간 |
| 5 | 서버 손상분 복구 + 프레임 1,518장 업로드 (GPU 미사용) | 1시간 |
| 6 | 질의 표현을 바꿔도 같은 장면을 찾는지 측정 (dev 96, 노트북 GPU) | 3시간 |
| 7 | 작업현황 문서 갱신 | 1시간 |

**4번을 3번보다 먼저 끝내지 않는 이유**가 있다. 프롬프트를 고친 뒤에 지표를 정하면
결과를 보고 기준을 맞추게 된다. 그래서 GPU가 없는 지금 척도부터 고정한다.

### 4-2. 서버 GPU 확보 후 (약 4.5시간, 순서 고정)

`docs/test_재평가_프로토콜_2026-08-13.md`에 **결과를 보기 전에** 등록해둔 순서다.

```
1. 캡션 재생성    10편 · Qwen3-VL-4B/P1 · bf16            90분
2. 재임베딩       text_hash 변경 자동 감지                 20분
3. α 재탐색       dev 96에서만                             20분
4. τ 재캘리브레이션 dev 96 + 무관 20                       10분
5. test 병합      33건 추가 (39 → 72)
6. M6 재평가      ← test 접촉 8회차                         2분
7. M8 재생성      10편                                     35분
8. M9 재평가      14B 판정자                              1.8시간
```

3·4를 dev에서만 하는 이유는 test를 보고 α나 τ를 고르면 그 순간 튜닝이 되기 때문이다.

**사전에 고정한 판정 규칙 중 가장 중요한 것:** 새 설정의 test 수치가 기존보다 낮게
나와도 **채택을 철회하지 않는다.** 철회하면 test로 고르는 것이 되어 "튜닝 0회"가
깨진다. 그 경우 "독립 표본에서는 이겼으나 최종 평가에서는 재현되지 않았다"로 보고한다.

### 4-3. 그 이후

| 항목 | 상태 |
|---|---|
| M8 요약 프롬프트 개선 | §4-1 4번 척도 확정 후 착수 |
| M8 분할 크기 변형 실험 | 한 번도 안 바꿔봤다 (60구간·겹침 5) |
| Phase 4 회의록 생성·평가 | 설계 8/9 확정, 착수 전 |
| 더 큰 캡션 모델(8B·14B·32B) | 튜터 판단 대기 (직전 회의 §7-3) |

---

## 5. 자원 사용 — 어디에 많이 썼나

세션 기록에 남은 사용량을 그대로 집계했다(추정 아님).

### 전체

| 항목 | 값 |
|---|---|
| 모델 출력 토큰 | **31,490,323** |
| 캐시 읽기 토큰 | 8,240,402,616 |
| 캐시 생성 토큰 | 206,846,188 |
| 요청 수 | 28,457 |
| 도구 호출 | 14,523 |

*캐시 읽기가 압도적으로 큰 것은 긴 대화에서 앞선 맥락을 매번 다시 읽기 때문이다.
실제 생성 비용은 출력 토큰 쪽이다.*

### 출력 토큰 상위 6일 — 무엇을 하던 날인가

| 날짜 | 출력 토큰 | 그날 한 일 |
|---|---|---|
| 8/5 | **5,025,863** | STT 후보 비교 2차 — 길이 편향·표기·청킹 경계 교란을 하나씩 제거하며 재측정, 결국 **현행 유지 확정** |
| 8/6 | **4,160,542** | M8 reduce 퇴화 결함 4건 + M9 판정 결함 2건 수정, 화자분리 Phase 3, 자막 크레딧 환각 필터 |
| 8/7 | 2,596,919 | **후보 모델 검증 규약 5항목 확립**, AI Hub 외부 벤치마크 구축, KconfSpeech CER 평가 |
| 8/4 | 2,505,520 | STT 후보 3종 벤치마크 준비 — 한국어 파인튜닝 모델의 학습 누수 확인 |
| 8/9 | 2,204,719 | 평가 지표 자체를 검정, 캡션 길이 혼입 발견, 라벨링 도구 |
| 7/7 | 1,870,384 | 초기 파이프라인 M1~M7 구축 |

### 읽는 방식

- **가장 많이 쓴 두 날(8/5·8/6)은 둘 다 "고치기"였다.** 8/5는 STT 비교의 교란
  요인을 제거하는 데, 8/6은 M8·M9 결함을 잡는 데 썼다. 새 기능이 아니라
  **기존 결과를 믿을 수 있게 만드는 작업**에 가장 많이 들어갔다.
- **8/5의 결론은 "바꾸지 않는다"였다.** 가장 비싼 하루의 산출물이 기각 결정인데,
  이건 낭비가 아니라 **잘못 채택했을 경우의 손실을 막은 값**이다.
- **8/10은 커밋이 23건으로 가장 많았지만 토큰은 1.5M으로 적다.** GPU 배치를
  돌려놓고 기다린 날이기 때문이다. 토큰 사용량과 진척은 비례하지 않는다.
- 도구 호출 14,523건 중 절반 이상이 명령 실행(7,776건)이다. 코드를 쓰는 것보다
  **돌려보고 확인하는 데** 더 많이 썼다.

---

## 6. 튜터께 여쭙고 싶은 것

1. **M8을 어디까지 고칠 것인가.**
   지금 출력은 요약이 아니라 구간 나열이고, 뒤 2/3가 잘려 있다. 절단은 결함이라
   반드시 고쳐야 하는데, **"요약답게 묶는 것"은 프롬프트 개선으로 되는지 아니면
   구조(map-reduce 분할 방식)를 바꿔야 하는지** 판단이 필요하다.

2. **M8 품질을 무엇으로 잴 것인가.**
   §3의 지표(압축률·인용 폭·문체 반복)는 제가 이번에 만든 것이라 표준이 아니다.
   요약 품질을 재는 통상적인 방법을 쓰는 게 나은지, 아니면 이 과제 특성상 근거
   인용 구조를 재는 지표가 맞는지 조언을 구한다.

3. **직전 회의 §7의 3번이 아직 미결이다.**
   6GB 제약이 풀려 8B·14B·32B가 후보가 될 수 있다. **독립 확증을 통과한 4B로
   마무리할지, 더 큰 모델까지 볼지** 판단이 필요하다. 남은 기간을 고려하면
   4B로 확정하고 M8·Phase 4에 시간을 쓰는 쪽이 나아 보인다.

4. **이번 배치 사고를 보고서에 어떻게 쓸 것인가.**
   성능과 무관한 운영 결함이지만, "자동 배치가 성공을 보고했는데 실제로는 산출물이
   파괴됐다"는 사례라 재현성 관점에서는 기록할 가치가 있다고 본다. 다만 최종
   보고서에 넣을 성격인지는 판단이 필요하다.

---

## 부록 — 이번 기간 확정 수치

| 항목 | 값 | 근거 |
|---|---|---|
| 최종 평가 규모 | 39건 → **72건** (신규 3편 33건) | `label_kit/queries_new.jsonl` |
| 테스트 통과 | **216건** | `pytest tests/ -q` |
| 배치 실패 규모 | 10편 중 7편, 실패 세그먼트 1,525건 | `/ssd/<SERVER_USER>/logs/adopt_phase1.log` |
| M8 압축률 | 0.66 문장/구간 | `report.json` 재집계 |
| M8 영상 커버 | **34%** (262구간 중 87까지) | 위와 같음 |
| 8회차 소요 추정 | 약 4.5시간 | `docs/test_재평가_프로토콜_2026-08-13.md` |
| 누적 출력 토큰 | 31,490,323 | 세션 기록 집계 |
