# 작업현황 2026-08-22

`2026-08-20` 스냅샷은 **승인 ② 이전 상태**다. 그 문서만 읽으면 P2 입력이 아직 staging에
있고 M1~M4가 HOLD인 것으로 오해한다. 승인 ②는 실행됐고 색인 배치가 돌고 있다.

## 0. 배포·후보 상태 — 먼저 읽어라

```
배포        Qwen2.5-VL-3B / P0 / 4bit · KURE-v1(1024) · z-score 융합
            α = 0.5 (CLI 주입, config에 없음) · static_threshold = 0
            abstention max(sub,cap) τ = 0.55 · seg_len_sec = 5
후보        Qwen3-VL-4B — **채택 상태가 아니다.** 부호 역전이 미해결이다
test        39질의. 공식 평가 7회로 종료. **비가역 자원, 접촉 금지**
```

미해결 장벽은 그대로다 — AI Hub 1,086질의 `+0.031` vs dev 96질의 `−0.090`.
P2는 그 부호를 fresh long-form에서 다시 재는 절차이고, **판정이 나와도 채택이 아니다.**

## 1. 승인 ② 실행 완료 — P2 입력이 본 경로에 있다

```
승격        31편 → data/videos/ (p2_promote.py)
            선정목록·manifest·실파일 **3중 sha256 일치** 후에만 복사
            move 아니라 copy — staging은 P2 종료까지 보존
            복사 후 다시 계산해 대조. 기존 파일 덮어쓰기 없음(dest_conflict)
전송        35편 14GB 서버 이관. 대용량 4편이 부분 파일로 실패해 재전송
            (해시 게이트가 정확히 이 경우를 잡기 위해 있다)
입력 게이트  p2_input_verify.py (서버측 재계산)
            31 match · 4 legacy(출처 해시 없음, 기록만) · missing 0 · mismatch 0
provenance  35편 — recorded 31 · legacy_exempt 4 · missing 0
```

## 2. AV1 변환 — 사전등록 구간 격자를 유지한 채만 허용

```
원인        서버 cv2 5.0.0 내장 FFmpeg에 AV1 소프트웨어 디코더가 없다.
            ffmpeg CLI는 libdav1d로 디코드하지만 cv2는 못 한다.
            OPENCV_FFMPEG_CAPTURE_OPTIONS(hwaccel;none · video_codec;libdav1d)
            둘 다 우회 실패 — 후자는 h264까지 깨뜨렸다. **파일 손상이 아니다**
조치        AV1 2편만 libx264 crf16으로 변환. 오디오는 `-c:a copy`
            (재인코딩하면 STT 입력이 바뀌어 자막이 달라진다)
검증        변환 후 n_segments가 사전등록값과 같아야 통과.
            baekmansonghee_jirisan 183(기대 183) · pland_costco_hosting 395(기대 395)
            다르면 되돌리고 멈춘다. 원본은 `<id>.av1source.mp4`로 보존
대조 영향    두 arm이 **같은 변환본**을 입력으로 받으므로 arm 대조는 영향받지 않는다
기록        docs/probes/_scratch/p2_transcode_av1.json
```

## 3. 사고 3건 — 전부 캡션 시작 전에 죽었다 (GPU 낭비 0)

```
①  `-_mgcIUbbX4`   하이픈으로 시작하는 video_id를 argparse가 옵션으로 읽었다
                   → 인자를 등호 형식(`--video-id=…`)으로 고정 (5fd70b9)
②  AV1 2편        위 2절. m2에서 `프레임 저장 실패: seg 0` (e328823)
③  ffmpeg 4.4.2   `-fps_mode`는 5.1부터다 → 1프레임 실행으로 능력을 탐지해
                   `-vsync 0`으로 대체. **버전 문자열로 추측하지 않는다** (ab73e1c)
```

**근본 원인은 세 개가 아니라 하나다** — CANARY가 신규 avc1 1편만 돌렸다. 기확보분(AV1
포함)을 밟지 않아 ②가 FULL 31편째에서야 드러났다. 그래서 CANARY를 **입력 종류마다 한 편**
(신규 / 기확보)으로 넓혔다. 이제 두 종류를 전부 통과해야 FULL에 들어간다.

## 4. 색인 배치 — 두 arm이 caption_model만 다르게

```
arm         3b = Qwen/Qwen2.5-VL-3B-Instruct (base) · 4b = Qwen/Qwen3-VL-4B-Instruct
            둘 다 4bit
단계        m1 → m2 → m3(base arm 전체) → mirror_frames → m3 --captions-only(4b)
            → m4(양 arm)
왜 이 순서   `m3 --subtitles-only`는 기존 캡션을 **보존만 하고 만들지 않는다**(8-5(7)).
            신규 색인에서 거부된다. 그래서 base arm을 정식으로 돌리고 프레임·자막을
            미러링한 뒤 후보 arm은 캡션만 생성한다 — STT는 한 번, 자막은 바이트 동일
```

**이 순서는 성능 최적화 대상이 아니다 — 비교가능성 불변식이다.** 다음 세션이 "arm별로
독립 실행하면 병렬화된다"는 이유로 순서를 되돌리지 않게 이유를 남긴다.

```
STT를 arm별로 두 번 돌리면   Whisper 출력이 arm 간에 달라질 수 있고, 그러면 두 arm이
                          caption_model **말고도** 자막에서 달라진다. PRIMARY Δ에
                          캡션 모델 효과와 자막 차이가 섞여 귀속이 불가능해진다
프레임을 arm별로 추출하면    같은 근거로 시각 입력이 갈릴 수 있다. 그래서 미러링한다
게이트가 실제로 막는다       검증 훅이 자막 sha256 동일·프롬프트 해시 동일·구간 수
                          사전등록 일치를 요구한다. 순서를 바꾸면 통과하지 못한다
격리        config_p2_{arm}.yaml을 config.yaml에서 재생성.
            paths.work=work_p2_{arm} · results_p2_{arm}
검증 훅      scripts/hooks/p2_index_hook.py — 비교가능성 12게이트
            (모델 id·양자화·프롬프트 동일·자막 동일·구간 사전등록 일치·
             캡션 결측 0·text_hash·emb shape)
```

`index_text_hash`는 구간별 자막+캡션만 해싱하므로 provenance 같은 최상위 키를 넣어도
색인이 무효화되지 않는다.

## 5. 현재 실행 상태

```
run_id      p2idx_0821d — **FULL 완주 · validator PASS · RUN_COMPLETE 기록됨**
commit      ab73e1c · plan_hash d0bb2330e371c416… · returncode 0
소요        74,811.3s (20.8h) — 08-22 19:21 시작, 08-23 16:08 종료
단계별       m1 94s · m2 3,997s · m3_base 40,988s · mirror 3.8s ·
            m3_captions 28,808s · m4 921s
환경        kixlab2 · RTX 4090 · py3.12.13 · torch 2.13.0+cu130 · transformers 5.14.1
검사        **17항목** 전부 true (validator_version 2).
            앞서 "18항목"으로 적었던 것은 출력 JSON의 키 수를 센 것이고
            `validated_stage`는 검사가 아니라 라벨이다
산출물       3b Qwen2.5-VL-3B-Instruct rev 66285546 / 4b Qwen3-VL-4B-Instruct rev ebb281ec
            양 arm 4bit · 35편 9,115구간 · 구간 격자 35/35 사전등록 일치
            빈 캡션 0 · text_hash 35/35 일치 · emb (n,1024)×2 35/35
            prompt_sha256 b7c2598a… 양 arm 동일 · 자막 sha256 arm 간 전건 동일
```

**캡션 내용은 열지 않았다** — GT 315건을 작성해야 하므로 보고에도 넣지 않는다.

`canary_validated`는 **run_id별 상태**다 — 이전 run의 PASS가 새 run으로 넘어오지 않는다.
resume은 설계상 금지이므로 실패하면 새 run_id로 간다.

## 5-1. I1 detector validation — one-shot 소비 완료

```
라벨        A 83/83 · unclear 0 · cjk_text_present 0 → **B단계 공집합**
게이트      carried 재현 PASS (baseline 71 · primary 71 · fallback 70 · drift 71)
판정        primary·fallback 둘 다 우세 관측 (recall CI가 현행과 비중첩, precision >= 0.95)
채택        fallback R_only(2) — **validation 우열이 아니라** 사전등록된 단순 규칙 우선
어긋난 것    산출물의 separable_on_fresh_data=false가 관측으로 반증됐다
            (fresh C2에서 2건이 두 후보를 갈랐고 그 2건은 전부 TP)
```

상세는 `docs/I1_detector_validation결과_2026-08-22.md`. 재튜닝은 하지 않는다.

## 5-2. P2 질의 intake 도구

```
도구        scripts/p2_label_intake.py (make / build) · 테스트 34건
격리        segments.json을 아예 열지 않는다 — 구간 수·영상 길이는 사전등록 파일에서
            읽는다. m5_search·m6_evaluate·frame_human_kit import 0
기확보 4편   선정표본에 duration_sec이 없어 파일을 직접 잰다(m1과 같은 cv2 경로).
            n_segments×5 상한은 실제보다 0.57~3.37s 느슨했다 — 그만큼 영상 끝 뒤의
            gt_end가 통과할 수 있었고 그 구멍을 닫았다. 파일이 없으면 멈춘다
출력        label_kit/p2/p2_queries_staging.jsonl · split="p2" · 자동 병합 없음
```

## 5-3. 운영 결함 1건 — CANARY 마커가 같은 run 디렉터리에 남는다

```
증상        CANARY가 STAGE_m1..m4_index_DONE을 쓰고, 같은 run_id로 FULL을 돌리면
            그 마커가 FULL 시작 시점에 이미 존재한다. 상태를 사람이 잘못 읽는다
            (2026-08-22 실제로 오독했다 — FULL이 m2 중인데 m4까지 끝난 것으로 보였다)
영향 범위    **판정·수치에는 섞이지 않는다.** 확인한 것 셋:
            ① 단계 소요는 배치 안의 time.time()에서 나온다 — 마커를 읽지 않는다
            ② 마커는 기록일 뿐 건너뛰기 근거가 아니다. FULL은 전 단계를 다시 돈다
            ③ validator hook은 *_full.json을 *_canary.json보다 우선한다
            그리고 launcher의 no_stale_marker는 RUN_COMPLETE.json만 본다
이번 run     **손대지 않는다.** 실행 중 코드를 바꾸면 execution_commit 규율이 깨진다
FULL 이후     마커 이름을 stage_mode로 분리(CANARY_STAGE_* / FULL_STAGE_*)하거나
            마커 안에 stage_mode·created_at·산출물 해시를 넣어 status reader가
            현재 실행의 것인지 확인하게 한다
```

## 6. GO / HOLD

**GO** (추가 승인 없이 진행)

```
I1   완료 — A 83/83, one-shot 소비, fallback R_only(2) 채택 (5-1절)
P2   색인 완료 — FULL validator PASS · RUN_COMPLETE (5절)
P2   컨택트시트 생성 — 35편, 3B arm의 공통 프레임 기준, label_guard 경유,
     타일에 프레임·시각·세그먼트 번호만. 격자 불일치·프레임 누락은 fail-closed
P2   시트 육안 확인 후 질의 315건 + GT 라벨 작성
     허용 도구 label_contact_sheet · p2_label_intake만
     금지 3B/4B 캡션 · 검색 결과 · frame_human_kit
```

**HOLD**

```
GT 작성 완료 전 P2 평가 실행
캡션 내용 열람 · 검색 결과로 GT 위치 결정 · GT 결과를 보고 query·type·config 변경
test 접촉·개방 (M9 실행 포함 — M8 확정만으로 권한이 생기지 않는다)
4B 채택 — P2가 어떻게 나오든 자동으로 열리지 않는다
결과를 보고 reservoir·질의·GT·config 수정
I1 validation 결과 후 detector 재튜닝
39 → 72 확장(신규 33건) — 준비돼 있으나 별도 test-opening 이벤트
```

## 7. 판정 규칙 — 결과를 보기 전에 고정된 것

```
PRIMARY     Δ_deploy = MRR_cap(4B q4) − MRR_cap(3B 4bit), caption-only α = 0.0
단위        paired video-cluster bootstrap, cluster = 영상 (k = 35)
판정 불가    achieved CI half-width > 0.04 → "이 규모로는 판정 불가"로 멈춘다
```

`k = 35`는 통계적 optimum이 아니다 — 필요 cluster 추정이 16~53으로 3배 어긋나고
reservoir 천장이 37이라 **어떤 선택도 half-width ≤ 0.04를 보장하지 못한다.** 즉
"판정 불가"는 실패가 아니라 사전등록된 정상 출구다.

## 8. 발표 자료

`docs/presentation/중간성과발표_2026-08-21.pptx` 15슬라이드로 재작성. 낡은 주장
3건(4B 교체 예정 · test 72건 · 10편/2,173구간)을 제거하고 11편/2,568구간 · 최종평가
39건(33건은 닫아둠) · 후보 보류로 맞췄다. 시간 단위 분절 근거와 5초 선택 근거(dev 96:
3초 0.559 / 5초 0.570 / 10초 0.384 MRR), 캡션 모델 7종 정량 비교, 실제 프레임 캡션
예시를 넣었다.

## 9. 다음 승인 지점

```
1  FULL 완주 → validator PASS → 산출물 보고 (여기까지 자동)
2  [승인 필요] P2 질의·GT 완성 후 평가 실행 여부는 승인 ② 범위 안이지만,
   결과가 4B 채택 논의로 넘어가는 순간 별도 승인 사건이다
3  [완전 별도 승인] test-opening — P2 결과와 무관하게 자동으로 열리지 않는다
```

`docs/preregistration/`은 **내용을 고치지 않는 문서**다. 이탈은 보충으로 적는다.
문서 지도는 `docs/README.md`.

## 10. 2026-08-23 추가 — P2 검색 러너 `p2_retrieve.py` (GT 완성 전 동결)

`p2_evaluate`는 `--base`/`--candidate`로 arm 결과 JSON 2개를 받는데 **그 JSON을 만드는
러너가 없었다.** 기존 `m6_evaluate`로는 못 돌린다 — 두 군데서 막힌다.

```
m6_evaluate:67   assert q["split"] in ("dev","test")  → staging의 split="p2"를 거부
per_query 키      m6는 "mrr", p2_evaluate는 "rr"을 읽는다
```

**m6의 split 게이트는 넓히지 않았다** — 그 게이트가 test를 지키는 장치다. 대신
`scripts/p2_retrieve.py`를 얇게 분리했다. GT를 열기 전에, 합성 fixture만으로 TDD했다.

### 후보 풀 = 그 영상의 세그먼트 (러너가 새로 만든 규칙이 아니다)

```
보충2 §2-1 표 첫 행   "후보 풀 크기 (영상별 세그먼트 수) | 연속 | 연속 변수로 그대로 기록"
사전등록 §40 P1-b      "AI Hub 검색을 영상 내 12개에서 전체 2,328세그먼트 풀로 바꿔"
                      → 영상 내 검색이 baseline이고, 전체 풀은 P1의 조작이었다
m6_evaluate:94        search_fn(q["text"], indexes[q["video_id"]], …) — 배포 경로도 영상별
```

### 랭킹 동일성 — 캡션 코사인만 계산하는 근거

M5는 코사인 → per-query z-score → 정적 치환 → α 가중합이다. `alpha=0.0`이고
`static_threshold=0`이면 치환 마스크가 전부 False이므로 최종 점수는 캡션 코사인의
**단조 변환**이고 순위가 같다. 그래서 자막 임베딩을 읽지 않는다.
**`static_threshold != 0`이면 그 등식이 깨지므로 fail-closed로 거부한다** — 치환이 켜지면
자막 점수가 캡션 채널로 들어와 캡션 단독이 아니게 된다.

### fail-closed 목록

```
동결 GT      split="p2"만 · 315/315만 · sha256 없거나 불일치면 거부
후보 수      색인 n_segments != 사전등록 선정표본 n_segments면 거부
gold        gt_seg_idx가 후보 범위 밖이면 거부. **후보에 gold가 없으면 RR=0이 아니라 오류**
arm 혼입     config caption_model != arm 기대값이면 거부
            색인 caption_provenance.model_id != arm 기대값이면 거부 (경로 뒤바뀜 차단)
색인 정합    embed_model 불일치 · text_hash 불일치 · 세그먼트 수 불일치 거부
부분 완주    한 건이라도 실패하면 예외를 던지고 **파일을 쓰지 않는다**
```

### 산출 스키마 (작게 둔다)

```json
{"run": {"arm":"3b","gt_sha256":"…","query_count":315,"alpha":0.0,
         "caption_only":true,
         "provenance":{"caption_model":"…","embed_model":"…","static_threshold":0,
                       "seg_len_sec":5,"work_root":"…","git_head":"…",
                       "by_video":{"<vid>":{"n_segments":361,"text_hash":"…",
                                            "work_dir":"…","model_id":"…",
                                            "model_revision":"…","prompt_sha256":"…"}}}},
 "per_query": [{"query_id":"…","video_id":"…","rr":0.5,"rank":2,"n_candidates":361}]}
```

`rank`·`n_candidates`는 audit용이고 PRIMARY는 `rr`만 쓴다.

### 경계

```
비교·verdict·재표집을 하지 않는다 — 두 arm 비교는 p2_evaluate 단독
m6_evaluate·p2_evaluate·m5_search를 import하지 않는다 (import 노드로 테스트)
work_p2 경로를 하드코딩하지 않는다 — config로만 받는다
exclusion 기계를 러너에 두지 않았다. 사전정의 exclusion은 p2_evaluate의 --exclude다
assert_same_query_set은 query_id 집합·gt_sha256만 본다 (수치를 보지 않는다)
```

테스트 51건. 전체 **1092 passed**.

### 실행 순서 (라벨 완료 후)

```bash
python scripts/p2_label_intake.py build                      # 315/315 검증
# staging JSONL sha256 동결 → <GT_SHA>
python scripts/p2_retrieve.py --arm 3b --config config_p2_3b.yaml \
    --gt label_kit/p2/p2_queries_staging.jsonl --gt-sha256 <GT_SHA> --out results_p2/arm_3b.json
python scripts/p2_retrieve.py --arm 4b --config config_p2_4b.yaml \
    --gt label_kit/p2/p2_queries_staging.jsonl --gt-sha256 <GT_SHA> --out results_p2/arm_4b.json
python scripts/p2_evaluate.py --base results_p2/arm_3b.json --candidate results_p2/arm_4b.json
```

### 사전동결 테스트 조정 (규칙은 그대로)

`p2_gt_freeze.freeze()`는 **채워진 CSV를 거부한다** — 동결은 작성 전 상태의 스냅샷이므로
의도된 fail-closed다. 라벨링이 시작되자(2행) 실입력 테스트가 이 규칙에 걸렸다.
**규칙을 느슨하게 만들지 않고** 테스트의 검증 대상을 기록된 산출물
(`docs/probes/_scratch/p2_gt_freeze.json`)로 옮겼다. `p2_gt_freeze.OUT` 상수를 추가해
그 경로가 코드와 argparse 기본값의 단일 출처가 되게 했다.
