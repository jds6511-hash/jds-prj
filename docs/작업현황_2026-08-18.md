# 작업현황 스냅샷 (2026-08-18)

이전 스냅샷 `작업현황_2026-08-17.md`. 이 문서가 **재시작 진입점**이다.

---

## 0. 지금 어디인가

| | |
|---|---|
| **배포 구성** | `Qwen2.5-VL-3B / P0 / 4bit` · KURE-v1 · z-score 융합 · α\*=0.5 · τ=0.55 — **불변** |
| **8회차** | HOLD 종료(I1·A2 FAIL). **test 미접촉.** 공식 평가 7회 · 튜닝 0회 |
| **캡션 트랙** | 2×2로 귀속 닫음. **여기서 멈춘다** |
| **다음 후보군** | **`4B/P0`·`4B/P1`** — 잠정 선호 `4B/P0`, 채택 결정 아님 (§2-3) |
| GPU | 유휴. 실행 중 배치 없음 |

---

## 1. 08-17~18에 닫은 것

### 1-1. 평가 절차 감사 → 결함 3건 중 3건 처리

`docs/감사_2026-08-17.md`. 외부 검토 지적을 받아 기계적으로 검증되는 것을 전수 확인했다.

| 결함 | 처리 |
|---|---|
| 자문 문서의 α 식이 거꾸로 | 정정. 코드가 정답 — `score = α·자막 + (1−α)·캡션` (`m5_search.py:39`) |
| `+0.0377`이 model+prompt 교락 | **2×2로 해소** — 재사용 표본에서 주로 model effect로 귀속 (§2) |
| `per_query` 미저장 | `m6_evaluate` 수정(`5017264`) + 보존 캡션 재임베딩으로 복구 |

통과 8건(테스트 253→256건, 인덱스 무결성, 사전등록 시점 17시간 선행, `gt_seg_idx` 파생,
`eval_test.json` 재집계 일치, 커밋 1회 등)은 감사 문서 §2에 있다.

### 1-2. 재표집 단위 — 클러스터 보정은 필요 없었다

`docs/재분석_전달분해_2026-08-17.md`. 주지표 `+0.0377`은 query·video-cluster·
video-macro **세 방식 전부 0을 배제**하고, 융합 `+0.0187`은 세 방식 전부 0을 포함한다.
쌍체 차이의 **ICC(1)이 음수**(−0.02~−0.07), 설계효과 < 1 — 질의 단위 CI가 반보수적이
아니라 약간 보수적이었다.

### 1-3. 융합 전달 구조 — "융합이 개선을 먹는다"는 주 원인이 아니다

캡션 순위가 오른 질의의 **76~81%가 융합에서도 오른다.** 흡수·역전 21~24%.
실제 손실은 **포화**(1,086 중 178~180건이 양쪽 arm 모두 융합 rank 1)와
**자막 2~5위 구간의 상쇄**다(포화 보정 후 전달률 그 층만 45~50%).

*도메인별 효과 주장은 철회했다 — 대조군 정밀도만 바꿔도 부호가 뒤집힌다.*

### 1-4. 융합 feature 진단 — **적응 α 트랙 종결**

`docs/재분석_융합feature_2026-08-18.md` · 사전등록 `융합feature진단_사전등록_2026-08-18.md`
(`f9acb69`, 데이터 읽기 전) · 실행 `2c87cab`(clean) · G1~G4 PASS · GPU 0.

§1-3의 "자막 2~5위" 층은 **GT를 알아야 보인다.** 실사용 질의 시점에는 모른다.
그래서 GT 없는 관측량 8개로 그 층을 판별할 수 있는지 쟀다.

> **사전등록한 8 feature·로지스틱으로는 판별하지 못한다.** OOF AUC **0.5812**,
> 영상 클러스터 CI **[0.5382, 0.6212]**. 사전등록 판정 **신호 없음**(하한 < 0.60).

**표본 부족 탓이 아니다.** 두 진술을 구분해 쓴다 — CI가 **참 AUC 0.622 이상을
배제**하고, 판정식("CI 하한 ≥ 0.60")상 "약한 신호"로 넘어가려면 점추정이 대략
**0.642 이상**이어야 했다(반폭 ±0.042 근사). 관측 0.5812는 그보다도 0.06 낮다.

캡션 채널·채널 합의 feature는 전부 우연 수준(0.494~0.517)이고, `4B/P0`로 캡션을
바꿔도 0.5862로 동일하다. **캡션 품질 문제가 아니다.**

**따라서 적응 α rule 사전등록을 열지 않는다.** 현재 late-fusion 구조 안에서
남은 지렛대는 없다. 구조 변경(reranking·gating)은 이 진단의 사정거리 밖이다.

### 1-5. α 곡선 — **α 재조정은 4B 때문에 필요한 게 아니다**

`docs/재분석_알파곡선_2026-08-18.md` · 사전등록 `alpha곡선_2x2_사전등록_2026-08-18.md`
(`88ea8be`) · 실행 `875dbe6` · H1~H4 PASS · VLM 0. **진단 전용, 배포 α\*=0.5 불변.**

> **AI Hub에서 α 최적점이 낮은 것은 4B의 성질이 아니라 데이터셋의 성질이다.**
> 현행 `3B/P0`의 best도 0.4이고 tie_set이 `4B/P0`과 거의 겹친다([0.0–0.4] vs
> [0.0–0.5]). AI Hub는 자막이 약해(α=1.0에서 0.4107) 곡선이 **단조 하락**한다 —
> dev(0.4에서 최대, 자막이 주 채널)와 모양이 반대다.

**4B의 이득은 α 전 구간에서 유지된다** — `4B/P0 − 3B/P0`이 11점 중 10점 양수,
8점에서 영상 클러스터 CI가 0 배제. **고정 α가 이득을 죽이고 있는 게 아니다.**

**best α에서도 새로 얻는 게 없다.** α=0.3에서 Δ=+0.0309인데 캡션 단독 Δ가
+0.0310이다. **병목은 α calibration이 아니다.**

**부수 소득 — I1과 독립인 P0 선호 근거.** `4B/P0`은 현행과 같은 plateau 폭(0.4)을
유지하는데 `4B/P1`은 0.1이고 α=0.5 이웃 하락도 가장 크다(0.0102, 현행의 2.3배).
지금까지 P0 선호의 유일한 근거가 **재설계 대상인 I1a**여서 순환 위험이 있었다.

### 1-6. I1 검증셋 — 시트 생성 완료, **사용자 라벨 대기**

사전등록 `I1검증셋_사전등록_2026-08-18.md`(`3ab4a8f`) + 보충 `B단계경계`(`8705e1b`).

셀 C0~C5 층화 → **130 인스턴스 → 116 프레임**(프레임이 4 arm 공통이라 라벨 1건이
4 arm에 재사용된다). `label_kit/i1_frames/` — 시트 3장 + 원본 116장 + `labels.csv`.

| 관측 | |
|---|---|
| **I1a 적중이 9,312 중 82건뿐** | C4를 전수로 전환(이탈 1건, 라벨 0건 시점 기록) |
| **C3(CJK 1–2 ∧ 적중) = 0** | 비율 규칙(>0.2)이 발동하려면 캡션이 10자 미만이어야 하는데 실제는 82~131자다. **짧은 CJK는 현행 규칙으로 절대 안 걸린다** |
| 검증 | arm별 적중 수가 2×2 보고와 정확히 일치(22/36/5/19) |

**B 단계 경계를 라벨 0건 시점에 고정했다** — A만으로는 precision/recall이 나오지
않는다. 그 전에는 **"A 기반 proxy"**로만 표기한다.

### 1-7. I1 A단계 — **오탐 0건, 문제는 미탐 쪽** (2026-08-18 완료)

`docs/재분석_I1검증셋A_2026-08-18.md` · 라벨 116프레임 전건 · `unclear` 16.2%.

> **I1a가 잡은 72건(전수) 중 화면에 한자·가나가 있던 프레임은 0건.**
> scene text를 오염으로 오인한 사례가 이 표본에 없다.

**순환 우려가 상당 부분 해소된다** — `4B/P0` 선호의 근거였던 I1a 적중이 오탐으로
부풀려진 값이 아니었다(§2-3의 잠정 선호 단서를 완화할 근거).

**반대쪽이 문제다.** CJK 1~2자만 흘린 캡션(모집단 800)은 화면에 글자가 없는데도
현행 임계(CJK 절대 3자)가 못 잡는다 — 표본 20 중 **19**가 화면 글자 없음.
`foreign_script_present`(≥1자) 분리안이 실측으로 지지된다.

**화면 CJK 기저율 0.0092**, 영상 클러스터 CI [0, 0.0308](61영상). AI Hub 프레임에
한자가 사실상 없다 — **C0 대조군을 넣지 않았으면 이 판단을 못 했다.**

**B단계 대상이 21건으로 줄었다**((가) `S069` 1건 + C2 20건). 보충 사전등록 문안과
도출 규칙표가 어긋나는 것을 결과 문서에 기록했다(문서는 고치지 않았다).

*한계: `unclear` 16.2% · 대표 프레임 1장이라 **drift 과대추정 방향** · C2는 800중 20.
`precision`·`recall`이라는 말을 쓰지 않았고 산출 JSON에 `recall` 키가 없다.*

---

## 2. 2×2 결과 (핵심)

`docs/재분석_2x2_2026-08-18.md` · 사전등록 `caption_2x2_사전등록_2026-08-17.md`(`c676c46`)
· 실행 `85e47f7` / `run_id=full_2026-08-17` · 6.16시간 · **validator 12항목 전부 PASS**.

### 2-1. 셀

| arm | 캡션 단독 | 융합 | I1a | I1b(CJK) | 길이 |
|---|---|---|---|---|---|
| 3B/P0 (현행) | 0.4773 | 0.4741 | 22 | 125 | 131.4자 |
| 3B/P1 | 0.4819 | 0.4678 | 36 | 174 | 83.6자 |
| **4B/P0** | 0.5083 | **0.4932** | **5** | 241 | 82.0자 |
| 4B/P1 | **0.5150** | 0.4928 | 19 | 341 | 97.6자 |

### 2-2. 귀속 — 재사용 표본에서 주로 모델 효과로 귀속

| 대비 | Δ | 판정 |
|---|---|---|
| model under P0 | **+0.0310** | BH 유의 · 세 단위 전부 0 배제 |
| model under P1 | **+0.0331** | BH 유의 |
| prompt at 3B | +0.0046 | 비유의 |
| prompt at 4B | +0.0067 | 비유의 |
| interaction | +0.0021 | 비유의 (p=0.895) |

**프롬프트는 검색이 아니라 오염에 작용한다** — P0가 두 모델에서 I1a를 각각 **14건**
줄인다(3B 36→22, 4B 19→5).

### 2-3. 후보군은 `4B/P0`·`4B/P1`, 잠정 선호는 `4B/P0`, 채택은 아님

A1~A5를 `4B/P0`·`4B/P1` 둘 다 통과. `3B/P1`은 4개 실패. **검색으로는 두 4B 셀을
구분하지 못하므로 후보군은 둘 다 유지한다.** 그중 `4B/P0`을 선호하는 유일한 근거는
**I1a 검출기 적중이 현행보다 적다**는 것이다(5 vs 22).

> **이 선호는 잠정이다 — 순환 위험이 있다.** 선호의 근거인 I1a가 §2-4에서 **재설계
> 대상으로 판정된 지표**다. 새 I1 정의에서도 `4B/P0`이 우위를 유지하는지 확인한
> 뒤에 최종 후보를 확정한다.

> **"오염이 적다"로 쓰지 마라.** I1b(CJK 1자 이상)는 `4B/P0`이 241건으로 현행
> 125건보다 **많고**, 최대 157자짜리 심한 사례도 남아 있다(§2-4). 지금 말할 수
> 있는 것은 **현행 검출기 기준 적중이 낮다**까지다. 의미론적 오염 판단에는
> 프레임 검증과 새 detector 정의가 필요하다.

**남은 장벽 3개.**

| # | 장벽 |
|---|---|
| 1 | **표본 재사용** — AI Hub 1,086을 두 번 썼다. 이번 결과는 선택·추정. 새 표본 확증 필요 |
| 2 | **정밀도** — 2×2는 전부 bf16. 배포는 6GB 노트북 4bit이고 4B는 4bit에서 −0.068. **`4B/P0` 채택은 서버 추론 전제** |
| 3 | **개방 게이트** — I1 검출기 재설계 + A2. test 재평가는 사용자 승인 |

### 2-4. I1 검출기 설계 문제 정량화

`is_corrupted_caption`의 임계가 **CJK 절대 3자**다. 4B는 1~2자만 흘리고(176·125건)
3B는 3~9자에 쌓여(21·35건) 여기서 갈린다 — I1a와 I1b가 역전한다.

`foreign_script_present`(≥1자, 진단)와 `language_drift`(긴 연쇄·고비율, hard fail)
분리가 필요하다. **이번에 hard gate로 승격하지 않았다** — 프레임 검증 후 다음 사이클
사전등록.

*단서: `4B/P0`은 적중이 5건으로 가장 낮지만 터질 때 최대 157자다. 빈도와 심각도를
함께 봐야 한다.*

### 2-5. 부수 — 같은 조건에서 생성은 결정적이다

같은 서버·같은 commit 재생성이 **n=2,328에서 상이 0건, 완전일치 1.0**(arm 2종).
2026-08-17에 근거로 쓴 "655구간 중 8건 상이"는 **통제되지 않은 차이** 때문이고 원인
미확정. CLAUDE.md 재생성 규칙과 DESIGN_SPEC 8-7(4) 근거를 정정했다. **규칙 자체는 유지.**

---

## 3. 실행 규율에서 보강한 것

| 항목 | 내용 |
|---|---|
| `--run-id` 분리 | FULL 산출물을 `aihub_2x2_captions/<run_id>/`로. 완료 마커 있는 run_id는 실행 거부 |
| validator 10항목 | 구간수·per_query 행수·프롬프트 해시·OCR 금지 배치·비양자화·K10 등 |
| 완료 마커 | **PASS일 때만** `RUN_COMPLETE.json` 기록. 마커 존재가 곧 성공 판정 |
| 실행 헤더 | 목적(test 아님)·사전등록 경로·execution commit + dirty·시작시각·마커 위치 |
| provenance 실효값 | 로드된 모델의 dtype·양자화·revision + 프롬프트 SHA256 + `prompt_forbids_ocr` |
| 민감값 스캔 | `scripts/scan_sensitive.py` — reachable history 전수(671 blob). 즉석 셸 대체 |
| 라벨 도구 경계 | `frame_human_kit.py`는 `rank==1`로 항목을 골라 **라벨 작성 금지**로 명시 |

**기동 중 잡은 사고 1건.** 첫 FULL 기동에서 `nohup` 로그를 리포 루트에 만들어
`dirty=True`가 됐다 — 6.5시간 뒤 validator FAIL로 마커를 못 쓸 상태였다. 2분 만에
중단하고 로그를 리포 밖으로 옮겨 재기동. **`git_head`·`dirty`를 결과 JSON에 박아둔
설계가 즉시 드러냈다.**

**민감값 스캔 자체 결함 3건도 잡았다.** 셋 다 "0건"을 무의미하게 만드는 종류다.

1. `subprocess(text=True)`가 cp949로 디코드하다 한글 경로에서 터지고 예외가 리더
   스레드에 삼켜져 `blob 0개 · 적출 0건 · exit 0`으로 **거짓 통과**했다.
2. `\b<LAB_MACHINE>\d*\b` 형태가 `<LAB_MACHINE>_gpu_server_usage`류를 놓쳤다
   (밑줄이 단어문자라 경계 불성립) — 수동 grep보다 약했다.
3. **스캐너 자신이 민감값을 평문으로 담고 있었다.** 서버 계정·랩 호스트명을 리터럴
   정규식으로 박아서, 이 스캔이 **자기 파일을 적출**했다. 실제 값은 gitignore된
   `SERVER_LOCAL.md`에서 **런타임에 읽도록** 바꿨고, 끝자리 숫자를 뗀 접두 패턴도
   함께 생성해 커버리지를 유지한다. 해당 커밋은 미푸시였으므로 `reset --soft`로
   되돌려 오염 blob이 원격에 가지 않게 했다(공개 이력은 건드리지 않았다).

---

## 4. 사용자 결정 대기 2건

### 4-1. test 39 → 72 확장 — **준비 완료, 미병합**

`label_kit/queries_new.jsonl`에 **라벨 33건 완성**(장면 18·자막 9·복합 6).
유형 목표는 `type_targets.json`에 `declared_before_labeling: true`로 라벨 작성 전 등록,
목표 일탈 사유는 `planning/평가확장_계획.md` §3-4에 기록. 신규 3편 인덱싱 완료.

**병목은 라벨 작성이 아니라 병합·재평가 승인이다.** 본 `queries.jsonl`은 135건 불변.

> **권고: 캡션 실험과 분리해 단독 사유로 열 것.** 표본 확장과 모델 변경을 같은 시점에
> 겹치면 나중에 무엇이 수치를 움직였는지 못 가른다. 보고 시 `legacy test-39`와
> `expanded test-72`를 **표본이 다른 별개 추정치**로 구분해야 한다.

### 4-2. dev 96 → 8~12편 확장 — 미착수

라벨이 없다. **사용자 작업 시간이 병목**이고 대리 작성은 절대규칙 3 위반이다.
내가 할 수 있는 것은 영상 선정·인덱싱·유형 목표 사전등록·라벨킷 준비까지다.

---

## 5. 다음 할 일 (내 트랙)

**순서는 배포 방식 분기를 기준으로 잡는다**(§5-1). 1~3은 배포 방식과 무관하므로
결정 없이 진행 가능하고, 4는 GPU 배치라 결정과 승인이 선행한다.

1. ~~**융합 feature 진단**~~ — **닫음.** §1-4. 적응 α 트랙 종결
2. **I1 검증셋** — 시트 생성 완료(§1-6). **프레임 라벨 116장은 사용자 대기.**
   라벨 수령 후 A 단계 분석 → B 단계 → 그다음에야 detector 설계
3. ~~**AI Hub 4-arm α 곡선**~~ — **닫음.** §1-5. α 재조정은 4B 도입의 전제 아님
4. **dev 3-arm 동시점 배치** — `4B/P0 bf16` · `4B/P0 4bit` · `3B/P0 4bit`(동시점
   대조군). 정밀도 판정·τ 재캘리브레이션·dev α 재탐색을 **한 배치로** 닫고
   **배포 방식 결정의 입력**으로 쓴다(§5-1). 655구간×3 arm ≈ 2h.
   **사전등록 + 사용자 승인 선행**

**인프라 트랙 — 지금 진행 중(2026-08-18). 순서가 중요하다.**

5. **M8/M9 provenance** — M3·M6에 적용한 철학을 확장. **M8 재실행보다 먼저 한다** —
   나중에 붙이면 "새 규율 적용 전 산출물"이 하나 더 생긴다
6. **실험 launcher 표준화** — PLAN→CANARY→FULL→VALIDATE→REPORT를 하나의 tracked
   command로. 조각(§3)은 이미 있다
7. **event coverage 정의 + 사전등록** — 정의를 고정한 뒤
8. **M8 dev 재실행** — 위 셋이 끝난 상태에서 **한 번만** — M8 새 스키마는 dev에서만.
   **M9는 `split=="test"` 하드코딩이라 실행 자체가 test 접촉** — 같이 돌리지 않는다.
   M9 14B 교체(튜터결정 2026-08-14 §4 확정, 미적용)와 지표 교체는 M8 확정 후 한 번에
9. 평가 확장 사전등록에 오염 2편(`jissi_farm`·`pland_costco_hosting`)
   `--recaption-corrupted` 포함 — **지금 하지 않는다**(보고 수치 영향 0, 지금 손대면
   "왜 이 시점에 기존 산출물을 바꿨나"가 새 감사 포인트가 된다)

**32B는 동결 유지** — dev CI 반폭 ±0.086 바로 아래(+0.0799)라 표본이 커지지 않으면
다시 열어도 같은 벽이다.

### 5-1. dev 3-arm 배치는 **배포 방식 결정의 입력**이다 (순서 정정 2026-08-18)

**앞서 "배포 방식 결정 → 배치"로 적었으나 순서를 뒤집는다.** 그 배치가 바로 결정에
필요한 정보를 만들기 때문이다. 운영상 노트북/서버를 먼저 택해야 할 사유가 없다면
배치를 **아키텍처 결정 프로브**로 본다.

```
dev 3-arm 동시점 배치 → 4bit 생존 여부 확인 → 배포 방식 최종 결정
```

| arm | 답하는 질문 |
|---|---|
| `4B/P0 bf16` | 2×2 결과가 dev에서도 재현되는가 |
| `4B/P0 4bit` | **4B를 노트북에서 쓸 수 있는가** |
| `3B/P0 4bit` | 동시점 대조군 — 현행 |

**과거 4bit 손실 −0.068은 경고이지 결론이 아니다.** 그것은 `4B/P1` 조건의 기록이고,
최종 후보군은 `P0`다. **동시점 대조군으로 다시 재는 편이 판단에 깨끗하다.**

*판정이 갈리는 방식:* 4bit에서 이득이 살면 노트북 배포 유지로 닫힌다. 죽으면
질문이 **운영 아키텍처 변경**(서버 bf16 추론)으로 바뀌고 지연·비용·가용성·fallback
경로가 같이 판정 대상이 된다. M8/M9가 이미 랩 GPU 전제이므로 "노트북 전용"은 이미
사실이 아니지만, 검색 경로(M3 캡션 생성)를 서버로 올리는 것은 별개 결정이다.

**지금 급하지 않다.** I1 프레임 라벨이라는 사람 대기열이 이미 걸려 있어, 그 사이
인프라·M8 트랙을 닫는 편이 wall-clock 효율이 높다.

### 5-2. 실행 순서 (2026-08-18 확정) — 짧은 병목부터

| # | 작업 | 주체 | 추정 |
|---|---|---|---|
| 1 | ~~I1 A단계 프레임 116장~~ | **완료 2026-08-18** | §1-7 |
| 2 | `gwaktube_soviet_apartment` event inventory (12분 영상) | 사용자 | 25~40분 |
| 3 | M8 pilot 승인·실행 | GPU | 35~60분 |
| 4 | 3번이 도는 동안 나머지 inventory 2편 | 사용자 | 2~3시간 |
| 5 | **dev 3편 전부 freeze 확인 후** M8 결과 열람·보고 | 나 | 30~40분 |
| 6 | I1 B단계 시트 → 라벨 → 새 detector 설계 | 양쪽 | |
| 7 | dev 3-arm 동시점 배치 — 정밀도·τ·dev α | GPU | 2~2.5h |

**1번을 먼저 두는 이유:** 가장 싸고(20~35분), 끝나는 즉시 `4B/P0` vs `4B/P1`
후보 결정의 핵심 불확실성이 줄어든다. inventory는 한 번 시작하면 긴 작업이다.

> **정정.** 앞서 "gwaktube 1편 freeze → pilot 결과 열람"이라고 적었던 것은 틀렸다.
> launcher 게이트는 `requires_frozen_inventory`에 **dev 3편 전부**를 요구한다.
> 한 편만 동결한 상태에서 열람하면 나머지 두 편의 목록이 오염된다.
> 2번은 **규칙이 실제 영상에서 애매하지 않은지 확인하는 용도**이지 열람 조건이 아니다.

**열람 경계의 한계를 명시한다.** 게이트는 `exp_launcher.py report` 진입을 막을 뿐,
사람이 `work/{video_id}/report_pilot_*.json`을 직접 여는 것까지 기술적으로 막지
않는다(OS 권한·암호화는 과하다). 대신 실행 디렉터리와 각 `work/{video_id}/`에
`DO_NOT_INSPECT_BEFORE_INVENTORY_FREEZE.txt`를 자동 생성하고,
**정식 열람 경로는 `report` 하나뿐**으로 둔다.

**`test 39→72`는 이 흐름에 섞지 않는다.** 준비 완료 카드로 두고, 캡션·리포트 트랙이
정리된 뒤 **표본 확장 단독 사유**로 연다(§4-1).

---

## 6. 주의사항

- **M9를 돌리지 마라.** `split=="test"` 하드코딩 = test 접촉
- `results/eval_test.json`·`alpha_search_dev.json`은 **legacy 스키마 유지**.
  전자는 재생성이 test 접촉, 후자는 확정 α\*=0.5가 바뀔 위험
- 라벨 작성 도구는 `m5_search`·`m6_evaluate`를 **import조차 하지 않는다**
- 서버 실행은 `config_server.yaml` 사본으로. 본 `config.yaml` 편집 금지
- 장시간 배치는 **PLAN → CANARY → FULL(사용자 승인)**. 진행 판정은 완료 마커 +
  validator PASS로만. 수치는 UTF-8 JSON에서만 읽는다
- 서버 `nohup` 로그는 **리포 밖**에 쓴다(dirty 방지)

### 6-1. 서버 현재 상태 (2026-08-18 기준)

| 항목 | 값 |
|---|---|
| 작업 디렉터리 | `/ssd/<SERVER_USER>/jds-prj` |
| HEAD | **`85e47f7`에 detached** — master 아님 |
| dirty | 0 |
| 로그 | `/ssd/<SERVER_USER>/logs/caption_2x2_full.log` (리포 밖) |
| GPU | 유휴 |

**다음 서버 작업 전에 할 것**: `git fetch origin && git checkout --detach <목표 SHA>`.
detached 상태를 그대로 두고 새 코드를 기대하면 **구버전이 돈다.**

**untracked 76건을 격리해 뒀다** — `/ssd/<SERVER_USER>/quarantine_untracked_2026-08-17/`.
과거 세션이 남긴 임시 `run_*.sh`·`*.log`이고, 정식 실험은 dirty tree에서 돌리지 않는
규칙 때문에 **삭제가 아니라 이동**했다(`git clean` 미사용). 원본 경로 목록은 그 안의
`MANIFEST.txt`에 있고, 필요하면 되돌릴 수 있다. 8회차 마감 §7의 "서버 작업트리 정리"
미결 항목이 이것으로 닫혔다.

*그 안의 `run_*.sh`들은 CLAUDE.md가 금지하는 즉석 실행 스크립트다 — 되살려 쓰지 마라.
정식 실행은 git에 등록된 스크립트로만 한다.*
