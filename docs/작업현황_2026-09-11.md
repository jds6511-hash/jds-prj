# 작업현황 — 2026-09-11 (WVR C01 Overview preview V2 동기화 · 발표 산출물 정리)

이 파일이 현재 상태의 요약이다. 직전 스냅샷은 `docs/작업현황_2026-09-03.md`이고
**그 파일은 수정하지 않는다**(history 보존). 수치는 UTF-8 JSON 산출물에서 읽었다.

```
HEAD           9952ddb  (+ 이 스냅샷 커밋)
직전 스냅샷      2026-09-03 (v2.1 종결 · B1/B2 운영 · v3 제출 profile 확정)
이번 구간        2026-09-08 ~ 09-11 · WVR 프로브 캠페인 + 최종발표 산출물
GPU inference   이번 정리 작업에서 0회 (기존 frozen artifact 복사만)
```

---

## 0. 범위 선언 — 오독 방지용 (가장 먼저 읽을 것)

```
WVR frozen source video   full_xekZO4n4QuE.mp4
                          sha256 ea0e9f486661282056a36781a57d6d74e40da02c03ad9159b75c21fe435676cc
원본 영상 길이              2424.186485초 (40.4분) · 1920×1080 · 30fps · 72,724 frames

현재 집중 검증 범위          C01 [0, 600)
C01의 정체                 별도 clip 파일이 아니다 — 동일 원본 영상의 logical chunk다
                          (파일 분할 없음 · sha256도 원본과 동일)

C02 ~ C05                 NOT YET EXECUTED
```

chunk plan(동결, `WHOLE_VIDEO_REPORT_LIGHT_V1_2026-09-08.md` §5):

```
C01   0.0    – 600.0       300 프레임
C02   480.0  – 1080.0      300
C03   960.0  – 1560.0      300
C04   1440.0 – 2040.0      300
C05   1920.0 – 2424.186    253
chunk 600 / overlap 120 / stride 480
```

창 일정(동결, `src/wvr_shadow_v1.py:31-39` · `WVR_EVENT_EXTRACTION_SHADOW_V1` §6):

```
범위 [0,600) · window 48초 · stride 24초 · 0.5fps · 창당 24프레임 · 창 24개
EXPECTED_WINDOW_COUNT=24 가드 — 범위·개수가 어긋나면 실행이 거부된다
```

### 표현 규약 (반드시 지킬 것)

```
허용    C01-based whole-video architecture validation
        C01 Overview preview
금지    full 2424-sec Overview completed
        whole source video fully analyzed
```

> **2026-09-11 정정 기록.** 같은 날 세션에서 "V2 커버리지가 598초뿐이라 40분 영상의
> 앞 10분만 덮는 한계"라고 보고한 적이 있다. **오독이다.** 598초는 결함이 아니라
> 동결된 실험범위 C01의 정상 동작이다. 남아 있는 것은 결함이 아니라 미완(C02~C05)이다.

---

## 1. 한 장 요약

```
1  Track A 검색        동결 · 변경 없음 (Qwen2.5-VL-3B/4bit · KURE-v1 · z-score · α=0.5)
2  Track B 제출        v3 summary-only arm · SUBMISSION_READY = YES · 변경 없음
3  Track C WVR         C01 범위에서 event → map → chapter/boundary → overview 계열 프로브 진행
4  Conservative Event Map   CLOSED / CONSERVATIVE_EVENT_MAP_PASS
5  Chapter · Boundary       전부 HOLD 또는 INCONCLUSIVE — 채택된 경계 없음
6  Overview preview (event-map 경로)   V1·V2 실행 · 품질 미달로 경로 전환
7  Overview preview (video-direct 경로) V1 FAILED · V2 GENERATED / REVIEW_REQUESTED
8  official test       UNOPENED (39건) · M9 미실행 · 둘 다 변동 없음
```

---

## 2. Track A — 영상 검색

```
상태        동결. 이번 구간에 코드·인덱스·캡션 변경 없음
배포 구성    Qwen2.5-VL-3B-Instruct (4bit) · faster-whisper large-v3 · KURE-v1(1024)
            5초 고정 구간 · z-score 융합 · α=0.5(CLI 주입) · abstention τ=0.55
            static_threshold=0 (치환 off)
근거 파일    results/eval_test.json · results/alpha_search_dev.json
official test  39건 UNOPENED — 이번 구간에 접촉 0회
```

이번 구간의 Track A 관련 활동은 **발표 자료 작성 목적의 read-only 재인용**뿐이다
(기존 dev 96건·AI Hub 결과를 슬라이드에 인용). 재평가·재탐색·재캡셔닝 없음.

---

## 3. Track B — 제출 baseline

```
상태            2026-09-03 스냅샷에서 변동 없음
arm             episode_content_v3_summary_only
SUBMISSION_READY  YES (뜻은 좁다 — §6 HOLD 항목 참조)
contract hash   a08e3c3c4988daa66…
renderer        A2' pure-Python OWPML · 한글 Open() True · PDF export True
provenance      runs/v3_paired/submission_manifest.json
알려진 한계      2/41 parse-contract failure · GRD-004 P1 WAIVED
```

---

## 4. Track C — WVR (C01 범위)

### 4-1. Event Extraction / Stitching / Conservative Event Map

```
WVR_EVENT_EXTRACTION_SHADOW_V1        실행 완료 · 이벤트 160건 (C01 24창)
WVR_OVERLAP_EVENT_STITCHING_SHADOW_V1 CLOSED / EVENT_STITCHING_SHADOW_HOLD
                                      blinded 판정 22건 기록 후 리뷰어 HOLD
WVR_CONSERVATIVE_EVENT_MAP_SHADOW_V1  CLOSED / CONSERVATIVE_EVENT_MAP_PASS
```

Conservative Event Map 실측(`conservative_event_map_v1_summary.json`):

```
region 11 · consensus event 6 · conflict region 4 · conflict block 10
conflict duration 240.0초 · events_represented 160 · events_missing 0
false_resolution 0 · invalid_source_dependency 0
```

> 산출물 JSON의 `executor_state.state`는 `EXECUTED / REVIEW_PENDING`으로 남아 있다.
> 설계상 verdict는 executor가 쓰지 않기 때문이며(`verdict_by_executor: false`),
> PASS 판정은 후속 3개 사전등록 문서의 "동결된 선행 상태" 블록에 기록돼 있다.

### 4-2. Semantic Chapter / Boundary

```
WVR_SEMANTIC_CHAPTER_SHADOW_V1            CLOSED / SEMANTIC_CHAPTER_SHADOW_HOLD
WVR_SEMANTIC_CHAPTER_BOUNDARY_REPAIR_V1   CLOSED / SEMANTIC_CHAPTER_REPAIR_HOLD
                                          Stage B NOT EXECUTED
                                          blocker INSUFFICIENT_BOUNDARY_EVIDENCE
                                          event 시작 후보 144 · 전환근거 accepted 6
                                          선택 가능 밴드 안 accepted 1 (경계 400초)
WVR_SEMANTIC_BOUNDARY_CANDIDATE_SHADOW_V1 CLOSED / INCONCLUSIVE  (commit 168b492)
```

해석(사전등록 문서 기록): action/object lexical overlap 기반 detector가
whole-video semantic task transition을 찾기에 지나치게 보수적이었다.
**채택된 chapter 경계는 없다.**

### 4-3. Overview preview — 경로 ① event-map → Qwen2.5-7B (선행 경로)

```
WVR_OVERVIEW_PREVIEW_V1 / V2   실행 완료 (runs/wvr_light_v1/overview_preview_v*)
입력                           Conservative Event Map (opaque conflict set)
합성 모델                      Qwen/Qwen2.5-7B-Instruct · 비양자화 · 7.9초
관측된 품질 문제                존재하지 않는 어휘 생성 — "크래프트볼" · "프리미너블" · "유동"
결론                           품질 미달 → 경로 ②로 전환. 산출물은 provenance로 보존
```

### 4-4. Overview preview — 경로 ② video-direct Qwen3-VL-8B (현행)

```
WVR_VIDEO_TO_OVERVIEW_PREVIEW_V1   FAILED / REVIEW_REQUESTED
                                   error SHORT_COUNT: 3~5 sentences required
                                   raw는 parse 전 선저장돼 보존됨
                                   segment 24 + synthesis 1 = 25 추론 · 186.9초
                                   peak VRAM 17,511 MiB (RTX 4090)

WVR_VIDEO_TO_OVERVIEW_PREVIEW_V2   GENERATED / REVIEW_REQUESTED      ← 현행 최신
                                   segment 24 + synthesis 1 = 25 추론 · retry 0
                                   resumed_existing_segment_raw_count 14
                                   code_git_head 8d3c59f5ec90e9de954d1d45508c03bf94884de4
                                   resume/validator 교정 커밋 9952ddb
                                   track_a_used False (검색 색인 미사용)
                                   overview_raw_sha256 46de7f07…019d82
```

두 경로 모두 **관찰과 합성을 같은 Qwen3-VL-8B-Instruct 하나가 수행**한다(별도 텍스트 LLM 없음).

### 4-5. 최신 V2 Overview 실제 출력 (C01 [0,600) 기준)

SHORT OVERVIEW:

> 음식을 준비하고 조리한 후 식사를 하며, 이어지는 구매 또는 둘러보기 활동이 반복된다.
> 이후 의류 작업과 외출 준비, 포장 작업, 이동이 이어지며, 마지막으로 다시 구매 또는
> 둘러보기 활동으로 마무리된다.

compressed activity timeline — activity run 11개:

```
P01 S01~S11 음식 준비 및 조리      P07 S19~S21 음식 준비 및 조리
P02 S01~S01 구매 또는 둘러보기      P08 S20~S24 외출 준비
P03 S11~S16 식사                  P09 S22~S23 포장 작업
P04 S13~S14 구매 또는 둘러보기      P10 S24~S24 이동
P05 S14~S17 음식 준비 및 조리      P11 S24~S24 구매 또는 둘러보기
P06 S17~S19 의류 작업 및 수선
```

```
uncertainties                 0건 (불확실성 표현이 비어 있다 — 품질 과제)
context_inferences_excluded   0건
판정                           REVIEW_REQUESTED — 리뷰어 판정 전이다
```

이것은 **C01 Overview preview**다. 2424초 전체 Overview가 아니다.

### 4-6. V2 artifact 동기화 (2026-09-11)

```
source        kixlab2:/ssd/daeseok/jds-prj-video-overview-v2r/runs/wvr_video_overview_preview_v2
destination   runs/wvr_video_overview_preview_v2/
파일 수        56 → 56
hash 불일치    0 (56/56 sha256 완전일치)
record 내부 hash 대조   25항목(overview_raw 1 + segment_raw 24) 전부 일치
성격          재실행이 아니라 기존 frozen artifact의 read-only 복사
              inference count 증가 0 · 새 raw 생성 0 · source 수정 0
```

---

## 5. 발표 산출물 (2026-09-11)

```
빌드 스크립트   docs/presentation/build_final_deck.js   (15장)
자산           docs/presentation/assets/ — 프레임 2장(3B/4B 비교) · 실제 검색 UI 스크린샷 1장
골격 초안       docs/presentation/최종발표_골격_draft.md   (KEEP_LOCAL_ONLY · 기존 관례)
산출 pptx      *.pptx는 .gitignore:27 정책 유지 — 커밋하지 않는다(스크립트로 재생성)
```

슬라이드에 쓴 수치는 전부 기존 산출물 재인용이다(재실행 없음):
`results/alpha_search_dev.json` · `archive/ablation/results_bge/…` · `config.yaml` ·
중간성과발표(2026-08-21)의 3B/4B 실제 캡션·프레임.

> **발표 자료 갱신 필요 항목:** 슬라이드 11(Overview 출력)이 아직 V1 자리표시자다.
> V2 실물(§4-5)로 교체할 수 있으나, 교체 시 "C01 [0,600) 구간"임을 반드시 병기한다.

---

## 6. production HOLD 항목

```
Chapter 경계 채택              HOLD — 채택된 경계 없음
Event stitching production    HOLD
Overview production 승격       HOLD — V2는 REVIEW_REQUESTED 상태
C02~C05 실행                  미실행 · 별도 승인 사건
v3를 repository default로 승격  HOLD — 별도 채택 사건
box alignment 표현 수정         HOLD — frozen `_lines()` 변경 필요
caption 화면문자 인용 프롬프트 변경  HOLD — 튜닝이므로 사전등록 필요
search-research E0            PREREG_DESIGN_READY · 실행 승인 없음
Gate B-07 canonical artifact  scope 승인됨 · 생성 미수행
```

---

## 7. 접촉 금지 상태 (변동 없음)

```
official test 39건   UNOPENED — 이번 구간 접촉 0회
test 39 → 72 확장     HOLD (신규 33건 준비됨 · 별도 test-opening 승인 사건)
M9                   미실행 (split=="test" 하드코딩 — 실행 자체가 test 접촉)
확정 인덱스 캡션 재생성  없음
검색 파이프라인(M1~M7)  변경 없음
```

---

## 8. 알려진 환경 이슈 (코드 결함 아님)

```
tests/test_wvr_boundary_candidate_v1.py::test_wvr_b28
  TypeError: Path.read_text() got an unexpected keyword argument 'newline'
  원인: newline= 인자는 Python 3.13+ API · 로컬 환경은 3.12.10
  판정: environment compatibility issue — 이번 작업에서 수정하지 않는다
```

---

## 9. 다음 후보 (전부 승인 필요)

```
① V2 리뷰어 판정              GENERATED / REVIEW_REQUESTED → PASS/HOLD 결정
② C02~C05 실행 여부            결정되면 GPU 배치 승인 사건
③ Gate B-07 canonical artifact 생성
④ 발표 슬라이드 11을 V2 실물로 교체 (C01 범위 병기 필수)
```
