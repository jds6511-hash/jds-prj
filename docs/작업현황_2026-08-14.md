# 작업현황 — 2026-08-14

**재시작 시 여기부터 읽는다.** 직전 스냅샷: `docs/작업현황_2026-08-12.md`

---

## 0. 지금 돌고 있는 것

| 항목 | 상태 |
|---|---|
| 서버 배치 `[B] 큰 모델 확인` | **실행 중** — `/ssd/<SERVER_USER>/logs/bigmodel.log` |
| 완료 예상 | **2026-08-15(토) 정오 전후** (실측 기반, §5) |
| 노트북 웹 UI | 7860 포트 가동 중 |
| test 접촉 | **없음** — 여전히 7회, 튜닝 0회 |

배치는 `setsid nohup`으로 분리 실행 중이라 세션이 끊겨도 계속된다.
확인: `ssh <SERVER_USER>@<SERVER_HOST> 'grep -a "===" /ssd/<SERVER_USER>/logs/bigmodel.log | tail'`

---

## 1. 정정 — 이전 문서의 오기

`작업현황_2026-08-10.md:372`와 `작업현황_2026-08-12.md:237`에 신규 3편
(jissi_farm·softyeon_ceramics·baekmansonghee_jirisan)이 **"dev 확장 3편"**으로
적혀 있다. **틀렸다. 세 편은 test 전용이다.**

`label_kit/queries_new.jsonl` 33건 전부 `split: "test"`이고, `평가확장_계획.md`도
test 확장(39 → 72)으로 설계돼 있다. 위 두 문서는 과거 스냅샷이라 그대로 두고
여기서 정정한다.

---

## 2. 이번에 난 사고 — 채택 1단계 배치가 조용히 실패했다

### 무슨 일

캡션 모델 교체를 위해 서버에서 10편을 재생성하는 배치를 걸었다. 배치는
`=== 1단계 완료 ===`를 찍고 **정상 종료**했다. 실제로는 **10편 중 3편만 성공**했고,
나머지 7편은 캡션이 **빈 문자열로 덮이고 그대로 색인**됐다.

원인: **재캡셔닝에 필요한 프레임 이미지가 서버에 없었다.** dev 3편만 올려뒀고
test 7편은 올린 적이 없다. 실패 로그 1,525줄.

### 왜 못 막았나 — 구멍 세 개

| # | 위치 | 내용 | 수정 |
|---|---|---|---|
| 1 | m3 `--captions-only` | `rep_frame` **필드**만 검사하고 **파일 실물**은 안 봤다 | `2e33fd0` |
| 2 | m3 저장 | 전건 실패해도 `save_segments`가 먼저 돌았다 | `2e33fd0` |
| 3 | m4 | 빈 캡션 262건을 그대로 임베딩하고 새 `text_hash`까지 찍었다 | `bbeee7e` |

3번이 마지막 방어선이었는데 뚫려 있었다. m1·m2·m8·m9의 저장 경로도 같이 봤고
같은 유형의 구멍은 m4 하나였다(m2는 프레임 저장 실패 시 저장 전에 raise, m8은
`raw_output` 보존 목적으로 저장이 먼저인 것이 의도된 순서).

### 복구 결과

| 항목 | 상태 |
|---|---|
| 노트북 배포 인덱스 | **무사** — 11편 캡션 결손 0 · 프레임 전량 · `text_hash` 일치 |
| 서버 11편 | **백업에서 복구 완료. 노트북 해시와 전건 일치(불일치 0)** |
| 성공했던 dev 3편 신모델 캡션 | `/ssd/<SERVER_USER>/qwen3vl_4b_p1_dev3/`에 보존 |
| test 7편 프레임 | **업로드 완료** (1,913장 · 455MB, 8편) |

7편만 손상됐지만 **11편 전부 되돌렸다.** dev만 신모델이면 dev/test가 다른 캡션
모델로 갈려 그 위에서 뭘 재도 해석이 안 된다.

---

## 3. M8 리포트 — 요약이 아니었고, 절단 원인을 규명했다

### 리포트 4편 실측

| 영상 | 구간 | 문장 | 영상 커버 | 인용폭 | 인용당 글자 | 몰아쓰기 |
|---|---|---|---|---|---|---|
| itsub_viral_gadgets | 191 | 127 | 88.5% | 1.4 | 49.7 | 3 |
| kheritage_grave_excavation | 192 | 93 | 75.5% | 1.7 | 34.7 | 1 |
| panibottle_vietnam1 | 262 | 58 | **32.4%** | 2.1 | 18.0 | 0 |
| yunnamnopo_tongyeong | 357 | 28 | 86.0% | **40.1** | **9.3** | **9** |

**네 편 다 요약에 실패하는데 방식이 둘이다.**

- **나열형 3편** — 구간을 1:1로 훑는다. 인용 96~100% 시간순, 묶는 동작이 없다
- **몰아쓰기 1편** — 28문장으로 86%를 덮지만 한 문장이 70구간을 인용하고 본문은 20자

*인용 폭만 보면 마지막 편이 요약처럼 보인다. **인용당 글자수를 함께 봐야** 몰아쓰기를
개선으로 오독하지 않는다.*

### 절단 원인 — 캡션 속 한자 2글자

생성 상한 문제가 **아니었다.** reduce 출력이 3,261자, 상한 16,384토큰의 **1/5**에서
스스로 끝났다.

```
seg 88 캡션 :  "비행기 내부에서 파란색 좌석에 헤드靠垫이 달린 좌석들이 …"
리포트 출력 :  "… 파란색 좌석에 헤드靠垫的图片未加载成功，请稍后再试或检查网络连接。" [종료]
```

`靠垫`에서 모델이 중국어로 전환해 EOS를 냈다. **map 청크와 reduce에서 두 번** 일어나
seg 88~109가 통째로 빠지고 최종 커버가 32.4%가 됐다.

`common.is_corrupted_caption`은 한자·가나 **3글자 이상**만 잡는다. 2글자는 통과한다.
그런 캡션이 **전 인덱스 2,568건 중 183건(7.13%)**, 11편 전부에 분포한다.

### 대응

| 조치 | 커밋 |
|---|---|
| 리포트 입력에서만 CJK 제거 (인덱스 불변) | `3c8a0ba` |
| map 청크 커버 미달 시 1회 재생성 | `3c8a0ba` |
| 요약 예산 옵션 (기본 off, dev A/B용) | `f717768` |
| 개선 판정 기준 사전 등록 | `ec1c5c2` |

**튜닝은 dev에서만 한다.** 저장된 리포트 4편이 전부 test 영상이라, 그걸 보고
프롬프트를 고치면 M8 쪽에서 튜닝 0회가 깨진다. 4편은 현행 상태 기록으로만 쓴다.

---

## 4. 이번에 새로 잰 것 2건

### 4-1. 오염 필터 임계 — 낮출 근거 없다

| 지표 | 원본 | CJK 제거 | 차이 |
|---|---|---|---|
| 캡션 단독 α=0.0 | 0.5535 | 0.5546 | +0.0011 CI[−0.0003, +0.0032] |
| 융합 α=0.5 | 0.6692 | 0.6687 | −0.0004 CI[−0.0035, +0.0024] |

dev 655구간 중 80건이 바뀌었는데 **검색 성능은 사실상 불변**이다.

**결론:** 인덱스 임계를 낮출 근거 없음. 리포트 입력 시점 제거만 유지한다 — M8
절단은 막고 검색·인덱스는 안 건드린다.
**한계:** 이건 "제거"만 잰 것이다. "재생성" 효과는 이 결과로 추정하지 않는다.

### 4-2. 질의 표현 강건성 — 평가 집합의 템플릿 결함을 찾았다

**dev 96건 중 95건(99%), test 39건 전부가 "…하는 장면"으로 끝난다.**
평가 집합 전체가 한 템플릿을 공유한다.

접미를 기계적으로 흔들었다(패러프레이즈를 직접 지으면 "쉬운 쪽으로 썼다"는 반론을
못 막는다).

| 변형 | 융합 MRR | 차이 | p |
|---|---|---|---|
| 원본 | 0.6692 | — | — |
| "장면" 삭제 | 0.6450 | −0.0242 | 0.089 |
| → "부분" | 0.6504 | −0.0188 | 0.097 |
| → "순간" | 0.6571 | −0.0120 | 0.267 |
| → "장면 찾아줘" | 0.6865 | +0.0174 | 0.151 |

**BH-FDR q=0.05 통과 0건.** 접미 템플릿에 기대고 있지 않다.
다만 방향은 일관되게 "빼면 내려가고 붙이면 올라간다"이므로 한계로 기록한다.
**흔든 것은 접미 하나뿐이다 — 질의 강건성 전반으로 일반화하지 마라.**

---

## 5. 실행 순서 (튜터 결정 반영)

`docs/튜터결정_2026-08-14.md`가 근거 문서다. **3번 결정(더 큰 모델까지 확인)이
1번(test 재평가 시점)을 바꿨다.**

```
[A] GPU 없이 — 완료
    ✔ m3 프레임 게이트 + 전건 실패 저장 금지          2e33fd0
    ✔ m4 캡션 전량 공백 거부                          bbeee7e
    ✔ M8 절단 규명 + 개선 기준 사전등록               ec1c5c2
    ✔ M8 CJK 제거 + 청크 조기종료 감지                3c8a0ba
    ✔ M8 요약 예산 옵션(기본 off)                     f717768
    ✔ 서버 11편 복구 + 프레임 8편 업로드
    ✔ 오염 필터 임계 실험 · 질의 강건성 측정
    ✔ 웹 UI E2E 전건 통과 (거짓 실패 1건 수정 c5fb536)

[B] 큰 모델 확인          ← 실행 중, 토요일 정오 전후
    4B bf16 · 4B 4bit · 32B 4bit · 30B-A3B 4bit × P1·P4
    판정: 4bit끼리 비교. dev 96 캡션 단독 α=0.0이 주 지표

[C] M8 개선               약 2~3시간
    dev 3편 개선 전/후 리포트 → 사전등록 관문 판정

[D] test 재평가 8회차      약 4.5시간
    [B]가 끝나 캡션 모델이 확정된 뒤 1회 연속 실행
```

**[D]는 [B] 전에 시작하지 않는다.** 지금 4B로 열고 나중에 32B를 채택하면 9회차가
필요하고, 그때는 "결과를 보고 모델을 바꿨다"가 된다.

### [B] 소요 실측 근거

1단계 시작 `1786686821`, 20.7분 시점에 470/655장 → **2.64초/장**(사전 추정 2.5와 일치).

| arm | 추정 |
|---|---|
| 4B bf16 P1 | 29분 |
| 4B 4bit P1 | 37분 (2026-08-10 실측 2,240초) |
| 32B 4bit P1 | 1.5~2.5시간 |
| 30B-A3B 4bit P1 | 1~1.5시간 (활성 3B) |
| P4@256 전체 | 위의 약 3.5배 (2026-08-10 실측 5,764/1,644) |
| 가중치 내려받기 | 약 124GB, 1~2시간 |
| **합계** | **약 20~22시간** |

*32B·30B-A3B의 실제 속도는 처음 재는 것이라 폭이 있다. 첫 큰 모델 arm이 끝나면
실측으로 다시 잡는다.*

---

## 6. 확정치 — 근거 3문 검증

**"근거 파일 어디 · 기준 언제 정했어 · 표본 몇 개"** 를 이번에 보고한 전 수치에 적용했다.

| 수치 | 근거 파일 | 기준 확정 시점 | 표본 |
|---|---|---|---|
| 캡션 모델 dev 0.461 → 0.552 | `_scratch/caption_sweep.json` | 사전등록 2026-08-08 (`prereg.declared_before_run: true`) | dev 96 질의 · 655 캡션/arm |
| 캡션 모델 확증 +0.0377 CI[0.013, 0.061] | `_scratch/aihub_confirm_bf16matched.json` | 사전등록, 정밀도 맞춤 대조군 포함 | AI Hub 194편 · **1,086 질의** |
| 융합에서는 +0.0187 CI[−0.002, +0.040] **비유의** | 위와 같음 | 위와 같음 | 위와 같음 |
| 임베더 BH 통과 0건 | `_scratch/embedder_sweep_select_r2.json` | 사전등록, `combined_bh.family_size: 7` | AI Hub 97편 · **562 질의** |
| 생성 환경 효과 −0.0046 CI[−0.027, +0.017] | `_scratch/aihub_env_check.json` | 사전등록 §7-d(노트북 유리 설계) | 97편 · **562 질의** · 1,164 캡션 |
| 판정자 7B 0.550 탈락 / 14B 0.767 통과 | `_scratch/judge_gate_models.json` | 통과선 0.75를 **결과 전** 등록 | **셀당 60문항** |
| α\* = 0.5 (점추정 최적은 0.4) | `results/alpha_search_dev.json` | 규칙 사전 확정(tie_set + tiebreak=larger) | dev 96 · α 격자 11점 |
| test 0.649 → 0.829 | `results/eval_test.json` | 확정 config, 튜닝 0회 | **test 39 질의** |
| M8 요약 실패 4편 · 커버 32.4% | `_scratch/m8_report_quality.json` | 지표를 개선 **전에** 등록 | **리포트 4편**(전부 test 영상) |
| CJK 혼입 183건 (7.13%) | 위와 같음 | — (전수 집계) | 전 인덱스 **2,568 세그먼트** |
| 오염 필터 제거 Δ+0.0011 | `_scratch/cjk_threshold_probe.json` | 사전등록 2026-08-14 | dev **96 질의** · 655 구간 중 80건 변경 |
| 질의 강건성 BH 통과 0건 | `_scratch/query_robustness.json` | 사전등록 2026-08-14 | dev **96 질의** × 변형 4종 |
| 테스트 230건 통과 | `pytest tests/ -q` | — | 230 케이스 |
| 배치 2.64초/장 | `/ssd/<SERVER_USER>/logs/bigmodel.log` | — | 470장 시점 관측 |

### 근거가 약한 것 (같이 밝힌다)

| 수치 | 약한 이유 |
|---|---|
| M8 관문 임계 0.90 / 0.35 / 15자 | 현행 수치에서 역산했다. 외부 근거 없음 |
| M8 실측 4편 | 전부 같은 모델·같은 분할. dev 기준선이 아직 없다 |
| 32B·30B-A3B 소요 추정 | 처음 재는 것. 실측 아님 |
| 질의 강건성 | 접미 하나만 흔들었다. 문장 전체 재작성은 못 쟀다 |
| 오염 필터 실험 | "제거"만 쟀다. "재생성"은 GPU 확보 후 |

---

## 7. 주의 (그대로 유지)

- **test 재평가는 [B] 완료 후 1회 연속 실행.** M9는 돌리는 것 자체가 test 접촉이다
- **본 `config.yaml` 편집 금지.** 서버는 `config_server.yaml` 사본으로
- **확정 인덱스의 캡션을 다른 환경에서 재생성하지 마라** — 다만 환경 효과는
  독립 표본에서 재현 실패했다(§6). 이 규칙의 근거는 약해졌고, [B]에서 서버 통일로 간다
- **재캡셔닝 후 m4 필수.** `text_hash`가 자동 감지·차단한다
- 콘솔 한글은 cp949로 깨진다 — **수치는 항상 UTF-8 JSON에서 읽는다**
