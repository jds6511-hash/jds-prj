# 작업현황 — 2026-09-03 (v2.1 종결 · B1/B2 운영 · v3 제출 profile 확정)

이 파일이 현재 상태의 요약이다. 판정의 source of truth는 `docs/finalization/`과
테스트 가드다. 수치는 UTF-8 JSON 산출물에서 읽었다.

```
HEAD          fd49e80 (+ 이 스냅샷 커밋)
커밋           538
테스트         3,917 passed / 1 skipped / 0 xfailed
v2.1          IMPLEMENTATION_COMPLETE = YES   baseline 6e79ac3 · matrix 166/166
제출           SUBMISSION_READY = YES · arm = v3 summary-only
```

---

## 0. phase 선언

```
project_phase             POST-v2.1 · 운영 통합 + 제출 확정
검색 파이프라인            동결 · 변경 없음
                          Qwen2.5-VL-3B / P0 / 4bit · KURE-v1 · z-score 융합 · α=0.5
보고서 생성 (M8)           공식 판정 FAIL · 소급 변경 없음
M9                        HOLD · official test UNOPENED (39건)
기본 프롬프트 계약          episode_content_v2 (교체하지 않았다)
제출 프롬프트 계약          episode_content_v3_summary_only (명시적 선택)
```

---

## 1. 한 장 요약

```
1  v2.1 구현 종결        TRI-005(C3 sparse containment)로 마지막 P0 종결      6e79ac3
2  HWPX 렌더러 결함      기존 src 렌더러는 열리지 않는 패키지 — KNOWN DEFECT
   A1                   한글 COM 경로 (compatibility oracle)
   A2'                  순수 Python OWPML — **portable primary** · 3중 교차 검증
3  B1 실입력 특성        YouTube 40.4분 · 485구간 · 1.71h · VRAM peak 5,267/6,144
4  B2 orchestrator       S0~S7 · 지문 5종 + upstream hash + stage_complete · resume
5  B2 실행 (4090)        41/41 episode · 131초 · OOM 0 · HWPX 한글 열림
6  발견                  표현 회수율 2/41 — dialogue 실패가 요약을 표현에서 지운다
7  remediation ②         summary-only v3 **병행 계약** (v2 무수정)
8  paired R0/R1          2/41 → 39/41 · dialogue 기인 제외 38 → 0
9  제출 확정             arm = v3 · SUBMISSION_READY = YES (뜻은 좁다)
```

---

## 2. 확정 수치

```
paired 실험 (같은 입력·config·commit·모델, 계약만 변경)
  R0 v2      eligible  2 / 41 · grounding FAIL 38 · reason 349
  R1 v3      eligible 39 / 41 · grounding FAIL  0 · reason   0
  parse 실패  1 → 2 (EP35 · EP39 — 언어 이탈·절단, 일반화 근거 없음)
  시간        121.0s → 63.6s · VRAM peak 15,941 → 15,799 / 24,564

제출 artifact
  contract   episode_content_v3_summary_only · hash a08e3c3c4988daa66…
  input      b1_segments.json aa008317023c884a206c… · 485구간
  renderer   A2' pure-Python OWPML · 구조 validator PASS
  실물        한글 Open() True · PDF export True (134,938 B) · glyph 깨짐 0
  provenance runs/v3_paired/submission_manifest.json
```

---

## 3. 문서 지도 (이번 국면)

```
v2.1 종결          finalization/V2_1_FINAL_ACCEPTANCE_2026-09-02.md
                   finalization/V2_1_TRI_005_CLOSURE_2026-09-03.md
HWPX               finalization/V2_1_HWPX_A1_RESULT_2026-09-03.md
                   finalization/V2_1_HWPX_A2_RESULT_2026-09-03.md
B1                 probes/B1_FULL_RUN_2026-09-03.md
B2                 finalization/V2_1_B2_ORCHESTRATOR_PREREG_2026-09-03.md
                   finalization/V2_1_B2_DRYRUN_RESULT_2026-09-03.md
                   finalization/V2_1_B2_FULL_RUN_2026-09-03.md
v3                 finalization/V2_1_V3_SUMMARY_ONLY_PREREG_2026-09-03.md   (본문 무수정)
                   finalization/V2_1_V3_IMPLEMENTATION_2026-09-03.md
                   finalization/V2_1_V3_PAIRED_RESULT_2026-09-03.md
제출                finalization/V2_1_SUBMISSION_STATUS_2026-09-03.md        ← 상태 정본
```

---

## 4. 알려진 한계

```
2 / 41             parse-contract failure (제출본에 상태로만 남는다)
GRD-004            P1 WAIVED — semantic entailment는 자동 검증되지 않는다
문자 그림 box 정렬   표현 품질 diagnostic · P0 아님
C-09               표현 자격 규칙은 완화하지 않았다(우회한 것이 아니라 실패 표면 제거)
```

---

## 5. 다음에 하지 않을 것 (승인 없이)

```
v3를 repository default로 승격        별도 채택 사건이다
v2 프롬프트·hash 수정 · OPEN-12 완화
EP35·EP39 재생성 cherry-pick
summary semantic re-grounding
official test 접근 · test39→72 확장 · M9 실행
검색 파이프라인(M1~M7) 변경 · 확정 인덱스 캡션 재생성
추가 모델 비교 · 새 human GT
```

---

## 6. 열려 있는 트랙 (미승인)

```
search-research E0     PREREG_DESIGN_READY · 실행 승인 없음
                       선행 정리: alpha_star 0.5 vs 1.0 불일치
box alignment          표 구조로 고치려면 frozen `_lines()` 변경 → 별도 표현 티켓
caption 화면문자 인용    work_full에서 9.1% · 프롬프트 변경은 튜닝이므로 사전등록 필요
TRI-005 C1 · C2        DEFERRED
③ 프롬프트로 dialogue 제약  DEFER (v3가 ②를 먼저 해결)
```

---

## 7. 2026-09-07 — STT_VAD0을 제출 profile에 채택

```
채택 범위      제출 profile 하나. repository default·sanitation core·raw STT 무변경
제출 arm       R1-VAD0 / episode_content_v3_summary_only + explicit VAD0
방식           Tier 2 S1 artifact 승격 (모델·LLM 재실행 없음)
rollback       기존 제출본 f874f643… 보존 (삭제·덮어쓰기 없음)
```

Tier 2 hard gate 전부 PASS 후 사용자 승인으로 채택했다. 재실행하면 채택 근거가 된
실험과 제출 artifact가 분리되므로, S1 run 안의 그 파일을 그대로 올렸다. manifest
생성기는 S1 run 지문이 `paired_metrics`의 S1 지문과 같은지 확인하고 다르면 거부한다.

```
                      rollback   현행
presentation eligible  39 / 41   40 / 41
parse contract failure  2         1
claim evidence         777       567
hwpx sha256            f874f643… 4e10aaab…
한글 Open() · PDF                True · True (137,546 bytes · 본문 14,460자)
```

`40/41`은 비퇴행 게이트 통과다. 2 → 1 변화를 일반화하지 않는다.

```
runs/vad0_paired/submission_manifest_vad0.json    제출 manifest (policy·VAD·paired 기록)
Desktop/v3_submission_vad0/                       제출 사본 (hwpx·pdf·md·전사문 2종·manifest)
docs/finalization/V2_1_SUBMISSION_VAD0_PROMOTION_2026-09-07.md
tests/test_v2_1_submission_vad0.py                18건 · guard mutation M1~M4 RED 확인
```

여전히 HOLD·금지: production-wide 채택 NOT ADOPTED · VAD0 default off ·
repository default v2 · 전사문 교체 HOLD · VAD threshold tuning 금지 ·
새 human GT 금지 · M9 HOLD · official test UNOPENED.

---

## 8. 2026-09-08 — 최종 출력 품질 게이트 (quality candidate)

```
사건        final-output validation defect correction (VAD0 채택 사건과 별개)
LLM 재실행   없음 — S5 확정 정본 재사용, 표현 계층만 재렌더
결과        보고서 한자 drift 3줄 → 0 · presentation eligible 40 → 39 · 9 group
```

H13에 중국어 절이 노출돼 있었다. 같은 시점 STT는 한국어였으므로 STT 문제가 아니라
report-output 계층 결함으로 분류했다. 상수는 결과를 보기 전에 freeze했다
(`docs/finalization/V2_1_OUTPUT_QUALITY_ADDENDUM_2026-09-08.md`).

```
FOREIGN_SCRIPT_RUN_MIN         8     연속 Han/Kana(+CJK 구두점)
PRESENTATION_GROUP_WINDOW_SEC  300   presentation-format 상수
interlock                      quality.status != FAIL  (PASS-only 아님)
Q1 · Q4 FAIL / Q2 · Q3 SUSPECT(diagnostic)
```

```
quality PASS 38 · SUSPECT 1(EP02 카레우don) · FAIL 2(EP13 drift · EP17 parse)
한글 Open() True · PDF 115,181 bytes · 본문 7,887자 · 구조 validator PASS
candidate hwpx 8ef85645… · canonical partition hash 원본과 동일
전사문 타임스탬프 38건 교정(45.98 → 00:00:46.0) · 텍스트 0건 변경
```

기존 제출본(`4e10aaab…`)·이전 제출본(`f874f643…`) 모두 무변경. 제출 arm 교체는
하지 않았다 — 현행은 `submission-vad0-2026-09-07`이다.

frozen acceptance 충돌로 못 한 것: **개요 압축(GLS-001)** · **핵심 내용 분석 섹션
제거(SECTION_NAMES·한글 E2E)**. 둘 다 별도 승인 사건이다.

```
src/v2_1_output_quality.py                        신규
tests/test_v2_1_output_quality*.py · _reason_render · _presentation_grouping
tests/test_v2_1_quality_candidate.py              신규
scripts/v2_1_quality_candidate.py                 재렌더 (LLM 없음)
runs/quality_candidate/ · Desktop/v3_submission_quality/
```

---

## 9. 2026-09-08 — 제출 arm 승격 R1-VAD0-QUALITY (TRACK A CLOSED)

```
정책 3층    episode_content_v3_summary_only + STT_VAD0 + output_quality_v1
표현        fixed presentation grouping 300초 · 9 group
LLM 재실행   없음 (기존 S5 content artifact 재사용)
```

```
NEW  5732075871fd…  tag submission-quality-2026-09-08
OLD  4e10aaab…      tag submission-vad0-2026-09-07     rollback 유지
이전  f874f643…      tag submission-ready-2026-09-03    rollback 유지
```

승격 스크립트가 지문 대조(candidate.source_run_fingerprint == 직전 fingerprint)와
`llm_rerun is False`를 먼저 검사하고, 그 정확한 파일에 대해 구조 validator·한글
Open()·PDF export를 다시 확인한다. rollback 2건 해시 전건 일치.

```
presentation eligible 39 / 41 · quality PASS 38 · SUSPECT 1 · FAIL 2
표현되지 않는 구간 EP13 (OUTPUT_LANGUAGE_DRIFT) · EP17 (PARSE_CONTRACT_FAILURE)
한글 Open() True · PDF 115,382 B · 본문 7,907자 · 구조 validator PASS
scripts/v2_1_promote_candidate.py · tests/test_v2_1_promote_candidate.py 13건
docs/finalization/V2_1_SUBMISSION_QUALITY_PROMOTION_2026-09-08.md
```

repository default v2 · VAD0 default OFF · raw STT·B1 확정 인덱스 무변경.
full 재전사 · B1' · B2 재생성 · official test · M9 는 계속 HOLD.

---

## 10. 2026-09-08 — STT_RETRANSCRIBE_V1 canary (TRACK B · 결론: HOLD 유지)

```
사전등록      docs/preregistration/STT_RETRANSCRIBE_V1_2026-09-08.md (commit 1d36bad · 실행 전 커밋)
결과 문서      docs/probes/STT_RETRANSCRIBE_V1_CANARY_2026-09-08.md
승인 범위      600–780초 × 2회. full 40.4분 · B1' · B2 재생성은 계속 HOLD
```

`vad_filter=True` + Phase A freeze VAD 파라미터로 3분 클립을 돌리면 **전사가 0발화**다.

```
오디오 180.0초 · frozen VAD가 찾은 발화 1.68초(chunk 1개 · 178.32–180.00초)
새 전사 0발화 · 기존 설정 전사 26발화
게이트 C1~C8 정의대로는 전부 true지만 행이 0이라 **공허한 통과**다
CANARY_DETERMINISM = 평가 불가 (빈 전사 두 개가 같은 것은 재현성 증거가 아니다)
```

VAD 발화 구간과 겹치던 3건(`샘플 두께 이정도니까` · `아 네` ·
`앉아보시고 그러시면 됩니다`)도 출력되지 않았다. **원인은 미분리다**(2026-09-08 정정) —
VAD는 1.68초를 남겼고 그 오디오는 디코더에 들어갔다. 따라서
`VAD가 실제 발화를 잘랐다` · `false-negative가 실현됐다`고 쓰지 않는다. 후보 원인은
① Silero 과소검출 ② 짧은 fragment 디코딩 실패 ③ Whisper no_speech·logprob 억제.
GT가 없어 나머지 23건이 환각이라고도 판정하지 않는다.

계측 결함 1건을 보정했다(재전사 없음): C8이 요청 범위(절대 600–780)로 클립
segments(상대 0–180)를 필터해 0행이었다 → `segments_range()`로 고치고 0행이면
통과하지 않게 막았다. 보정 결과 36행 · segment_id 0–35 · 경계 무변경.

사전등록 결함도 기록했다 — **비퇴화 전제조건이 없었다.** 지금 추가해 재판정하지
않고 새 사전등록 사건으로 남긴다.

```
다음 선택지(실행 승인 없음)
① VAD-제약 디코딩을 "frozen 파라미터에서 too aggressive"로 종료
② 비퇴화 게이트 + VAD 파라미터 재검토 → threshold 사건이므로 별도 승인
③ 발화가 분명한 구간에서 VAD만 재실행하는 진단 프로브(POST_HOC_DIAGNOSTIC_ONLY)
```

기존 산출물 무변경 확인(감시 6개): 기존 stt_cache 2종 · 클립·전체 segments ·
현행 제출본 hwpx · rollback hwpx.

## 11. 2026-09-08 — WHOLE_VIDEO_REPORT_LIGHT_V1 capacity probe (C01: CAPACITY_FAIL)

사전등록 `docs/preregistration/WHOLE_VIDEO_REPORT_LIGHT_V1_2026-09-08.md`를
commit `d6153ed`로 **먼저 동결**한 뒤 정확히 그 구성으로 10분 chunk 1개를 돌렸다.

```
MODEL_LOAD          PASS    Qwen3-VL-8B-Instruct bf16 · 17,336.1 MiB · 로드 35.12초
VIDEO_PROCESS       PASS    300프레임(512×288) → video token 21,600 · input 23,463
10MIN_INFERENCE     CAPACITY_FAIL   prefill에서 torch.OutOfMemoryError
peak                allocated 21,890.1 / reserved 23,460.0 / device 23,972.1 MiB
offload             0 (device_map을 쓰지 않았다)
SEMANTIC_RESULT     NOT_EVALUATED
```

**chunk를 줄여 성공시키지 않았다.** 사전등록 §11대로 CAPACITY_FAIL을 기록하고
멈췄다. fps·해상도·max_new_tokens를 낮추는 것도 같은 금지에 해당한다.

원인은 좁은 여유다 — 가중치 17.3GB 뒤 남은 약 6.6GB에 23.5k 토큰 prefill의
활성값이 들어가지 못했다. 사전등록 시점 계산은 KV만 셌고 **prefill 활성값을
넣지 않았다.**

```
확인됨   transformers 5.14.1 + torch 2.13.0+cu130에서 Qwen3-VL 로드·video 경로 동작
확인됨   토큰 예산 산식 일치 (512×288 → 144/프레임 · temporal 2 → 150×144 = 21,600)
안 됨    10분 chunk 추론 · event extraction 이후 전 단계
```

다음 축(B 표집 축소 · C attention 구현 · D 양자화 · A chunk 축소)은 전부 capacity
파라미터 변경이므로 **별도 사전등록**이다. 상세: `docs/probes/WVR_LIGHT_V1_CAPACITY_C01_2026-09-08.md`.

```
코드     src/wvr_prompts.py (계약 8종 · hash 동결) · src/wvr_contract.py ·
        scripts/wvr_capacity_probe.py · tests/test_wvr_contract.py (72건)
뮤테이션 13건 전부 RED (구멍 없음)
산출물   runs/wvr_light_v1/capacity_C01.json · capacity_C01.log
상태     SUBMISSION_PROMOTION HOLD · official test UNOPENED · M9 HOLD ·
        R1-VAD0-QUALITY FROZEN 무변경
```

## 12. 2026-09-08 — GPU 서버 캐시 정리 (T1+T2+T3 · 승인 후 실행)

디스크만 회수하는 작업이다. **Qwen3-VL-8B의 VRAM OOM과는 무관하다** — 그건 §11의
prefill 용량 문제다.

```
삭제 전 free    806.2 GiB      (934G 사용 · 54%)
회수            319.5 GiB      (계획 322.4 · 차이는 파일시스템 오버헤드)
삭제 후 free  1,125.7 GiB      (614G 사용 · 36%)
manifest        runs/ops/server_cleanup_2026-09-08.json  (삭제 전에 먼저 기록)
항목            28건 — T1 비채택 VLM·LLM 12종 · T2 비채택 임베더 10종 ·
                T3 pip 캐시·stale lock 387개·tmp_b02b_* 4개
```

지운 것은 전부 **HF에서 재다운로드 가능한 가중치**다. 코드 참조를 먼저 확인했다 —
`config.yaml`이 실제로 부르는 것은 `Qwen2.5-VL-3B`·`KURE-v1`뿐이고, 나머지 이름은
진단 스크립트의 사용법 예시·파일명 패턴 문자열이었다.

```
보존 (7종)   Qwen3-VL-8B(WVR) · Qwen3-VL-4B(후보) · Qwen2.5-VL-3B(배포 캡션) ·
            Qwen2.5-VL-7B(서버 옵션) · Qwen2.5-7B(M8/M9) · KURE-v1 · faster-whisper-large-v3
보존 (생성물) data · data_aihub · work_* · runs · results — 재취득 불가·재생성에 GPU 필요
승인보다 좁게 backup_pre_* · quarantine_untracked_2026-08-17 · failed_gate_8th은
            사고·rollback 기록이라 남겼다(합쳐 5MiB 미만이라 회수 이득이 없다)
사후 검증    보존 7종 경로 존재 True · 잔존 대상 0건 ·
            Qwen3-VL-8B snapshot 0c351dd0… 무변경
```

## 13. 2026-09-08~09 — WVR capacity 축과 sampling semantic-density 체인

C01 `CAPACITY_FAIL`(§11) 이후 사전등록 5건을 순서대로 열었다. 전부
prereg → commit → 실행 순서이고, 결과를 본 뒤 파라미터·게이트를 바꾼 사건은 없다.

```
WVR_CAPACITY_ALLOC_V1        CLOSED / CAPACITY_FAIL
  A0 default allocator · A1 expandable_segments — 둘 다 OOM
  treatment는 실제로 적용됐다(is_expandable true · segment 268→2 ·
  inactive_split 3.0GiB→0 · +1,433.7MiB 수용 · 부족분 441.25→71.25MiB)
  baseline delta 0.0MiB · workload 차이 0 → 단일 변수 비교로 유효
  A0 baseline 434.1MiB가 C01과 동일 → "GPU가 더러워서 죽었다"는 가설 기각

WVR_CAPACITY_SAMPLING_V1     CLOSED / CAPACITY_PASS
  단일 변경 fps 0.5→0.25 (150프레임) · allocator default · 해상도 512×288 동결
  input 11,927 tok · 여유 2,730MiB · 1,024 tok 생성 완주
  용량만 통과했고 의미 충분성은 이 사건에서 주장하지 않았다

density Stage 1              REVIEWED / PASS   (VISUAL_TEMPORAL_COVERAGE)
  0.25fps ⊂ 0.5fps 포함관계 잠금 · 임계값 없음 · 창 선택 결정적
  D1 W11 300–480 · D2 W02 30–210 · D3 W05 120–300 (D2·D3는 90초 겹침 — n=3 아님)
  최소 luma novelty 5.7761 · 첫 실행은 INVALID / STALE_BYTECODE_EXECUTION로 기록

density Stage 2 (V1)         CLOSED / INCONCLUSIVE — OUTPUT_TRUNCATION_AT_GENERATION_CAP
density Stage 2B (V1B)       CLOSED / INCONCLUSIVE — 표현 축퇴 + 시간전용 매칭 + D2 언어 confound
density V2                   게이트 DECISION_ELIGIBLE → 리뷰 판정 후
                             CLOSED / PAIRED_OUTPUT_STABILITY_HOLD
  event interval 계측기 · English-only · 연속 run-collapse · 2단 매칭
  자동 의미 동등 0·0·1 · ADJUDICATION 110쌍 · 리뷰어가 D1·D2에서
  report-material 충돌을 확인 → acceptance 조건(충돌 0) 실패

WVR_EVIDENCE_RESOLUTION_V1   CLOSED / BRANCH_C_EVIDENCE_INCONCLUSIVE
  새 추론 0회 · 채택 제출본 evidence 층(segments sha aa008317… · aar_canonical 41 episode)
  REFERENCE_ONLY 3 · ARM_ONLY 10 · BOTH 0 · NEITHER_RESOLVED 37
  리뷰어 지목 9행 중 8행 미해결 (D1 420–450 sewing · D2 104–112 ricer 포함)
  subtitle 채널 판정력 0 · 지지 hit 17건 전부 캡션(다른 VLM 출력)
  상세 docs/probes/WVR_EVIDENCE_RESOLUTION_V1_2026-09-09.md
```

현재 고정 상태:

```
0.25fps semantic sufficiency   NOT ESTABLISHED      0.5fps factual authority   NO
WVR event extraction           HOLD (chapter·highlight·report 포함)
QWEN_OUTPUT_LANGUAGE_STABILITY OBSERVED / REPRODUCED_ON_D2_S0 · 별도 실험 HOLD
flash-attn 설치 · 양자화 · chunk 축소 · repetition_penalty 변경   전부 HOLD
현행 제출본 R1-VAD0-QUALITY    READ-ONLY / NO PROMOTION
official test UNOPENED         M9 HOLD
```

분기 C의 다음 단계는 **리뷰어가 짧은 동일-window paired probe를 새로 설계하는 것**이고,
프레임 실물 열람으로 판정을 격상하는 것은 별도 승인 사건이다.

## 14. 2026-09-09 — SHORT_WINDOW · FRAME_ADJUDICATION · SHADOW (TRACK C)

§13 체인의 후속이다. 전부 prereg → commit → 실행 순서이고, 결과를 본 뒤 파라미터·
게이트·판정 어휘를 바꾼 사건은 없다.

```
WVR_SAMPLING_SEMANTIC_DENSITY_SHORT_WINDOW_V1   CLOSED / PAIRED_STABILITY_HOLD
  단일 변경 context 180초 → 48초 · 창 P1 360–408 · P2 416–464 · P3 104–152
  (미해결 충돌 cluster에서 결정적 파생) · 6 arm 전부 기술 게이트 통과
  blinded 판정: P1 GRANULARITY_SHIFT · P2 MATERIAL_DIVERGENCE · P3 MATERIAL_DIVERGENCE
  → long-context-only-collapse 가설 NOT SUPPORTED
  divergence 방향 비대칭 — P2 food-on-plate 전환·P3 peeling 단일구간은 둘 다 S0(고밀도)
  자동 matcher EQUIVALENT 0·0·2 → 사람 판정을 primary로 둔 결정이 수치로 정당화됨

WVR_..._FRAME_ADJUDICATION_V1                   CLOSED / SAMPLING_LOSS_CONFIRMED
  추론 0회 · KEEP 14 · DROP 14 프레임(512×288 · 모델 입력 픽셀)
  Q1 P2 448–464  GENERATION_ERROR_NOT_SAMPLING / S0_CLAIM_MATCHES
  Q2 P3 104–128  GENERATION_ERROR_NOT_SAMPLING / S1_CLAIM_MATCHES
  Q3 P3 128–144  DROP_FRAMES_CARRY_MATERIAL_INFORMATION / S0_CLAIM_MATCHES
  → 같은 56초 안에 표집 손실 1건과 생성·표현 오류 2건이 함께 나왔다
  절차 한계 공개: packet 검증 때 혼합 대지를 먼저 열어 관찰 순서가 오염됐다
  (observation_order = KEEP_THEN_DROP_WITH_PRIOR_COMBINED_EXPOSURE · 무효 사유로 쓰지 않음)

WVR_EVENT_EXTRACTION_SHADOW_V1                  CLOSED / INCONCLUSIVE
  C01 전체를 48초 창 · 24초 stride로 tile → 24창 · 24회 추론 · 재시도 0
  inference 설정 변경 NONE (SHORT_WINDOW S0 대비) · architecture characterization
  23창 WINDOW_VALID · W00 [0,48) INVALID
    증상: max_new_tokens 4096까지 start_sec=0/end_sec=0 반복 → JSON 미완결
          cap 도달은 반복의 결과에 가깝다 (언어 플래그는 파싱 실패의 하류 결과)
  겹침 identity 23/23 — 공유 12/12 · 픽셀 sha256 불일치 0 · bank 대조 576건 0
  reason WINDOW_TECHNICAL_INVALID → 사건 INCONCLUSIVE
  overlap semantic stability · frame-grounding 모두 NOT ADJUDICATED (부분 판정도 안 함)
  blinded mapping 봉인 유지 · W01~W23 raw·packet 보존
```

현재 승인 범위와 정지선:

```
W00_DEGENERACY_FORENSIC_V1   NEXT / APPROVED · READ-ONLY · NO NEW QWEN INFERENCE
  분류 4값 INPUT_PIPELINE_DEFECT · MODEL_OUTPUT_DEGENERACY · MIXED · UNRESOLVED
금지   SHADOW_V1B·재실행 · token cap 4096→8192 상향 · prompt 수정 ·
      mapping reveal · production Event extraction 이하 전 단계
0.25fps semantic sufficiency  NOT ESTABLISHED
0.5fps                        PROVISIONAL WORKING DENSITY ONLY
현행 제출본 R1-VAD0-QUALITY     READ-ONLY   official test UNOPENED   M9 HOLD
```

상세: `docs/probes/WVR_SHORT_WINDOW_V1_2026-09-09.md` ·
`docs/probes/WVR_FRAME_ADJUDICATION_V1_2026-09-09.md` ·
`docs/probes/WVR_EVENT_EXTRACTION_SHADOW_V1_2026-09-09.md` ·
트랙 지도는 `docs/PROJECT_OVERVIEW.md`.
