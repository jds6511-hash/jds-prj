# 사람 서술 ↔ 동결 GT 정성적 대조 — 2026-08-28

```
성격   illustrative sanity check · 정성적 대조 1편
아님   독립 평가 · 모델 성능 비교 · 사건 입도 정의의 보편적 명확성 증명
용도   보고서·발표의 정성적 human-reference contrast
```

**공식 판정을 바꾸지 않는다.** `M8 evaluation COMPLETE · acceptance FAIL`은 그대로다.

---

## 1. 무엇을 대조했나

대상: `wonyi_geoje` (거제 여행, 1,632초 · 327구간). **소비된 판정 패널 8편 중 1편.**

사람이 이 영상에 대해 쓴 사건 중심 서술 7개 절을 동결 GT 8건과 **사후에** 대조했다.

```
사람이 쓴 서술 7절                동결 GT 8건
1 거제 여행의 시작           →   1 거제시내를 대화하며 이동     0:00~4:25
2 시내버스를 타고 해변으로     →   2 버스를 타고 이동           4:25~5:55
                                3 바닷가로 이동             5:55~8:15    ← 병합
3 주민들과 만나 낚시 준비      →   4 해산물 구경과 시식         8:15~9:55
4 방파제에서 줄낚시 도전       →   5 부두 근처로 나가 낚시       9:55~14:50
5 해변으로 돌아와 입수        →   6 해변에서 물놀이           14:50~19:00
6 식사 준비와 주민 교류       →   7 해변가에서 자리잡고 식사     19:00~27:00
7 해변에서 식사하며 마무리     →   7 (같은 사건의 후반)                   ← 분할
  (없음)                    →   8 아웃트로                27:00~27:11   ← 누락
```

```
병합 1건 · 분할 1건 · 누락 1건
같은 영상 M8 공식 산출물: 생성 10건 중 GT와 매칭되지 않은 것 3건
```

---

## 2. 보고서에 쓸 문구 — 이대로 인용한다

> **사람이 작성한 사건 중심 서술은 이 영상에서 동결 GT의 사건 입도와 대체로 유사한
> 구조를 보였다.** 7개 서술 절을 GT 8건과 사후 대조했을 때, 인접 GT 2건을 하나로 묶은
> 사례 1건, 하나의 GT를 두 절로 나눈 사례 1건, 짧은 아웃트로 누락 1건이 있었다.
> 이는 이 사례에서 사람이 사건 단위의 서술 형식을 비교적 자연스럽게 구성할 수 있었음을
> 보여주는 정성적 대조다. 다만 단일 영상의 사후 대응이므로 사건 입도의 보편적 명확성이나
> 모델 성능 차이를 검증하는 근거로 사용하지 않는다.

M8과 대비할 때는 여기까지만 쓴다.

> 같은 영상에서 M8은 10건을 생성했고 그중 3건이 GT와 매칭되지 않았다. 반면 사람이
> 작성한 사건 서술은 GT 구조와 비교적 가까운 형태를 보였다.

발표 한 줄 요약.

> 이 사례에서는 목표 사건 단위가 사람이 작성하기 어려운 형식으로 보이지 않았다.
> 단, 단일 영상의 정성적 대조다.

---

## 3. 쓰지 않을 진술

```
쓰지 않는다   "사건 입도 정의가 모호하지 않음을 증명했다"
쓰지 않는다   "따라서 M8 실패 원인은 granularity가 아니라 모델이다"
쓰지 않는다   "사람은 GT와 ±1로 일치한다" (일반화 표현)
```

근거가 못 받쳐주는 이유를 그대로 적는다.

```
① 소비된 판정 패널의 영상이다 — 독립 표본이 아니다
② 7절 ↔ 8건 대응은 **사후에 사람이 매핑**한 것이다
③ 서술과 GT를 **같은 사람**이 만들었다
④ n = 1편이다
```

---

## 4. 형식에 대해 — 이번 범위에서 하지 않은 것

사람이 쓴 산문은 현행 M8 출력 계약과 세 군데에서 어긋난다(진단으로만 기록).

```
인용 없음      M8 스키마는 문장마다 cites를 요구하고, M9 structural precheck는
              인용 없는 평가대상 문장을 STRUCTURAL FAIL로 거부한다
해석 포함      "영상의 주요 특징"·"결론"은 근거 구간으로 뒷받침되지 않는다.
              인용 면제 역할은 title·metadata·limitation 셋뿐이다
분량           GT 8건에 그 분량이면 C3(문장 수 ÷ 정답 사건 수)가 임계 2.0을 넘는다
```

**이 관찰을 근거로 M8 목표 형식이나 프롬프트를 바꾸지 않았다.** 소비된 패널에서 유도한
설계가 되고, 2026-08-28에 닫은 종결(`M8_REDESIGN_CLOSURE_2026-08-28.md`)을 되돌리는
것이 된다. 사전등록 초안도 작성하지 않았다 — future work에 한 문장으로만 남긴다.

```
향후 fresh 데이터에서 재검증할 경우, 사건당 1~2문장과 문장별 segment citation을 갖는
사건 중심 출력 형식을 사전에 동결한 뒤 평가할 수 있다.
```

---

## 5. 경계

```
official 판정 변경        없음
M8 프롬프트·형식 변경      없음
사전등록 신규 작성         없음
fresh panel · 새 라벨      없음
M9                       HOLD 유지
```
