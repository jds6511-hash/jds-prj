# 인프라 개선 4건 — 준비 완료, 통합은 HOLD (2026-08-22)

**연구 결과·모델 성능·사전등록 규칙을 바꾸지 않는다.** 목적은 재현성·관찰가능성·입력
커버리지·provenance 안정성이다. 네 모듈 모두 **독립 모듈 + 테스트 + 문서까지만**이고,
실행 중인 P2 FULL(`p2idx_0821d`, commit `ab73e1c`)의 코드·config·control flow·산출물에
연결하지 않았다.

## 1. CANARY 입력 커버리지 — `scripts/canary_coverage.py`

```
문제       판정 기준이 "CANARY N편 PASS"라서 입력 **종류**를 못 밟아도 통과했다
사고       하이픈 id(-_mgcIUbbX4) argparse 오인 · AV1 cv2 디코드 실패 ·
           ffmpeg 4.4.2 -fps_mode 미지원. 앞의 둘은 FULL 31편째에 드러났다
새 계약     FULL 입력에 존재하는 입력 클래스를 CANARY가 전부 밟았는가.
           축 5개는 전부 corpus에서 관측되는 속성이다 —
           codec(native_h264 / transcoded_h264) · provenance(new / legacy) ·
           id_shape(plain / cli_sensitive) · duration(shortest / longest) ·
           audio(known / unresolved).
           없는 범주를 만들지 않는다. 클래스 소속이 0인 축은 생기지 않는다.
           성능·캡션·자막·검색 결과는 입력으로 쓰지 않는다(정렬 키는 구간 수·id뿐)
구현       inventory() · coverage() · require_coverage()(fail-closed) ·
           select_representatives()(결정적 최소 표본) · probe_codec_class()
테스트      tests/test_canary_coverage.py 22건 — 축 5개 · 관측되지 않은 범주 금지 ·
           FULL 입력 밖 영상으로 커버리지 채우기 거부 · fail-closed · 선정 결정성 ·
           금지 토큰(caption·mrr·recall·score·rank·segments.json) 0
호환성      기존 launcher·plan·hook을 건드리지 않는다. 새 검사만 추가된다
FULL 영향   없다 (연결 안 함)
통합        HOLD — FULL 종료 후 plan의 CANARY 게이트로 연결 판단
```

**실측 1건.** 이번에 실제로 돌린 CANARY(`OBxKlA5rxjQ`, `baekmansonghee_jirisan`)를
넣으면 10개 클래스 중 **2개 미포함**이다.

```
missing   duration:longest · id_shape:cli_sensitive
제안 표본  -_mgcIUbbX4 · JPaaxxI3CEY · OBxKlA5rxjQ · baekmansonghee_jirisan  (4편, 10/10)
```

미포함 하나가 **사고 ①의 원인 영상 그 자체**다. 즉 이 검사가 있었다면 사고 ①은
CANARY에서 걸렸다.

## 2. 세션 인수 생성기 — `scripts/make_handoff.py`

```
문제       새 세션이 낡은 스냅샷을 읽고 이미 닫힌 결론으로 되돌아간다
새 계약     **수집기다. 해석하지 않는다.** 지표를 읽어 PASS/FAIL을 만들지 않고,
           문서를 비교해 모델 승자를 추론하지 않으며, 날짜를 코드에 박지 않는다.
           없는 항목은 null로 두고 "관측하지 못했다"고 적는다.
           항목마다 source를 붙인다
수집       git HEAD·dirty·최근 커밋 5 / 최신 작업현황(파일명 날짜 최대) /
           GO·HOLD·다음 승인 지점(원문 코드블록 그대로) / 현재 run 상태 /
           테스트 결과(--with-tests로 돌렸을 때만) / config.yaml / preregistration 목록
구현       latest_status_doc() · sections() · collect() · render()
테스트      tests/test_make_handoff.py 17건 — 판정·지표 계산 토큰 0 ·
           날짜 하드코딩 0 · 최신 문서 결정적 선택 · 없는 절은 null ·
           마커를 완료로 쓰지 않음 · 항목마다 source 존재
호환성      읽기 전용. 기존 문서를 수정하지 않는다
FULL 영향   없다 (읽기만)
통합        지금 사용 가능 — 산출물 docs/HANDOFF_CURRENT.md
```

## 3. 중앙 provenance registry — `scripts/video_registry.py`

```
문제       영상 identity가 선정표본·승격기록·입력해시게이트·변환기록·meta로 흩어져
           "이 파일이 어디서 왔고 어떤 바이트인가"를 한 곳에서 답할 수 없다
새 계약     신규는 source_id·source_url·production_sha256 없으면 fail-closed /
           video_id·source_id 중복 fail-closed / sha256 형식 검사 /
           staging≠production이면 사유 필수(승격은 복사이므로 같아야 한다) /
           legacy_exempt는 acquisition_class=pre_existing + 사유 필수 /
           legacy에서 없는 필드를 채우면 거부(추측 금지) /
           identity 5필드는 다운스트림이 못 바꾼다(assert_identity_unchanged) /
           registry 필드를 지표·적격성·채택 판단에 쓰지 않는다
구현       schema 상수 · validate() · load()(읽기 전용, writer 없음) ·
           assert_identity_unchanged() · project_from_selection()
테스트      tests/test_video_registry.py 23건 — 위 계약 전부 +
           기존 선정표본 투영 35건이 스키마 검증 통과(legacy 4건, 값 조작 0)
호환성      기존 manifest를 읽기만 한다. writer가 없어서 전환이 불가능한 상태다
FULL 영향   없다
통합        HOLD — source of truth 전환은 FULL 종료 후 별도 GO/HOLD.
           경로 후보 data/registry/videos.jsonl
```

## 4. 실행 상태·마커 — `scripts/run_status.py`

```
문제       같은 run_id의 CANARY 마커가 남아 FULL 상태를 오독했다(2026-08-22 실제 사고)
오염 없음   ① 단계 소요는 배치 내부 time.time() ② 마커는 건너뛰기 근거가 아니다
           ③ hook은 *_full.json 우선 ④ launcher no_stale_marker는 RUN_COMPLETE만 본다
새 계약     마커 이름을 mode로 분리(CANARY_STAGE_*_DONE.json / FULL_STAGE_*_DONE.json)하고
           내용에 mode·run_id·commit·stage·created_at·output_hash를 담는다.
           판독기는 **현재 실행과 맞는 마커만** 완료로 세고, 불일치는 ignored_markers,
           옛 이름은 legacy_markers로 따로 적는다.
           완료 근거는 RUN_COMPLETE.json뿐이고 이 모듈은 그것을 쓰지 않는다(읽기만).
           기존 마커를 지우거나 옮기지 않는다
구현       marker_name() · write_marker() · status() · render()
테스트      tests/test_run_status.py 13건 — CANARY 마커가 FULL 완료로 세지 않음 ·
           다른 run_id/commit 마커 무시 · 옛 이름 마커 불신 · 읽기 전용 ·
           삭제·이동 토큰 0 · RUN_COMPLETE 쓰기 경로 0
호환성      기존 stale marker 검사를 약화시키지 않는다(추가 검사만 한다)
FULL 영향   없다. **이번 run의 마커 이름·control flow는 손대지 않았다**
통합        HOLD — p2_index_batch/launcher 연결은 FULL 종료 후.
           지금 연결하면 실행 중 commit이 바뀌어 execution_commit 규율이 깨진다
```

## 요약

```
테스트        928 passed (신규 75건: 커버리지 22 · handoff 17 · registry 23 · status 13)
FULL 영향     none — 서버 트리 clean 0건, commit ab73e1c 불변, 마커·산출물 무수정
지금 통합 가능  make_handoff (읽기 전용)
FULL 후에만    canary_coverage 게이트 · registry source-of-truth 전환 · marker/status 연결
```

### 새로 발견한 위험 2건

```
1  현재 CANARY 규칙이 통과시킨 조합이 실제로는 클래스 2개를 빠뜨렸다.
   duration:longest(JPaaxxI3CEY 399구간)와 id_shape:cli_sensitive(-_mgcIUbbX4).
   이번 FULL은 이미 m1·m2를 전 편 통과했으므로 그 두 종류를 FULL에서 밟았다 —
   지금 조치할 것은 없고, 다음 run의 게이트로 쓴다
2  선정표본에 기확보 4편의 duration_sec이 없다. registry 투영은 그 필드를 비워 두므로
   duration 기반 검사를 registry만 보고 하면 4편이 조용히 빠진다.
   intake는 파일을 직접 재서 닫았지만(작업현황 5-2), **registry 쪽은 아직 열려 있다**
```
