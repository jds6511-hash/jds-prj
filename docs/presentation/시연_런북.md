# 간단 시연 런북 (2026-07-14 검증 완료)

데모 영상: **pland_costco_hosting**(코스트코 하울·요리, 32:52, 395세그, **시연 전용·평가셋
미포함**). 전체 경로는 오늘 브라우저 E2E 12항목 전부 통과로 검증됨.

## 실행 순서 (시연 2~3분 전에 미리)

1. **포트 정리**(이전 서버 잔여 확인):
   ```bash
   # 7860 점유 시 PID 확인 후 종료 (PowerShell)
   #   Get-NetTCPConnection -LocalPort 7860 -State Listen | Select OwningProcess
   #   Stop-Process -Id <PID> -Force
   ```
2. **서버 기동**:
   ```bash
   python3 src/m7_webui.py --alpha 0.5 --port 7860
   ```
3. 브라우저에서 **http://localhost:7860** 접속.
4. **영상 업로드**: `data/videos/pland_costco_hosting.mp4` 업로드
   → 인덱스 산출물(work/pland_costco_hosting)이 이미 있어 **resume으로 수 초** 내 준비
   (395세그 렌더링·무발화 표기 확인).
5. **워밍업 검색 1회**(청중 보기 전): 첫 검색은 KURE 임베딩 로드로 **~1분** 걸린다.
   아무 질의나 한 번 던져 로드를 끝내두면 이후 검색은 즉시.

## 시연 질의 (전부 오늘 프레임 실물 검증 완료)

핵심 메시지: **"자막(말한 것)엔 없는데 화면(보이는 것)으로 찾아낸다."**

| # | 질의 | 결과(top-1) | 말할 포인트 |
|---|---|---|---|
| 1 | **새우를 손질하는 장면** | 13:45 — 도마에서 새우 손질(그릇에 손질된 새우) | 이 구간 **자막은 "고추/상차림"**(무관). 말론 못 찾는데 캡션이 새우를 잡아 1위. score 3.34로 결정적 |
| 2 | **상품이 진열대에 잔뜩 쌓여 있는 장면** | 08:15 — GLAD·Tefal·커클랜드 상품 더미 | 자막은 "헬스장"(무관). 코스트코 하울 장면을 시각으로 검색 |
| 3 | **밤하늘에 불꽃놀이가 터지는 장면** (무관) | (배너) "이 영상에 관련 구간이 없을 수 있습니다" | 영상에 없는 걸 물으면 **경고 배너** — 단, 결과는 숨기지 않고 함께 보여줌(연구 도구로 오판 관찰). 정직성 어필 |

- 카드 클릭 → 플레이어가 해당 초로 점프(시연 시 13:45 카드 클릭해 새우 장면 재생).
- 여유 있으면: 질의 1을 하면서 "말한 것만 검색하는 기존 방식이면 이 장면을 놓친다"를 강조.

## 만약을 위한 대비

- **첫 검색이 느리면**: KURE 로드 중(정상, ~1분). 워밍업을 미리 했다면 안 겪음.
- **업로드가 안 붙으면**: jobs가 인메모리라 서버 재시작 시 재업로드 필요. mp4 경로
  `data/videos/pland_costco_hosting.mp4` 확인.
- **포트 충돌(10048)**: 위 1번으로 잔여 서버 종료 후 재기동.
- **전체 경로 재검증**(불안하면 시연 전 1회):
  ```bash
  python3 .claude/skills/webapp-testing/scripts/with_server.py \
    --server "python3 src/m7_webui.py --alpha 0.5 --port 7860" --port 7860 --timeout 60 \
    -- python3 scripts/m7_browser_e2e.py
  ```
  마지막 줄 "결과: 전부 통과" 확인(스크린샷 results/e2e/).

## 한 줄 요약

말한 것(자막) + 보이는 것(캡션)을 함께 검색 → 말없이 지나간 장면도 시각으로 찾고,
없는 건 없다고 경고한다. (수치·방어는 예상질문_방어.md, 확정 상태는 DESIGN_SPEC §8-0.)
