# 작업 현황 스냅샷 (2026-07-13 오후 기준)

> 세션 재시작 시 이 문서부터. 규율은 프로젝트 CLAUDE.md, 배치·검증·E2E 절차는
> .claude/skills/{gpu-batch, pipeline-verify, m7-e2e} 스킬 참조.

## 확정 상태 (변경 없음이 기본)

- **최종 config**: α*=0.5(tie_set [0.2–0.5], CLI 주입), seg_len 5초, caption_prompt P0,
  static_threshold 0(치환 off), **abstention max(sub,cap) τ=0.55(2026-07-13 개정)**,
  embed KURE-v1.
- **공식 test (n=39)**: baseline mrr 0.6489 → proposed 0.7953, hit@1 0.5641→0.7179(유의),
  hit@5/10 CI 0 포함(유의 주장 금지), 장면형 0.174→0.667. 접촉 이력: 튜닝 0회·공식 실행
  4회(8-6에 사유 명기). **test 재평가 금지.**
- **테스트 133개 통과**, 브라우저 E2E 12체크 통과(scripts/m7_browser_e2e.py).

## 7/12~13 완료분

- 데모 영상 인덱싱 완료: pland_costco_hosting(32:52, 395세그, 세정 2건 잔존은 규약상
  정상) — **시연 전용, 평가셋 미포함.** M5 실검색·M7 브라우저 E2E 검증 완료.
- 설계 점검 2라운드: 사용자 6건(1·2번은 thr=0으로 이미 해소 — ablation 2-4-3 명문화,
  5·6번 문서 반영) + 자체 점검(abstention 장면형 편향 → **max 채널 τ=0.55 교정, 오배제
  0/96**, IoU≡hit@1 각주, wl_q03 라벨 예외 문서화). 커밋 8fafae9, b62ecf1.
- 오염 캡션 harm 실측: 타 질의 무해(전파 없음), 자기 세그먼트만 국소 손실 —
  docs/probes/corrupt_harm_probe.py. 빈 자막 임베딩: 정상 단일 벡터, τ 바닥 형성 —
  empty_sub_probe.py.
- claude-mem 설치(worker 37777) — 다음 세션부터 메모리 주입.
- 발표 방어 스크립트 완성: **docs/presentation/예상질문_방어.md (Q1~Q13 + 한 줄 요약)**.
- 실사용자 테스트 실패 모드 2종 발견·진단·처방 실증(Q13, 본 시스템 미반영 dev 프로브):
  동의어 갭(초밥→스시, 질의 확장으로 21→2위 복구) + 언급≠행위(마늘, 3B verifier 기각
  4/4·양성 2/4 — 크기 종속 확인). 재현: docs/probes/synonym_expansion_probe.py,
  vlm_verifier_probe.py. **판정 시 프레임 실물 검증 필수 규율은 분석에도 적용할 것**
  (자막·캡션 텍스트만 보고 장면 판정했다가 사용자 지적으로 정정한 전례).

- 구조 점검 실측(2026-07-13 밤, 전부 dev·기존 데이터 재분석 — test 미접촉): ① 유형별
  oracle α 여유폭 +0.073(자막형 0.8/장면형 0.0/복합형 0.4로 분화), ② **minmax 정규화가
  z-score 대비 유의 열세(−0.065, CI 0 배제)** — 무정규화보다도 나쁨, ③ test 개선의 영상
  간 균질성(+0.140~0.153, 4장르) = 일반화 강점, ④ RRF 열세(부정 결과). 상세: 방어 문서
  Q14, docs/probes/fusion_alternatives_probe.py. **정식 반영(z-score·유형별 α)은 확정
  절차 필요 — 발표 전 config 변경 금지, 발표 후 1순위 실험.**

## 다음 할 일 (우선순위)

1. **중간발표 자료 완성** — docs/presentation/중간발표_골격_draft.md에 확정 수치 채우기.
   인용 기준: 8-6 + ablation 재검증 표(1-6-1, 3-6-1) + 예상질문_방어.md. 산출물이 .pptx면
   document-skills:pptx + 차트는 dataviz 스킬. 데드라인: 7/19 출국 전.
2. **튜터 서버 GPU 확답** (사용자 액션) — M8/M9 실구동·7B 캡셔너 비교·타 패밀리 judge.
3. 발표 후: 8-3(캡션 상한)·8-4(emb_joint), 캡션 드리프트 근본 해소(서버), 벤치마크 확장.
   → config 변경 유발 항목은 발표 전 착수 금지.

## 함정 목록 (재시작 시 주의)

- test 재평가·캡션 수동 편집·라벨 시 검색 참조 = 금지(CLAUDE.md 절대 규칙).
- 배치 수치는 UTF-8 JSON에서만. 재캡셔닝 후 m4 필수. 변형은 격리 config 재생성.
- M8/M9 로컬 실행 불가(실측 확정). scratchpad/m8m9_3b_rejected는 오염 산출물 격리분.
- work_bge·work_rp13은 폐기된 구 변형(현행 결론과 무관).
- 데모 시연은 웹UI에서 mp4 업로드(resume으로 수 초) — jobs가 인메모리라 서버 재시작 시
  업로드 필요.
