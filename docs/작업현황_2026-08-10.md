# 작업현황 2026-08-10 — 후보 모델은 이겼다. 그런데 배포하면 0이다

## 아침에 이것부터

밤사이 예약한 4단계가 **전부 정상 종료**했다(06:38). 서버에 도는 것 없음.

**한 줄 결론.** Qwen3-VL-4B/P1은 제3자 데이터(AI Hub n=1,086)에서 현행을 이겼다.
사전 등록 확증을 통과했다. **그런데 채택해도 dev 성능은 변하지 않는다**(Δ−0.0013).
모델 이득(+0.0913)을 생성 환경 손실(−0.0926)이 정확히 상쇄하기 때문이다.

**따라서 오늘 결정할 것은 "채택할까"가 아니라 "환경 손실을 없앨 수 있나"다.**
승인 요청은 §5에 정리했다. **test 재평가는 지금 하면 안 된다** — 이유도 §5에 있다.

읽을 순서:

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `docs/probes/_scratch/deploy_delta.json` | **핵심** — 채택 시 실제 변화량 |
| 2 | `_scratch/aihub_model_confirm.json` | 독립 확증 통과 기록 |
| 3 | `_scratch/caption_sweep.json` | P4 5 arm(2단계), 순위 역전 |
| 4 | `_scratch/frame_content_judge.json` | 판정 불가 + 게이트 탈락 사유 |

수치는 콘솔 말고 **UTF-8 JSON에서** 읽는다(cp949로 깨진다).

---

## 1. AI Hub 독립 확증 — 통과했다

dev에서 고른 후보를 제3자 데이터에서 **단일 비교**로 확인했다. 선택은 dev에서
끝났으므로 보정할 다중성이 없다. 프로토콜·판정 규칙은 결과 생성 전 커밋(`5410fb5`).

n = 1,086 질의 / 194 영상 / 2,328 프레임. 부트스트랩 B=2,000.

| 지표 | Δ (후보−현행) | 95% CI | 판정 |
|---|---|---|---|
| **캡션단독 MRR** (사전 등록 주 지표) | **+0.0342** | [0.0110, 0.0585] | 0 배제 |
| 융합 MRR | +0.0233 | [0.0019, 0.0438] | 0 배제 |
| hit@1 캡션단독 | +0.0414 | [0.0074, 0.0764] | 0 배제 |

판정: **독립 표본에서 재현됨 — 채택 검토 가능.**

부가로 확인된 것 셋.

1. **효과 크기가 dev의 1/3이다**(dev +0.0913 → AI Hub +0.0342). dev 수치는 16개
   arm 중 최대값을 고른 값이라 선택 편향으로 부푼다. 편향 없는 추정치는 이쪽이다.
2. **후보 캡션이 더 짧은데 더 좋다**(97.6자 vs 135.0자, 오염 19 vs 24). 길이가
   아니라 글자당 정보량이라는 dev 길이 일치 분석 결론이 독립 표본에서 재현됐다.
3. **유형별로 이득이 고르지 않다** — 드라마 +0.046, 여행 +0.026, **요리음식
   +0.001**. 요리 도메인에서는 사실상 이득이 없다.

---

## 2. 그런데 배포하면 변화가 없다 (핵심)

**스윕과 확증이 재는 것은 모델 효과뿐이다.** 둘 다 후보 검증 규약 4항에 따라
**같은 배치에서 새로 생성한 서버 대조군**과 비교한다. 그래야 모델 효과와 생성
환경 효과가 섞이지 않는다.

**하지만 실제 배포된 인덱스의 캡션은 노트북(RTX 3060)에서 생성한 것이다.**
후보(qwen3vl_4b, bf16 11.4GB 실측)는 6GB 노트북에서 못 돌리므로, 채택하면
전부 서버 생성분으로 교체된다. 사용자가 겪는 변화는 `서버 후보 − 노트북 현행`이고,
이건 스윕이 잰 값이 **아니다**.

`docs/probes/deploy_delta.py`로 분해했다(dev 96, 부트스트랩 20,000, 순열 200,000).

| 대비 | Δ | 95% CI | 순열 p |
|---|---|---|---|
| ① 모델 효과 (서버 후보 − 서버 대조군) | **+0.0913** | [+0.0066, +0.1778] | 0.0414 |
| ② 생성 환경 효과 (서버 대조군 − 노트북 현행) | **−0.0926** | [−0.1608, −0.0252] | 0.0085 |
| ③ **배포 변화** (서버 후보 − 노트북 현행) | **−0.0013** | [−0.0846, +0.0834] | 0.9752 |

평균 MRR — 노트북 현행 **0.5535** / 서버 대조군 0.4609 / 서버 후보 **0.5522**.
질의 단위로도 후보 우세 32 / 동률 32 / 현행 우세 32로 완전히 갈린다.

**② 가 ① 보다 크고 더 확실하다.** 환경 효과의 p=0.0085는 모델 효과의 p=0.0414보다
한 자릿수 작고, CI도 0에서 더 멀다. 그리고 이 값은 2026-08-07 실측
(Δ−0.0879 CI [−0.158, −0.023])을 독립적으로 **거의 그대로 재현**했다
([−0.1608, −0.0252]). 우연이 아니다.

**정리하면 지금 가장 큰 지렛대는 캡션 모델이 아니라 생성 환경이다.** 같은
모델·양자화·프롬프트·그리디인데 노트북과 서버의 캡션 완전일치가 25.6%뿐이라는
것이 8/7에 이미 나와 있었고, 그 차이가 MRR 0.09를 먹는다. 원인을 찾아 없애면
후보 모델의 +0.09가 **그때 비로소 실현된다**.

---

## 3. 2단계 P4 — 프롬프트가 바뀌면 순위가 뒤집힌다

"문장 수를 제한하지 않는다" 프롬프트(P4)를 상한 256토큰으로 5모델에 돌렸다.

| arm | 길이 | 절단률 | 캡션단독 MRR | 1단계 자기 최고 |
|---|---|---|---|---|
| varco_1_7b/P4@256 | 377.3 | 62.9% | **0.5272** | 0.504 (P1) |
| qwen25_7b/P4@256 | 190.6 | 94.7% | 0.4788 | 0.4566 (P1) |
| qwen3vl_4b/P4@256 | 363.1 | 83.0% | 0.4771 | **0.5522 (P1)** |
| qwen25_3b_4bit/P4@256 | 179.7 | 71.8% | 0.4447 | 0.4609 (P0) |
| kanana_3b/P4@256 | 0 | — | 0.0276 | (제외) |

**P4 조건의 1위는 varco다.** qwen3vl은 1위에서 3위로 떨어진다(−0.0751). 7B와
varco는 문장 수 제한을 풀자 올랐고, qwen3vl과 3B는 떨어졌다. **어떤 모델도
프롬프트를 건너 지배하지 못한다** — model×prompt 상호작용이 모델 주효과보다 크다.

**단, 전체 최고는 바뀌지 않았다.** 21개 arm 전체에서 여전히 qwen3vl_4b/P1
0.5522가 최대이고, varco/P4 0.5272는 그 아래다. 선택한 후보는 유효하다.

**varco/P4를 새 후보로 올리면 안 된다.** P4는 사후 가설로 선언해 1단계 격자에서
뺀 조건이고, 교차검증 A~E도 BH-FDR family 14개도 이 arm을 포함하지 않는다. 지금
승격시키면 결과를 보고 후보를 고르는 것이 되어 지금까지의 절차가 무효가 된다.
넣으려면 family가 19개로 늘고, varco/P4는 자기 몫의 독립 확증을 따로 받아야 한다.

**조건 자체의 한계.** P4는 설계 의도였던 "길이 무제약"이 아니라 사실상 **"전 arm
256토큰 균등 절단"** 이 됐고, 절단률이 62.9~94.7%로 arm마다 달라 대칭도 아니다.
1단계 길이 일치 분석에서 65자 절단이 MRR을 0.155 떨어뜨린 것을 이미 봤으므로,
이 조건의 절대 수치는 신뢰 구간 밖의 해석에 쓰지 않는다.

**kanana는 여기서도 빈 캡션이다**(길이 0). 8/9의 `vision_sanity False`가 재현됐다.
0.0276은 모델 성능이 아니라 빈 문자열 임베딩의 우연 수준이므로 **비교표에서 빼고
family 크기에도 넣지 않는다**. 넣으면 보정 임계만 헐거워지는 가짜 비교가 된다.

---

## 4. 프레임 진단 — 판정 불가. 사람에게 넘어갔다

### 4-a. 로컬 v1 (판정자 = 캡션을 만든 3B, 예/아니오)

```
A(실패×정답) 0.519 | B(실패×무작위) 0.038
C(성공×정답) 0.818 | D(성공×무작위) 0.023
A−B +0.481 · C−D +0.795 · A/C 0.634    판정: 혼재
```

계측기 관문(C−D ≥ 0.3)은 통과했다. 사전 등록 두 조건 중 A−B는 넘고
`A ≥ 0.7×C`에서 0.519 < 0.573으로 걸려 **혼재**다. 임계값은 건드리지 않았다.

규칙 밖의 파생 추정으로, C=0.818이 판정자의 누락률 18.2%를 뜻하므로 A를 보정하면
A/C = 0.634다. 실패 52건이 대략 **캡션 갈래 2/3 · M2 갈래 1/3**로 갈린다. 이 1/3은
**캡션 모델을 아무리 바꿔도 손댈 수 없는 실패 질량**이고, 모델 교체 실험의 효과
크기가 계속 작게 나오는 이유를 설명한다.

### 4-b. 서버 v2 (판정자 3종 · 2지선다 · 게이트) — 판정 불가

| 조건 | 판정자 | 양성 대조 | 하드네거티브 | 결과 |
|---|---|---|---|---|
| capped | qwen3vl_4b | 0.977 | **0.000** | 제외 |
| capped | varco_1_7b | 0.923 | **0.277** | 제외 |
| capped | **CLIP** | 0.955 | **0.519** | **통과** |
| full | qwen3vl_4b | 0.955 | **0.000** | 제외 |
| full | varco_1_7b | 0.783 | **0.130** | 제외 |
| full | **CLIP** | 0.977 | **0.462** | **통과** |

**생성형 판정자 2개가 위치 편향으로 탈락했다.** 하드네거티브는 정답이 아예 없는
문항이라 우연 수준(0.35~0.65)이어야 하는데, qwen3vl은 0.000 — 파싱된 답이 **전부
"2"** 였다. 모르면 뒤쪽 사진을 찍는다는 뜻이다. 양성 대조가 0.977인 것이 중요하다:
답할 수 있는 문항에서는 내용을 제대로 읽고, **답할 수 없을 때만** 기본값을 낸다.
하필 진단이 알고 싶은 것이 그 애매한 경계다.

**CLIP은 두 조건 모두 0.5에 붙어 통과했다**(0.519 / 0.462). CLIP은 유사도를 직접
비교하므로 "모르겠으면 2번"이라 답할 언어 출력 자체가 없다. 같은 문항에서
생성형만 쏠렸다는 것이 위치 편향 진단을 확정한다. **게이트가 설계 목적대로
작동했다** — 이 게이트가 없었으면 v1의 "캡션 2/3"을 그대로 믿고 GPU를 캡션 모델에
계속 썼을 것이다.

사전 등록 규칙(통과 판정자 2개 미만 → 판정 불가)에 따라 **두 조건 모두 판정
불가**다. CLIP 단독 결과는 규칙상 쓰지 않았다.

### 4-c. 사람 키트를 새로 만들었다

판정자 스크립트의 `--human-kit`은 **모델이 갈린 항목**만 내보내는데, 판정 자체가
성립하지 않아 **0건**이 나왔다. 판정문은 "사람 확인으로 넘긴다"인데 넘길 물건이
없는 상태였다. `docs/probes/frame_human_kit.py`로 별도 생성했다.

- **76문항** — 실패 52(A) + 양성 대조 12(C) + 하드네거티브 12(N), 순서 무작위
- 모델과 **같은 2지선다** 형식이라 사람·모델 결과가 같은 축에서 비교된다
- **사람에게도 같은 임계값의 관문**을 건다(C ≥ 0.75, N 쏠림 0.35~0.65). 못 넘기면
  그 사람 답은 전량 제외
- 정답 위치는 조건별 **균형 배정**(A 26:26, C 6:6). 초안에서 C가 9:3으로 치우쳐
  "무조건 1번" 응답자가 관문을 통과할 수 있었다
- 원본 해상도. 축소로 잃는 부분은 두 조건 모두 관문을 통과한 CLIP이 따로 답한다
- 검색 결과·캡션·자막 미포함(절대규칙 3), 정답표는 `_keymap.json`에만
- 위치: `docs/probes/_scratch/frame_human_kit_full/` (49.6MB, gitignore 대상)
- 소요 예상 30분 안팎

### 4-d. 사람 채점 결과 — **(가) 확정. M2는 병목이 아니다**

채점 스크립트는 답을 보기 전에 작성·커밋했다(`frame_human_score.py`).

| 조건 | n | 정답률 | 0응답 | 비영 중 '1' |
|---|---|---|---|---|
| A 실패 질의 × 정답 프레임 | 52 | **0.8077** | 0.1923 | 0.524 |
| C 성공 질의 × 정답 프레임 | 12 | 0.8333 | 0.1667 | 0.400 |
| N 하드네거티브 | 12 | **1.000** | 1.000 | — |

**관문 통과.** C 0.8333 ≥ 0.75, 그리고 **N 12건 전부 `0`** — 정답이 없는 문항에서
억지로 고르지 않았다. 생성형 판정자 2개가 전멸한 바로 그 문항이다.

**보정 A/C = 0.9692, CI95 [0.755, 1.385].** 임계 0.6 아래로 내려갈 확률 0.006%,
(나) 판정이 될 확률 0%. C의 n=12 불확실성을 넣어도 결론이 안 바뀐다.

**판정: (가) 프레임에는 있는데 캡션이 안 썼다.**

모델 v1과의 대조가 진단의 핵심이다.

| | 모델 v1 (3B) | 사람 |
|---|---|---|
| C 성공 질의 | 0.818 | 0.833 |
| **A 실패 질의** | **0.519** | **0.808** |
| 보정 A/C | 0.635 (혼재) | 0.969 **(가)** |

성공 질의에서는 3B와 사람이 거의 같은데, **실패 질의에서만 3B가 크게 덜 본다.**
v1 docstring에 미리 적어둔 편향 방향("작은 판정 모델은 (가)를 과소평가한다")이
그대로 실측됐다. 판정자 게이트와 사람 확인이 없었으면 v1의 "혼재"를 믿고
M2·max_pixels에 GPU를 썼을 것이다.

**M2 프레임 선택·max_pixels는 우선순위에서 내린다.** 실패 52건 중 프레임에 내용이
없는 몫은 A의 0응답 19.2%인데 성공 질의에서도 16.7%가 0이므로, 순수 프레임 문제는
그 차이인 **약 2.5%p**다.

---

## 5. 승인이 필요한 것

### 5-a. test 재평가 — **지금은 하지 말 것을 권한다**

확증을 통과했으니 절차상으로는 재평가 조건이 열렸다. 하지만 **§2 때문에 지금
돌리면 접촉 8회차를 쓰고 아무것도 못 얻는다**. 채택해도 dev 변화가 −0.0013이므로
test에서도 변화가 없을 것이고, "변화 없음"을 확인하려고 남은 접촉 횟수를 쓰는 것은
낭비다. **환경 효과를 해결한 뒤에 재평가하면 그때는 +0.09를 확인하는 의미 있는
접촉이 된다.**

### 5-b. 환경 효과 원인 규명 — **이걸 먼저 하자고 제안한다**

지금 가장 큰 지렛대다(−0.0926, p=0.0085). 같은 모델·양자화·프롬프트·그리디인데
노트북과 서버 출력이 25.6%만 일치하는 원인을 찾는다. 후보: 라이브러리 버전,
attention 커널(sdpa/flash), 이미지 디코드·리사이즈 경로, max_pixels 반올림,
GPU 리덕션 비결정성. **승인 주면 노트북·서버 양쪽 환경을 고정 입력으로 대조하는
프로브를 짠다**(GPU 소량, test 미접촉).

### 5-c. 사람 확인 키트 실행 여부

76문항 30분. 실행하면 §4의 캡션/M2 갈래가 확정되고, M2·max_pixels에 GPU를 쓸지가
결정된다. 안 하면 v1의 "혼재"가 그대로 남는다. **누가 할지도 정해야 한다** —
2인 라벨링 키트(`label_kit/`, 39건)의 2번째 라벨러 배정과 같이 결정하면 된다.

### 5-d. dev 확장

새로 인덱싱한 3편(jissi_farm, softyeon_ceramics, baekmansonghee_jirisan)을 dev에
넣을지. 넣으면 dev 96 → 더 커져 검출 한계(현재 MDE ±0.086)가 내려간다. §3의
순위 역전을 가르려면 결국 필요하다.

---

## 6. 다음 우선순위 (실패 분해 기준)

§4-d에서 갈래가 **(가) 캡션**으로 확정됐고, §2의 환경 효과가 그 캡션 갈래를 막고
있다. M2 갈래는 닫혔다.

1. **생성 환경 효과 규명** — 확정 0.09. 캡션 갈래가 확정된 지금, 이걸 못 풀면
   캡션 모델 교체(+0.09)가 영원히 순변화 0으로 남는다. **최우선.**
2. **임베딩 모델 스윕** — 변별 실패 12%를 겨냥. 캡션 텍스트가 고정이라
   **환경 교란이 없는 유일한 지렛대**다. AI Hub 절반 선택·절반 확증으로 진행 중.
3. **dev 확장 3편** — 검출 한계 ±0.086을 낮춘다. P4의 순위 역전을 가르려면 필요.
4. **M9 판정자 교체** — coverage가 게이트 탈락했다(§8). 리포트 지표 자체가 흔들린다.
5. ~~M2 대표 프레임 선택 규칙~~ — §4-d로 **닫힘**. 순수 프레임 문제는 약 2.5%p.
6. ~~max_pixels~~ — 같은 이유로 우선순위 밖.

## 8. M9 판정자 게이트 — coverage 탈락

새 생성 없이 8/6 리포트와 segments만 재배열해 답을 아는 문항을 만들었다
(`judge_gate_probe.py`, 프로토콜 결과 전 커밋).

| 과제 | 양성 정답률 | 음성 정답률 | 예비율 | 판정 |
|---|---|---|---|---|
| groundedness | 0.817 | 1.000 | 0.408 | **통과** |
| coverage | **0.550** | 0.983 | 0.283 | **탈락** |

groundedness는 쓸 수 있다. coverage는 리포트가 실제로 인용한 세그먼트의 45%를
"커버 안 됐다"고 답한다 — 음성이 0.983이므로 아무거나 covered라 하는 게 아니라
**덜 잡는 방향**이다.

**단서.** 양성 대조가 "리포트가 그 세그먼트를 `cites`에 넣었는가"라 "내용이 담겼는가"의
대리 지표다. M8이 인용을 느슨하게 달았다면 양성 대조 자체가 오염된 것이다. 현재
설계로는 "판정자가 덜 잡는다"와 "M8이 느슨하게 인용한다"를 못 가른다. 다만 어느
쪽이든 coverage 해석에 문제가 된다는 결론은 같다.

**영향.** M9는 이미 test에서 두 번 돌았다(공식 평가 7회 중 리포트 2회). 그 coverage
수치는 하한이고, 하한의 폭이 설계 가정보다 크다(코드 주석의 합성 검증셋 재현 0.80
vs 실측 0.550). **재평가와 무관하게 문서에 한계를 명시해야 한다.**

---

## 7. 밤사이 고친 것

- **감시자 오탐 2회.** ① `caption_sweep.json` mtime 기반 정체 감지는 arm 하나가
  90분을 넘으면 무조건 오탐이다(파일은 arm 종료 시에만 쓰인다). ② 교체한
  감시자도 `pgrep -f`가 **파일명이 명령줄에 들어간 `bash -c` 래퍼**를 집어
  CPU 0으로 읽었다 — 8/9 21:01 데드락과 같은 부류의 실수를 반복했다.
  `/proc/PID/comm == python3` 확인 후 CPU 틱 증가와 GPU 사용률을 함께 보도록 고쳤다.
- **쌍 정렬 오류.** `deploy_delta` 초안에서 sweep의 `rr_caption_only`(파일 순서)를
  알파벳 정렬 키로 맞춰 CI·p가 틀렸다(평균은 순서 무관이라 맞았다). 파일 순서로
  고쳤고, 스크립트 docstring에 함정으로 적어 뒀다.
- **사람 키트 위치 불균형**(C 9:3 → 6:6). §4-c.

---

# 오후 추가 (16:30 기준)

## 9. 임베더 1단계 — 선택 없음, 현행 유지

AI Hub A 절반(97영상·562질의). 모델별 α 재탐색, BH-FDR q=0.05.

| 모델 | α* | 융합 MRR | Δ vs KURE | CI95 | 순열 p | BH |
|---|---|---|---|---|---|---|
| nlpai-lab/KURE-v1 (현행) | 0.2 | 0.4888 | — | — | — | — |
| BAAI/bge-m3 | 0.0 | 0.5024 | +0.0135 | [−0.005, +0.032] | 0.156 | 탈락 |
| dragonkue/BGE-m3-ko | 0.2 | 0.4940 | +0.0052 | [−0.010, +0.020] | 0.490 | 탈락 |
| upskyy/bge-m3-korean | 0.0 | 0.4909 | +0.0021 | [−0.021, +0.025] | 0.856 | 탈락 |
| Alibaba-NLP/gte-multilingual-base | — | 실패 | — | — | — | `trust_remote_code` 미전달 |

**BH-FDR 통과 0개.** 사전 등록대로 2단계(B 절반)를 돌리지 않고 확증 표본을 아꼈다.

α\*가 전부 0.0~0.2다 — AI Hub가 캡션 채널을 강하게 선호한다(Q18의 단조 감소 α 곡선과
같은 방향).

**연기분**(제외 아님): 접두어 필수 모델(e5·KoE5·Qwen3-Embedding·arctic)은
`m5_search.search`가 질의를 내부에서 임베딩하고 `expand_query` 변형을 거치므로 접두어를
안전하게 끼우려면 검색 경로를 손봐야 한다. 접두어 없이 돌리면 부당하게 진다(규약 3항).

## 10. 환경 2단계(커널) — 커널로는 못 고친다

| 조건 | 노트북과 완전일치 |
|---|---|
| `sdpa` (서버 기본값) | 27.5% (33/120) |
| `eager` | 13.3% (16/120) |
| `sdpa+math` | **30.8%** (37/120) |

최고가 +3.3%p로 사전 등록 임계 +20%p에 한참 못 미친다. **어떤 커널로도 서버가 노트북
출력에 붙지 않는다.** 스크립트가 찍은 "부분 기여"는 규칙 빈틈 때문이다 — eager가
−14.2%p로 *나빠져서* "전 조건 ±10%p 안" 분기를 못 탔다. 의도대로 읽으면 "커널이 아니다"다.

부수 확인: 커널을 바꾸면 캡션의 14%가 뒤집힌다. **생성이 미세 수치차에 민감하다는 것이
실측됐다.** 남은 요인은 라이브러리 버전과 GPU 아키텍처(sm_86/sm_89)인데, 서버 torch를
바꾸면 지금까지 잰 23 arm과 비교가 불가능해지므로 건드리지 않는다.

## 11. 후보 모델을 노트북에 올릴 수 있다 — 단, 양자화가 공짜가 아니다

후보 이름의 `4b`는 **파라미터 40억**이지 4bit가 아니다. 지금까지 bf16 11.4GB로만 재서
6GB 노트북에 못 올린다고 판단했는데, **4bit로는 올라간다**(최대 3.27GB / 6.44GB,
적재 18.3초). 노트북에서 생성하면 환경이 안 바뀌므로 −0.0926 손실 자체가 발생하지 않는다.

그런데 서버에서 같은 환경으로 잰 4bit 대조군이 문제를 드러냈다.

| 대비 | Δ | CI95 | 순열 p |
|---|---|---|---|
| **양자화 효과** (4bit − bf16, 환경 동일) | **−0.0604** | [−0.1255, +0.0031] | 0.068 |
| **4bit에서의 모델 효과** (qwen3vl4bit − 3B4bit) | **+0.0309** | [−0.056, +0.117] | 0.487 |
| bf16에서의 모델 효과 (참고) | +0.0913 | [+0.007, +0.178] | 0.041 |

2026-08-07에 Qwen2.5-VL-3B로 잰 양자화 비유의(Δ+0.0024)를 근거로 "4bit는 공짜"라고
말했는데, **Qwen3-VL-4B에서는 점추정 −0.0604다.** 유의하지 않지만 "공짜"라고 할 근거도
없다(dev 96으로는 0.06을 가릴 수 없다). 그 결과 후보의 우위가 +0.0913 → +0.0309로
줄고 비유의가 된다.

## 12. 신규 모델 스모크 — 5개 중 1개만 유효

본 배치 전 2프레임으로 API를 실증했다(kanana 전례).

| 모델 | 결과 | 사유 |
|---|---|---|
| **Qwen3-VL-8B** | **통과** | 한국어 캡션 정상, 18.2GB |
| GLM-4.1V-9B-Thinking | 형식상 통과, **실질 제외** | 출력이 **영어 `<think>` 추론 트레이스** |
| MiniCPM-V-4.5 | 제외 | `MiniCPMV has no attribute 'all_tied_weights_keys'` |
| HyperCLOVAX-SEED-Omni-8B | 제외 | `Unrecognized configuration class` |
| HyperCLOVAX-SEED-Vision-3B | 제외 | `HCXVisionConfig has no attribute 'text_config'` |

세 모델의 실패는 전부 **벤더 원격 코드와 transformers 버전 불일치**다. 고치려면 버전을
바꿔야 하는데 그러면 기존 23 arm과 비교가 불가능해지므로 하지 않는다. **한국어 특화
후보(HyperCLOVAX)가 통째로 빠진 것은 실질적 손실이므로 결과에 명시한다.**

**게이트 설계 누락도 기록한다.** 통과 기준을 "비어있지 않음 + 프레임 간 상이"로만 잡아
**언어·형식을 안 봤다.** GLM이 그 틈으로 통과했다. 다음 판에는 한국어 비율과 형식
검사를 넣어야 한다.

## 13. 오늘 낸 실수 (재발 방지용)

- **파일 미동기화.** `caption_model_sweep.py`에 8B를 추가·커밋하고 **서버 scp를
  빠뜨려** 6개 arm이 `KeyError`로 즉사했다. GPU 시간은 안 날렸지만 배치가 아무것도
  안 했다. 스크립트를 고치면 **커밋과 동기화를 한 동작으로 묶어야 한다.**
- **실행 순서.** AI Hub 2단계가 "dev 상위 3개"를 뽑는데 8B가 dev에 없는 상태로
  돌기 시작했다. 옛 순위로 몇 시간을 쓸 뻔했다. 정지 후 8B dev → 2단계로 바로잡았다.
- **추정 근거 부실.** 노트북 프레임당 시간을 **한 영상의 앞 10장**으로 재서 3.84초로
  추정했는데 실측은 17초대다(4.5배). 대부분의 프레임이 128토큰 상한까지 생성한다.
  표본이 대표성이 없으면 추정을 내지 말아야 한다.
- **`sec_per_frame` 지표 오류.** 스모크 테스트가 모델 적재 시간을 2프레임에 나눠
  넣어 프레임당 시간을 크게 부풀린다. 배치 소요 추정에 쓰면 안 된다.
