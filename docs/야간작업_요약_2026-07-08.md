# 야간 작업 요약 (2026-07-08 아침 검토용)

서버는 새 UI로 켜져 있습니다 → http://127.0.0.1:7860 (세부 진행률·재접속 복원·채팅 초기화 반영됨).

## 1. 완료·머지된 것

- **웹 UI 개선** (master 머지, `08c0b78`): 단계 내 세부 진행률("M2… 43/314"), 브라우저 재접속 시 진행 중 작업 복원(`/api/current`), 새 영상 준비 시 채팅 초기화. 65개 테스트 통과.
- **서바이벌 영상(26분) 인덱싱 완료**: 314 세그먼트, 자막 커버리지 97.8%, 캡션 성공 314/314. 총 소요 약 4시간 — **M2(프레임 선택)가 지배적**(고해상도 CPU 디코딩, 세그당 ~10초). M3는 예상보다 빨랐음.

## 2. 검토 후 결정 필요 (우선순위순)

### ① M8·M9 (AAR 리포트 생성·평가) — 머지 결정
`feature/aar-m8-m9` 브랜치에 구현 완료. 프롬프트 사전 검증(Fable) → TDD 구현 → 태스크 리뷰 2회(무지적) → 최종 브랜치 리뷰(opus) → 지적 3건 수정·재승인. 전체 77개 테스트, 경고 0.
- 구성: `src/llm.py`(지연 로딩+4bit), `src/m8_report.py`(인용 강제+few-shot, map-reduce, reduce⊆map 검사, raw 항상 보존), `src/m9_report_eval.py`(G-Eval 3단계 CoT judge, 보수 판정, same-model 가드+스팟체크)
- 실제 GPU 실행은 여전히 judge_model 확정 후. **머지만 결정하면 됨** — `git merge feature/aar-m8-m9`

### ② 질의 초안 21개 — 라벨 확인
`data/queries/DRAFT_REVIEW.md` (표 형식) + `data/queries/queries_draft.jsonl`
- 스파이더맨 9개(test) + 서바이벌 12개(dev), 유형 균형(자막/장면/복합)
- 확신도 낮음 1개(wl_q03: 갤럭시 폰 대사가 엔딩에서 재언급 — 경합), 중간 5개
- 확인 후 `data/queries/queries.jsonl`로 확정하면 정식 M6 실행 가능

### ③ static_threshold — 보정값 결정
실측: **현행 0.05는 사실상 발동 안 함** (스파이더맨 0/31, 서바이벌 4/314=1.3%)

| threshold | 스파이더맨 | 서바이벌 |
|---|---|---|
| 0.05 (현행) | 0% | 1.3% |
| 0.08 | — | 5.7% |
| 0.10 | 25.8% | 9.9% |

권장: ablation 계획(아래)대로 절대값 대신 **영상별 분위수 후보**로 스윕. motion_score가 기록돼 있어 **GPU 재실행 없이** 가능.

### ④ 문서 초안 2건 — 방향 확인
- `docs/presentation/중간발표_골격_draft.md`: 슬라이드 14+백업 2, 예상 질문 5개 답변 포함, 미확정 수치 자리표시자 4곳
- `docs/ablation_plan_draft.md`: 실험 우선순위 **threshold(CPU-only) → 캡션 프롬프트 → 세그먼트 길이(비용 큼)**

## 3. 예비 평가 결과 (⚠️ 초안 라벨·n 작음 — 참고용, 보고서 인용 금지)

dev(서바이벌 12) grid search → test(스파이더맨 9) 평가:

| | best α (dev) | test hit@1 | test hit@5 | test MRR |
|---|---|---|---|---|
| **KURE** baseline(α=1.0) | — | 0.556 | 0.667 | 0.602 |
| **KURE** proposed(α=0.5) | 0.5 | **0.778** | **1.000** | **0.889** |
| **BGE-M3** baseline | — | 0.444 | 0.667 | 0.552 |
| **BGE-M3** proposed(α=0.5) | 0.5 | 0.667 | 1.000 | 0.800 |

- **두 모델 모두 자막+캡션 결합(proposed)이 baseline을 명확히 상회** — 연구 가설과 부합하는 첫 신호
- 두 모델 모두 α*=0.5. KURE가 test에서 근소 우위 → 현행 KURE 유지가 무난하다는 신호
- BGE-M3 비교 인프라: `config_bge.yaml` + `work_bge/` (재사용 가능)

## 4. 남은 것 / 알아둘 것

- 서버 재시작 후에는 `/api/current`가 비워짐(잡 이력은 서버 수명 내) — 같은 파일 재업로드로 확인 가능
- M8 실행 대기 조건: judge_model 확정(튜터 협의) + `human_check_n` 상향 검토(가이드는 최소 50 권장, 현행 20)
- 문서 드리프트(경미): DESIGN_SPEC 3-5에 `map_raw_outputs`, §6에 `map_chunk_overlap` 미기재 — 코드가 상위호환이라 계약 위반은 아님
- 미커밋 파일: 질의 초안 2, 발표 골격, ablation 계획, 이 요약, config_bge.yaml (전부 초안이라 의도적으로 미커밋)

## 5. 원장 위치

세부 이력: `.superpowers/sdd/progress.md`, 리뷰·수정 근거: `.superpowers/sdd/*-report.md`, `m8m9-prompt-critique.md`
