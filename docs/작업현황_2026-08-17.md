# 작업현황 — 2026-08-17

**직전 스냅샷**: `docs/archive/작업현황_2026-08-14.md`
이 문서는 08-15~17 사흘을 덮는다.

---

## 0. 한 문장

캡션 모델을 바꿔 인덱스 10편을 재생성했고, **8회차 개방 게이트에서 하드 게이트 2건이
실패해 test를 열지 않았다.** 게이트는 결과를 보기 전에 커밋한 것이고 임계는 손대지 않았다.

---

## 1. 8회차 개방 게이트 — **HOLD**

수치 전문은 `docs/판정자료_2026-08-17.md`. 여기는 판정만.

| | 기준 | 결과 | |
|---|---|---|---|
| **P0** 자막 해시 불변 | 10/10 | **10/10** | PASS |
| **I1** 오염 캡션 | 0건 | **5건** | **FAIL** |
| I2 `text_hash` | 10/10 | 10/10 | PASS |
| I3 emb shape | 10/10 | 10/10 | PASS |
| I4 전량 공백 영상 | 0편 | 0편 | PASS |
| **A1** tie_set 크기 | ≥2 | **4** `[0.2,0.3,0.4,0.5]` | PASS |
| **A2** 최대 이웃 하락 | ≤0.06 | **0.0665** | **FAIL** |
| T1 오배제 | 0/96 | 0/96 | PASS |
| T2 무관 감지 | ≥11/20 | 12/20 | PASS |
| T3 여유 | 기록 의무 | 0.0082 | 기록 |

### 1-1. P0가 통과했다 — 두 경로로 확인됐다

이번 배치의 1차 시도에 `--force`가 잘못 들어가 있었다. 그대로 돌았으면 Whisper가
재실행돼 자막까지 바뀌었을 것이고, 그러면 캡션 모델 효과와 STT 재생성 효과가 섞여
8회차 전체가 해석 불가가 된다.

- **직접 확인**: 재캡셔닝 전 백업과 필드별 SHA256 대조 — 10/10 불변
- **교차 확인**: **α=1.0(자막 단독) MRR이 0.4144로 직전과 완전히 동일**

두 번째가 더 강하다. 자막이 한 세그먼트라도 바뀌었으면 이 값이 움직인다.

### 1-2. I1 실패 — 검출기가 더 나은 모델을 벌한다

오염 5건 중 **3건은 진짜 언어 이탈**(`的小径` / `ケース` / `一只手`)이고,
**2건은 캡션이 맞다.**

| 영상 | seg | 내용 |
|---|---|---|
| `itsub` | 138 | `'佰分购旗舰店'`에서 판매 중인 `小米`REDMI — **화면 속 중국 쇼핑몰 문자** |
| `kheritage` | 17 | 비석에 `'山郭氏世居'`라 새겨져 있고 — **비석에 실제 각자된 한자** |

`common.is_corrupted_caption`은 CJK 혼입을 오염으로 보는 휴리스틱이다. **화면 속 문자를
읽어내는 능력이 좋아질수록 더 많이 걸린다.** 같은 영상이 현행 3B 인덱스에서는 오염
0건이었는데, 3B가 저 화면들을 못 읽어서 통과한 것이다.

**임계를 낮출 사유로 쓰지 않는다.** 규명 결과로만 기록한다.

### 1-3. A2 실패 — 0.0065 차이

α\*=0.5(0.6926)의 이웃 하락이 0.4쪽 0.0129 / **0.6쪽 0.0665**. 기준 0.06을 넘는다.

**α\*는 0.5에서 움직이지 않았다.** 움직인 것은 점추정 최적(0.4→0.5)과 tie_set 크기(3→4).
단봉성도 유지된다(국소 최적점 1개). 문제는 위치가 아니라 **봉우리 오른쪽 어깨의
기울기**다 — 0.5→0.6 하강이 0.0504에서 0.0665로 가팔라졌다.

### 1-4. 더 근본적인 문제 — 캡션 단독이 안 좋아졌다

**α=0.0(캡션 단독) 0.5535 → 0.5466, Δ −0.0069.**

8회차를 여는 명분이 "캡션 채널 개선"이었는데 배포 인덱스 기준 dev에서 재현되지 않는다.
08-12의 "노트북 저장분 대비 +0.006"과 크기가 같고 부호만 다르다 — **둘 다 0 주변 잡음**
으로 보는 것이 맞다.

외부 표본의 +0.038 CI[0.013, 0.061]과는 다른 표본이라 모순은 아니다. 다만 **dev에서
재현되지 않았다**는 사실은 그대로 남긴다(절대규칙: 재현은 다른 표본에서만 인정하되,
비재현도 같은 무게로 적는다).

부수 관측: **캡션 평균 길이가 전편에서 약 30% 줄었다**(예 121.8→99.2자).

---

## 2. 배치 실행 결과

`phase1_run.sh` — 14:07 시작, 15:43 완료. 백업 `/ssd/<SERVER_USER>/backup_pre_8th_1786943224`.

| 단계 | 내용 | 소요 |
|---|---|---|
| C1 보강 | `pland_costco_hosting` | 6분 |
| 0 | 재캡셔닝 전 `segments.json` 10편 백업 | — |
| 1 | 캡션 재생성 10편 (Qwen3-VL-4B bf16 / P1) | **94분** |
| 2 | m4 재임베딩 10편 | 2분 |
| 2 검증 | 오염 스캔 + `text_hash` 대조 | 1초 |
| 3 | dev α 재탐색 | 26초 |
| 4 | τ 재캘리브레이션 | 14초 |

**5단계(test 접촉)는 실행하지 않았다.** 스크립트가 4단계에서 의도적으로 멈춘다.

τ는 0.55 → **0.56**으로 이동했다. 이 이동이 T2를 살렸다 — τ=0.55에 머물렀다면
무관 감지가 7/20으로 **T2 기준(≥11) 미달**이었다.

---

## 3. 사고 3건 — 전부 실행 절차, 연구 결과 오염 없음

| 사고 | 원인 | 손실 |
|---|---|---|
| 유령 프로세스 대기 | 대기 조건 `pgrep -f`가 **자기 런처 셸**까지 매칭 | GPU 8.5시간 유휴 |
| 배타 플래그 | `--captions-only --force` 동시 지정 | 배치 즉시 종료 |
| 편집본 ≠ 실행본 | `/tmp`가 git-bash와 Windows 파이썬에서 다르게 해석됨 | C1 보강 단계 누락 |

세 번째가 가장 위험하다 — **"고쳤다고 생각한 코드"와 "서버가 실행한 코드"가 달랐다.**

**같은 실수를 오늘 한 번 더 했다.** 배치 완료 대기 스크립트가 거짓 실패를 보고했다.
원인 둘:

1. `grep -c ... || echo 0` — `grep -c`는 0건일 때 "0"을 찍고 **exit 1**을 낸다.
   `||`가 한 번 더 찍어 출력 줄이 밀렸고, **줄 위치로 파싱**해서 여분의 `0`을
   프로세스 수로 읽었다 → 라벨 파싱으로 교체
2. 단발 관측으로 실패 선언 — 배치는 `for` 루프라 **영상 사이 순간엔 python이 없다**
   → 프로세스 부재 + 로그 정체가 **연속 3회**여야 실패로 센다

세 번째 결함도 같이 고쳤다: `pgrep` 패턴을 `^/ssd/.../python`으로 앵커해 자기매칭을
막았다(앵커 전 count=2, 후 count=1).

**교훈은 같다.** 즉석 shell 파이프라인에서 exit code 의미와 프로세스 매칭을 반복해서
틀린다. 연구 규율은 주의사항이 아니라 **코드 검사**로 옮겨야 한다 — P0가 그 예다.

---

## 4. 코드·문서

### 4-1. 커밋

| SHA | 내용 |
|---|---|
| `ed768df` | M8 `span`/`evidence_segments` 분리, 코드 검증자 4개 → **9개** |
| `841aeea` | docs 27개를 4개 하위폴더로 재배치 + `docs/README.md` 색인 신설 |
| `0c23445` | 판정자료 §4를 현재 코드에 동기화 |
| `c4da9b9` | 튜터 회의 자료 2026-08-18 |

테스트 **246건 통과**.

### 4-2. M8 구조 변경 — 8회차에는 넣지 않는다

`span`(사건의 시간 범위)과 `evidence_segments`(대표 근거)를 분리했다. 이전에는 한
필드라 "사건을 길게 잡는 것"과 "번호를 많이 다는 것"이 같은 뜻이었고, 모델이 후자로
커버를 올렸다(사건 9개가 149구간 인용, 인용당 6.2자 — 사전등록 G3 미달).

검증자 9개: `empty_event` / `no_segments` / `bad_span` / `seg_out_of_range` /
`too_many_evidence` / `duplicate_evidence` / `evidence_outside_span` /
`thin_description` / `foreign_language`. 상한 4와 하한 15자는 코드 상수를 프롬프트에
f-string 주입해 **프롬프트 숫자와 검증 숫자가 갈라질 수 없게** 했다.

병합도 "서술 문자열 완전 일치"(예비 실행 병합 0건) → **사건명 동일 + span 인접**으로
바꿨다.

**아직 재실행하지 않았다.** 8회차에는 넣지 않기로 이미 결정돼 있다.

### 4-3. 문서 재배치

`docs/` 루트 27개 → 6개. `preregistration/`(4) · `tutor/`(4) · `planning/`(3) ·
`archive/`(11). 색인은 `docs/README.md`.

**사전등록 4종은 `git mv`만 하고 내용은 한 글자도 안 고쳤다.** 그 문서들의 증거력은
결과를 보기 전에 커밋됐다는 이력에서 나온다 — 결과가 나온 뒤의 수정 기록이 링크
정확성보다 비싸다. 그래서 남는 낡은 경로 2건은 `docs/README.md`에 명시했다.

### 4-4. 로컬 ↔ 서버 대조

파일 69개를 개행 정규화 후 SHA256 대조: 동일 54 / 다름 15.

**실행에 영향 있는 차이는 `src/m8_report.py` 하나**(142줄, `ed768df` 미배포).
나머지는 주석·문서 경로 문자열, 그리고 로컬이 앞선 테스트 파일 8개다.
`config.yaml`은 주석 1줄만 다르고 실효값은 동일하다.

서버 HEAD는 `423889b`로 **120 커밋 뒤**다. 파일이 개별 전송돼 있어 서버
`git status --porcelain`이 16개 M을 뱉는다 — 어느 버전이 돌았는지 애매해지는 상태다.

---

## 5. 다음에 결정할 것

**5-1. I1 처리.** `m3 --recaption-corrupted`(자동 판정분만 재생성)는 배포 인덱스도 거친
정규 절차라 임계를 낮추는 게 아니다. 다만 **화면에 실제 한자가 있는 2건은 재생성해도
같은 결과가 나올 가능성이 높다** — 그러면 I1=0은 이 영상들에서 원리적으로 도달 불가다.
그 경우 선택지는 (a) 검출기를 고친다 (b) I1의 정의를 바꾼다 — **둘 다 사전등록 수정이라
결과를 본 뒤엔 할 수 없다.**

**5-2. A2 처리.** 0.0065 차이지만 임계 수정은 금지다. α=0.6 쪽 어깨가 왜 가팔라졌는지
규명이 먼저다.

**5-3. 8회차를 열 명분 자체.** §1-4대로 캡션 단독 개선이 dev에서 재현되지 않았다.
게이트 2건이 통과했더라도 이 사실만으로 재검토 대상이다.

**5-4. 깃 푸시.** 미푸시 **125**. 이력 재작성 필요(백업 브랜치 `backup-pre-scrub` 존재).

---

## 6. 주의 (유지)

- test는 열지 않았다. 접촉 이력 **공식 평가 7회 / 튜닝 0회** 그대로다
- 백업 `/ssd/<SERVER_USER>/backup_pre_8th_1786943224` — 자막·캡션 전 필드 보존
- 서버 정식 실험은 git 등록 스크립트로만. 장시간 배치는 계획 → 소규모 시험 → 본 실행
- `src/m8_report.py` 서버 배포는 M8 재실행 전에, 승인 후
