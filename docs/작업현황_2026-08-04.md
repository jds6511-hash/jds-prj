# 작업현황 2026-08-04 — STT 교체 검토(회의록·화자별 요약 기능) 진행 중

재시작 시 이 문서부터. 이전 스냅샷: `docs/작업현황_2026-07-14.md`.

## 판단 원칙 (2026-08-05 사용자 명시)

**속도보다 정확성이 최우선.** 모델 채택 판단에서 추론 속도는 기각 사유가 아니다.
CLAUDE.md "판단 기준" 절에 규칙으로 등재했다. 이 원칙에 따라 아래 §1의 arm C
속도 우려는 교체 판단에서 빼고 읽는다.

## 0. 오늘 완료된 것 (커밋됨)

- 방법론 선행연구 6편 반영(Smucker/Card/Dwork/Panickssery/Artstein/Ribeiro) — 커밋 `ddf9c0b`
- 문서-코드 동기 감사 8건 교정(§6 키 4개·값 2개, `expand_query` 미등재, 시그니처 5건)
- 신규 영상 3편 인덱싱 완료(586 세그먼트) — `평가확장_계획.md` 3-1·3-2
- 발표덱 `진행상황발표_2026-07-31.pptx`(8장), pptx는 추적 해제(`*.pptx` gitignore)

## 1. 지금 하는 일 — STT 3종 비교

배경: 2026-07-31 회의에서 **회의록·화자별 요약** 기능 추가 요구.
사용자 결정: 3종 비교해 **유의미한 향상 있으면 전면교체**.

arm: A 현행 `large-v3` / B `ghost613/faster-whisper-large-v3-turbo-korean` /
C `Qwen/Qwen3-ASR-1.7B-hf`. 스크립트: `docs/probes/stt_bench.py`(모든 함정·우회가
docstring에 기록돼 있음). 결과: `docs/probes/_scratch/stt_<dataset>_*.json`.

### 실행 상태

- **Phase 1a (zeroth)**: 완료했으나 **무효**. arm B가 Zeroth로 파인튜닝됐고 자체
  val/test를 원본 test 50/50 분할로 만들었다고 모델카드에 명시 → arm B 누수.
  (측정 CER 0.0189 vs 모델카드 보고 2.06% 일치로 확인). 누수 사례 기록으로만 보존.
- **Phase 1b (fleurs)**: **완료**. `google/fleurs:ko_kr` test 200발화, arm B 학습셋과 무관.
  결과: `docs/probes/_scratch/stt_fleurs_compare.json`.

  | arm | 모델 | cer_numfree | cer_raw | wer_raw | 발화당 |
  |---|---|---|---|---|---|
  | A | large-v3 (현행) | 0.0497 | 0.0508 | 0.1337 | 0.98초 |
  | B | ghost613 ko-turbo | 0.0831 | 0.1004 | 0.2671 | 7.56초 |
  | C | Qwen3-ASR-1.7B-hf | 0.0445 | 0.0478 | 0.1193 | 2.11초 |

  - **arm B 유의하게 악화**: delta +0.0334, CI [0.0264, 0.0406]. Zeroth에서 "압승"으로
    보였던 것이 완전히 역전 → **누수였음이 확정**됐다.
  - **arm C 유의하지만 미미**: delta −0.0052, CI [−0.0100, −0.0009](numfree). `cer_raw`
    기준으로는 **비유의**(CI [−0.0082, 0.0015]). 중앙값은 A 0.0196 vs C 0.0182로 동급.
  - **arm C 속도: A 대비 2.15배**(0.98초 vs 2.11초/발화). 실시간의 0.17배라 42분 회의에
    약 7분. 실사용 지장 없다.
    *정정(2026-08-05)*: 최초 기록은 89.97초/발화(92배)였는데 **재현되지 않았다**.
    동일 표본·동일 코드로 재실행하니 17994.3초→421.4초(43배 단축)이고 **전사문은
    200/200 완전 동일**하다 — 정확도 결론은 그대로, 시간 수치만 오측정이었다.
    원인은 함정 5(Windows VRAM spill) 참조. 애초에 속도는 기각 사유가 아니지만
    (위 판단 원칙), 잘못된 수치를 근거로 남기지 않기 위해 정정한다.
  - 품질 차이가 드러난 지점(회의록 용도 직결): 고유명사에서 C가 우세
    (C `키르치네르`·`가문비나무`·`국립 공원` vs A `히르치네르`·`가뭄비나무`·`공립공원`).
    또 **arm A가 `"자막 제공 배달의민족"` 환각을 생성**했다(Whisper의 알려진 한국어
    자막 학습데이터 오염 패턴). arm C는 없었다.

  **Phase 1 단독 판단: 근거 미달.** C의 개선은 유의하나 실질 크기가 0.5%p이고
  `cer_raw`로는 비유의다. 속도는 기각 사유가 아니다(위 판단 원칙 + 실측 2.15배).
  → Phase 2(§2) 결과도 비유의로 나왔다. **최종 결론은 §2 Phase 2 절 참조.**

### 함정 5개 (재발 방지 — 전부 실측으로 확인)

1. **학습 누수**: 위 Zeroth/ghost613. 한국어 파인튜닝 모델은 학습셋을 반드시 확인.
2. **숫자 표기 관습**: Zeroth=한글 수사("천 이백 억원"), FLEURS=아라비아("1,200억").
   데이터셋에 따라 유리한 모델이 뒤바뀐다. → 주지표를 `cer_numfree`(표기 중립)로 두고
   `cer_raw` 병기. 사용자 결정(2026-08-04): 모델 자체 성능 비교가 목적이므로 표기 요인 배제.
3. **비-hf 저장소**: `Qwen/Qwen3-ASR-1.7B`를 transformers로 로드하면
   `multi_modal_projector` 가중치가 **랜덤 초기화**된다(경고 확인). `-hf`가 정본.
4. **정규화 파손**: 수사를 음절 단위로 지우면 `사람들이`→`람들이`, `공사는`→`는`로
   일반 단어가 깨진다. **순수 수사 토큰 전체만** 제거해야 한다.
5. **Windows VRAM spill = 조용한 43배 지연**: 6GB VRAM이 일부 점유된 상태에서 모델을
   올리면 WDDM이 OOM 없이 시스템 RAM으로 넘긴다. 에러도 경고도 없고 결과도 동일한데
   속도만 수십 배 느려진다(arm C 실측 89.97초 vs 2.11초, 전사문 200/200 동일).
   → **속도 수치는 `nvidia-smi`로 VRAM 0MiB 확인 후 단독 실행으로만 측정**한다.
   배치 스크립트에서 arm을 연달아 돌리면 앞 arm의 잔존 점유가 뒤 arm 시간을 오염시킨다.

### 환경 제약 (Windows)

- `torchcodec` **제거 상태로 유지**. 설치하면 `backend="auto"`가 그걸 우선 선택하는데
  FFmpeg 공유 라이브러리가 없어 `libtorchcodec_core{4..8}.dll` 로드가 전부 실패한다.
  제거하면 librosa로 정상 폴백. `datasets`도 `Audio(decode=False)`+soundfile로 우회.
- `.venv_qwen3asr` = Qwen3-ASR 전용 격리 venv(transformers 5.14.1).
  **시스템 transformers는 5.9.0 그대로 유지** — 올리면 Qwen2.5-VL 캡션 출력이 바뀌어
  확정 인덱스 재현성이 깨질 수 있다.
- Qwen3-ASR 호출: `apply_transcription_request(audio=np배열, language="ko",
  processor_kwargs={"pad_to_multiple_of":100})`. 인코더가 feature length를 100프레임
  배수로 요구하고, `**kwargs`로 주면 무시된다(경고 확인). 출력 접두
  `language Korean<asr_text>`는 파싱으로 벗긴다.

## 2. Phase 2~4 (Phase 2 완료, 3~4 미착수)

데이터는 `data/meeting_probe/`(gitignore). 16kHz 모노 wav 5편:
`cabinet26_20260616`(42분) / `cabinet20_20260506`(165분) / `cabinet_20260804`(110분) /
`assembly_finance_20260729`(128분) / `defense_briefing`(117분).
공식 회의록이 **쌍으로 있는 것은 앞의 2편뿐**이고, 그 2편만 Phase 2에 쓸 수 있다.

### Phase 2. 회의 도메인 — 고유명사 재현율  ← **완료(2026-08-05), 회의 2편**

스크립트 `docs/probes/meeting_propnoun.py`. 결과 `_scratch/phase2_propnoun_pooled.json`
(회의별은 `phase2_propnoun_compare_{c26,c20}.json`).

| 회의 | 오디오 | 타깃(동결) | attempted |
|---|---|---|---|
| c26 제26회(2026-06-16) | 42분 | 98 | 73 |
| c20 제20회(2026-05-06) | 165분 | 145 | 128 |
| **합산** | 207분 | **243** | **201** |

arm: `A_prod`(현행 `m3_generate.py` 호출 인자 그대로) / `A_chunked`(같은 모델, 25초 창)
/ `C_chunked`(Qwen3-ASR). **arm B는 제외** — Phase 1 누수 + c26 붕괴로 이미 기각.

#### 결론: 전면교체 근거 없음 (확정)

합산 201건, τ=0.40 (τ 0.34/0.50도 같은 방향·같은 유의성):

| arm | hit | near | 전사 길이비 |
|---|---|---|---|
| C_chunked | 0.791 | 0.194 | 1.08~1.14 |
| A_chunked | 0.781 | 0.204 | 1.00 |
| **A_prod (현행)** | 0.756 | 0.209 | 0.89~0.92 |

| 비교 | delta | CI95 | 유의 | 불일치(승/패) |
|---|---|---|---|---|
| C_chunked vs A_prod | +0.035 | [−0.015, 0.090] | 비유의 | 19 / 12 |
| C_chunked vs A_chunked | +0.010 | [−0.045, 0.065] | 비유의 | 16 / 14 |
| A_prod vs A_chunked | −0.025 | [−0.060, 0.010] | 비유의 | 4 / 9 |

**c26에서 보였던 C의 우세는 재현되지 않았다.** 회의별로 갈라 보면 방향이 뒤집힌다.

| C_chunked vs A_prod | attempted | delta | CI95 | 불일치 |
|---|---|---|---|---|
| c26 (42분) | 73 | **+0.110** | [−0.000, 0.219] | 14 / 6 |
| c20 (165분) | 128 | **−0.008** | [−0.063, 0.047] | 5 / 6 |

c26 단독 결과를 두고 "CI 하한이 0에 붙었으니 표본 부족"이라고 적어뒀는데, **표본을
1.75배로 늘린 c20에서 효과가 사라졌다**(오히려 부호가 반대). 표본 부족이 아니라
c26의 +11%p가 잡음이었다는 쪽이 맞다. 사용자 기준("유의미한 성능향상이 있으면
전면교체")에 대한 답은 **교체하지 않는다**로 확정한다.

*c20의 hit이 전반적으로 높은 이유(0.80~0.82 vs c26 0.66~0.77)*: 지표가 존재 기반이고
회의가 4배 길어 같은 용어가 여러 번 언급되므로 "한 번이라도 맞을" 확률이 올라간다.
회의 간 절대 수치는 비교하지 말고 **회의 내 arm 간 차이만** 읽어야 한다.

#### 재현된 부수 소견 2건 (두 회의 모두에서 같은 방향)

1. **현행 설정은 전사를 가장 적게 뽑는다.** A_chunked 대비 길이비 c26 0.885 / c20 0.916,
   C 대비로는 0.77 / 0.85. `condition_on_previous_text=False` +
   `hallucination_silence_threshold=1.0`은 환각을 줄이지만 **실제 발화도 함께 버린다.**
   불일치 항목에서 A_prod만 놓친 것이 `영국`·`프랑스`·`일본`·`카타르`처럼 어렵지 않은
   단어라는 점이 방증이다(누락이지 오인식이 아니다).
2. **`place` 범주에서 A_prod가 약하다** (합산 0.769 vs A_chunked·C 0.923). c26 단독에서도
   0.688 vs 0.938이었다. 청킹이 지명 인식에 유리한 것이 두 회의에서 재현됐다.

→ 즉 **청킹은 여전히 검토 가치가 있다**(모델 교체 없이 현행 모델로 개선). 다만 전체
hit 차이는 비유의(−0.025, CI [−0.060, 0.010])이고 아래 환각 문제가 걸린다.

#### 부호를 만든 것 — 불일치 항목 원인 분석

c20에서 A_prod가 0.8%p 앞선 것은 **5승 6패 = 순 1건**이다. 그 개별 항목의 원인:

**C가 진 이유 4가지**
1. **계통적 외래어 음소 오류**(유일한 진짜 모델 결함). 같은 오디오·같은 구절에서
   `A_prod: 청년 뉴딜 방안을 추진하여` vs `C: 청년 유딜 방안을 추진하여`.
   `K-뉴딜 아카데미`→`k유딜 아카데미`에서도 재현 — **1개 오류가 2개 타깃을 깎았다.**
2. **청킹 경계 중복**(모델 결함 아님, 실험 설계 탓). `…추진하여 약 방안을 추진하여 약
   10만여…` — 25초 창 2초 겹침 산물. A_prod는 long-form, C는 chunked라 **C만** 이
   불이익을 진다. 조건을 맞춘 A_chunked vs C_chunked는 +0.010 CI [−0.045, 0.065] 동률.
3. **우연 매칭이 만든 허수**. `아세안`이 C 전사의 `아예 안 되거나`에, `AA`(S&P 등급)가
   `ai기술`에 걸렸다. C가 해당 고유명사를 아예 안 적었는데 근사 매칭이 무관한 단어를
   물어 near로 완화됐다 — 지표 한계이고 양쪽 arm에 대칭으로 작용한다.
4. **다어절 조사 차이 — 축자에 가까운 쪽이 불리**. 회의록(개조식) `거시경제 물가대응반`
   vs C `거시경제의 물가대응반`(조사 「의」 삽입, 1편집/9=0.111). 규모는 작다:
   다어절 24건 A_prod 0.792 vs C 0.750, **단어절 104건은 동률 0.817**.

**A_prod가 진 이유는 산발적 오인식이 아니라 누락이다** — 위 부수 소견 1번. 즉 두 arm의
실패 서명이 다르다: A_prod는 누락+자막 환각(두 회의 재현, 구조적),
C는 특정 외래어 음소 오류(소수, 계통적). 부호는 소수 개별 항목이 결정하고
두 arm은 측정 오차 안에서 동등하다.

#### 자막 크레딧 환각 — arm A의 고유 결함

| 회의/arm | chars | 환각 마커 | 최대 5-gram 반복 | 중복률 |
|---|---|---|---|---|
| c26 A_prod | 12,138 | 0 | 2 | 0.026 |
| c26 A_chunked | 13,723 | **7** | 2 | 0.035 |
| c26 C_chunked | 15,689 | 0 | 2 | 0.028 |
| c20 A_prod | 62,787 | **2** | 2 | 0.000 |
| c20 A_chunked | 68,579 | **4** | 3 | 0.008 |
| c20 C_chunked | 74,060 | **0** | 4 | 0.004 |

- `한글자막 by 한효정` / `한글자막 by 한효주`가 삽입된다. c20에서는 **`A_prod`에서도
  나왔다** — 청킹 전용 문제가 아니고 Whisper 한국어 자막 학습데이터 오염이다.
  실제 문맥: "국기에 대하여 경례. 아멘 **한글자막 by 한효정** 네. 닷새 동안에…"
- **arm C는 두 회의 모두 0건.** 회의록 산출물에 없는 문구가 끼어드는 것은 축자 정확도
  보다 심각한 결함이므로, 교체를 안 하더라도 **후처리 필터로 이 패턴은 제거해야 한다**
  (`common.is_corrupted_caption` 계열의 자막 오염 패턴 목록에 추가).

#### 측정 한계 (정직하게)

- **인명 재현율은 이 데이터로 측정 불가.** 합산 person 범주 hit이 3 arm 모두 0.167(n=12)
  이고 c26 참석자 9명은 attempted에서 아예 빠졌다 — 국무회의는 서로를 **직책으로**
  부르므로 이름이 발화되지 않는다. 회의록의 `• 대통령 이재명`은 화자 라벨이지 발화가
  아니다. attempted 규칙이 이를 올바르게 걸러낸다(설계 검증). 인명은 Phase 3 화자분리
  영역이다.
- 판정은 **문자열 존재 기반**이라 위치가 맞는지는 보지 않는다. 고유명사가 엉뚱한
  곳에 있어도 hit이 된다.
- c20 공개 영상은 165분인데 회의록은 10:00~13:33(213분)이다. 누락 구간의 타깃은
  attempted 규칙이 걸러내지만, 걸러지지 않은 채 남은 것이 있을 수 있다.
- 속도 수치는 순차 배치로 재서 **상한값**이다(함정 5 참조).

#### 사고 2건 — 재발 방지

1. **기준선을 라이브러리 기본값으로 세웠다(c26 1차).** 그때 "C가 현행을 15%p 유의하게
   이긴다", "청킹만 바꿔도 +9.5%p 유의 개선"이라는 결론이 나왔고 **둘 다 무효**다.
   `m3_generate.py:41-44`의 인자를 그대로 복사한 `A_prod`를 넣으면 두 비교 모두
   비유의가 된다. → **현행 arm은 운영 코드의 호출 인자를 복사할 것.**
2. **산출물 접미를 회의별로 안 붙였다.** c26만 하위호환으로 접미를 비워뒀더니
   c26의 glob `phase2_hyp_*.json`이 `phase2_hyp_A_prod_c20.json`까지 잡아
   **c26 타깃을 c20 전사문으로 채점**했다. 첫 `--pool` 결과가 여기 걸렸다
   (run 메타의 `audio_sec`가 두 회의 모두 9906초로 찍힌 것이 단서). 접미를 항상
   붙이도록 고치고 재채점했다. → **glob 기반 수집에서 접미 없는 키를 만들지 말 것.**

#### 설계 (스크립트 docstring에 상세)

- **타깃 사전 동결**: 회의록의 **발화 단락**에서만 추출하고 전사 **전에** 동결
  (sha256 기록). 범위 개회~의안심의, 그 안에서 문서기호(□▷◆※❶…) 시작 단락과
  개조식 종결 없는 표 조각을 단락 단위로 제외. 화자 라벨은 인명 표기 출처로 보존
  (단 `ㅇ (109전화…)` 같은 첨부문이 `ㅇ`를 공유하므로 "직책 접미 + 끝에 2~4자 한글
  이름"으로 좁힘). c26 제외 1건(`특명전권대사`), c20 제외 4건 — 전부 첨부문에만 존재.
  **규칙 교체 검증**: 단락 규칙을 c26에 적용해 기존 98타깃이 누락 0건으로 재현되는 것을
  확인한 뒤 c20에 적용했다.
- **지표**: 타깃별 근사 부분열 편집거리 d(시작·끝 자유). d=0 hit / 0<d≤τ near /
  d>τ absent. **분모 = 어느 arm이라도 d≤τ인 attempted 집합**(발화되지 않은 항목이
  분모를 오염시키는 것을 막는다). τ=0.40 사전 고정, 0.34/0.50 민감도 병기.
  쌍체 부트스트랩 95% CI(B=2000, seed 42 — DESIGN_SPEC 8-1(b)) + 불일치쌍 수.
- **공정성**: 청킹 계획을 오디오 길이만으로 결정(25초 창·2초 겹침)해 특정 arm의 VAD를
  쓰지 않는다.

#### 남은 일

- **회의록 없는 오디오 3편**(`cabinet_20260804` 110분 / `assembly_finance_20260729`
  128분 / `defense_briefing` 117분)은 이 지표로 쓸 수 없다. 공식 회의록이 공개되면 편입.
- `term` 범주는 합산에서도 C가 앞서지만(0.857 vs 0.786, n=70) 검정하지 않았다.
  사후 부분집합 검정은 다중비교 문제가 있다 — 필요하면 **다음 회의에서 사전 등록**하고
  검정할 일이다. 지금 근거로 삼지 마라.

### Phase 3. pyannote 화자분리

`pyannote/speaker-diarization-community-1`(CC-BY-4.0, 게이트). **HF 토큰 확보 완료** —
`paper/.env`의 `HF_TOKEN`(계정 `oceanspr`, role read). 게이트 4개 저장소
(community-1, segmentation-3.0, diarization-3.1, wespeaker) 접근 전부 확인됨.
`pyannote.audio 4.0.5` 설치됨. torchcodec 없이 동작하는지 확인 필요 —
안 되면 파일 경로 대신 파형 텐서 직접 전달로 우회.

*`.env` 파싱 주의: 등호 주변 공백·따옴표를 모두 벗겨야 한다. 따옴표가 남으면
`Bearer "hf_..."`로 나가 401(실측 확인).*

### Phase 4. 회의록·화자별 요약 생성 평가

M8/M9식 생성+판정. 서버 GPU 필요(로컬 6GB 불가).

## 3. 전면교체 시 주의 (결론은 "교체 안 함" — 아래는 향후 교체 시 대비 기록)

STT 교체는 **확정 config 변경**이다. `subtitle`이 바뀌면 `text_hash` 불일치로 M5가
`ValueError`로 막고, 재임베딩+test 재평가가 필요하다(CLAUDE.md 절대규칙 1 — dev 완료 후
사용자 승인). 또한 기존 7영상에 M1 `--force`를 쓰면 `audio.wav` mtime이 바뀌어 STT
캐시가 무효화되고 재전사되므로 **금지**.

*Whisper 전사는 완전 결정적이지 않다: `temperature` 기본값이 fallback 리스트
`[0.0, 0.2, ..., 1.0]`이라 임계값 미달 구간은 샘플링으로 재시도된다. 평소에는
`stt_cache.json`이 재전사를 막아 결과가 고정된다.*

**교체를 안 하기로 했으므로 지금 할 일은 두 가지뿐이다**(둘 다 확정 config를 안 건드림):
1. **자막 크레딧 환각 후처리 필터** — `한글자막 by …` / `자막 제공 …` 패턴. 현행
   `A_prod`에서도 나오므로 교체와 무관하게 필요하다. 다만 `subtitle` 텍스트를 바꾸면
   `text_hash`가 달라져 재임베딩·test 재평가가 걸린다 — **dev에서 영향 범위를 먼저 재고**
   사용자 승인 후 적용(CLAUDE.md 절대규칙 1).
2. **청킹 검토** — `place` 범주 개선이 두 회의에서 재현됐지만 전체 hit은 비유의이고
   청킹이 환각을 늘린다(c26 A_prod 0건 → A_chunked 7건). 순이득이 불분명하므로
   보류하고, 위 1번 필터가 붙은 뒤에 다시 본다.

## 4. 랩실 GPU (별건, 대기 중)

우선순위: ① Qwen2.5-VL-7B 캡션 교체(같은 계열이라 문체 위험 낮음) → ② M8/M9 실행.
계정·저장위치·예약 규칙은 메모리 `<LAB>_gpu_server_usage` 참조. HF_HOME을 `/hdd`로
리다이렉트 먼저.

## 5. 미결 결정 1건

`IMPLEMENTATION_GUIDE` 9-1(c)의 "2인 이상 라벨링 후 일치율 병기"가 **미이행**이다
(단일 라벨러, 일치율 미계산). 9-1-1에 선택지 A(문서를 현실에 맞춤) / B(표본 30~40건
유형 태그만 제2 라벨러, Cohen's kappa)를 제시했고 사용자 답변 대기 중.
